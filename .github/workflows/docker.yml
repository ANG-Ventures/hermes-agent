name: Isolated freeze-drain lifecycle probe
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  freeze-proof:
    runs-on: [self-hosted, linux, x64, freeze-probe-t-c975f014]
    timeout-minutes: 10
    steps:
      - name: Finish naturally across supervisor freeze
        run: |
          printf 'probe-start\n'
          sleep 180
          printf 'probe-completed\n'
