name: Publish E2E evidence

# This runs only from the default branch after CI completes. It intentionally
# checks out main, never the PR ref, and treats the downloaded artifact as
# untrusted input before uploading validated GitHub attachments.
on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    # A ci-overflow-ledger data commit must start zero workflows (spec §5.3).
    branches-ignore: [ci-overflow-ledger]

permissions:
  actions: read
  checks: write
  contents: read
  pull-requests: write

concurrency:
  group: publish-e2e-evidence-${{ github.event.workflow_run.id }}
  cancel-in-progress: false

jobs:
  publish:
    name: Publish inline E2E evidence
    if: github.event.workflow_run.event == 'pull_request'
    runs-on: ${{ fromJSON(vars.CI_RUNNER_LABELS || '["ubuntu-latest"]') }}
    timeout-minutes: 10
    environment: gh-image
    steps:
      - name: Check out trusted publisher
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false

      - name: Download and attach evidence
        # Deliberately NO ``continue-on-error``. Every call in this step
        # spends the shared installation rate-limit budget, and when that
        # budget ran out on 2026-09-19 the step failed 10 times and painted
        # a red X on PRs that had nothing wrong with them - but suppressing
        # the whole step also swallowed missing artifacts, bad credentials
        # and a broken gh extension, letting the workflow report success
        # while attaching nothing, which is its entire job.
        #
        # ``publish_evidence_step.sh`` therefore implements a three-valued
        # policy instead: RED on any real publish defect, GREEN on success
        # or a deliberate skip, and - only for a transient GitHub API
        # condition (rate limit or 5xx) that survives a bounded retry - a
        # visible NEUTRAL: a ``neutral`` check run on the head SHA plus a
        # warning annotation and a job-summary line. Never a silent green.
        #
        # ``checks: write`` above exists solely for that neutral check run.
        #
        # The gh-image extension is installed INSIDE that classifier and is
        # verified against a pinned SHA-256 rather than a mutable tag, so a
        # retargeted tag or a swapped release asset fails the step RED
        # instead of executing with this job's token.
        #
        # The OSV scan's SARIF upload is deliberately NOT given any such
        # treatment: ``osv-scanner`` IS in ``all-checks-pass.needs``, so
        # silencing it would hide a required check. This workflow is not in
        # that list, which is what makes a neutral result safe here.
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_TOKEN: ${{ github.token }}
          GH_SESSION_TOKEN: ${{ secrets.GH_IMAGE_SESSION_TOKEN }}
          SOURCE_REPO: ${{ github.repository }}
          SOURCE_RUN_ID: ${{ github.event.workflow_run.id }}
          HEAD_OWNER: ${{ github.event.workflow_run.head_repository.owner.login }}
          HEAD_BRANCH: ${{ github.event.workflow_run.head_branch }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          bash scripts/ci/publish_evidence_step.sh
