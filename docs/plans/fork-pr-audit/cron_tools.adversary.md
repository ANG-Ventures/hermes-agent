# cron+tools tranche - ADVERSARY (t_c0efb45a)

Input: audit/cron_tools/verdicts @ 257ff440ba (auditor t_80701715). Fork origin/main tree on that branch; upstream/main 1b57acf94a (115 commits past the auditor's 59004a6235 - every upstream claim re-checked at the new HEAD).

Method per challenged row: `git merge-base --is-ancestor` on every cited upstream sha (all 35 OK, deferred shas resolved below); git grep of every def/class/CONST the row added for consumers OUTSIDE its own paths on origin/main; live fleet config (14 config.yaml, read-only), live cron store (533 jobs / 332 scheduled), skills-shared (read-only), all 14 state.db (read-only) for tool-call counts; docs/sync/fork-features.json lifecycle; fork-parity-doctrine D10a; the fork's own tests copied onto ../up (repo venv python 3.11, narrow pytest only) and two live probes (`adv_probe_guard.py` lifecycle guard, `adv_probe_snapshot.py` shell-snapshot leak) run on both trees with the same interpreter; every revert branch diffed against origin/main.

## Totals

- challenged: 35 (DROP 7, SUPERSEDED-BY-UPSTREAM 28)
- STANDS 25 / OVERTURNED 7 / NEEDS-LEAD 3
- KEEP-UNPROVEN: 14 of 45 KEEP rows (list at the end)

## Overturned / needs-lead (read these first)

- **nopr:bb4e64499c** (SUPERSEDED-BY-UPSTREAM): OVERTURNED -> KEEP. One seam: bb4e64499c + 6ae6cbe84c + #130. Taking upstream's rule would silently remove the fallback net from the 17 opt-in jobs and every per-job `fallback` list. Trim candidate: retire the HERMES_CRON_ALLOW_CROSS_PROVIDER_FALLBACK env switch (AGENTS rubric). Candidate UPSTREAM offer: per-job fallback list + explicit cross-provider opt-in on top of e7bff4b6d8.
- **#591** (SUPERSEDED-BY-UPSTREAM): OVERTURNED -> UPSTREAM. False-positive half absorbed; the cd-chain resolution is not. Keep on fork until ported (audit/cron_tools/upstream-591); conflicts with upstream lifecycle_guard.py expected (2,469 vs 1,285 lines).
- **#351** (DROP): OVERTURNED -> SUPERSEDED-BY-UPSTREAM. Not a bare DROP: revert branch audit/cron_tools/revert-351 reopens the 2026-07-15 leak until the sync takes tests/home_io_guard.py + the conftest install. Sequence: take home_io_guard first, then revert-351.
- **#411** (SUPERSEDED-BY-UPSTREAM): OVERTURNED -> KEEP. NEEDS-LEAD flag on the larger seam: the agent-settable cron model pin (object schema + coercion) is fork-permanent in practice and has no docs/sync/fork-features.json entry. Every sync will re-delete it.
- **nopr:4d626ac813** (SUPERSEDED-BY-UPSTREAM): OVERTURNED -> UPSTREAM. Blast radius is smaller than in July: both trees now key gateway environments session:<key> (up tools/terminal_tool.py:458-500), so the residual is the shared 'default' env (CLI + cron ticker) and subagents sharing the parent env. Offer upstream the HERMES_HOME= + HERMES_KANBAN_* exclusions (unit with #543, nopr:124a8c0706's regex widening).
- **#543** (SUPERSEDED-BY-UPSTREAM): OVERTURNED -> UPSTREAM. Fold into the nopr:4d626ac813 upstream offer (one exclusion-list PR).
- **nopr:f504b1c928** (DROP): NEEDS-LEAD -> DROP pending Ace ruling on registry entry 4 (fork-permanent). Drop is sound on usage; the registry ruling is Ace's to retire (task step 3). Revert branch is otherwise safe (+3/-735; _DEFAULT_OFF_TOOLSETS import in tui_gateway/methods_profiles.py:650 survives).
- **nopr:091b3f915d** (DROP): NEEDS-LEAD -> follows nopr:f504b1c928. 
- **nopr:7e505547a7** (DROP): NEEDS-LEAD -> follows nopr:f504b1c928. 
- **#786** (SUPERSEDED-BY-UPSTREAM): OVERTURNED -> KEEP. Behaviour parity upstream is real (take 98a3324821), but the helper + AST ratchet are the fork-permanent seam. At sync route upstream's inline sites through the helper. Lead: this seam has no docs/sync/fork-features.json entry although D10a requires one.

## Challenged rows

| key | auditor verdict | adversary result | evidence (path:line or sha) | notes |
|---|---|---|---|---|
| #482 | SUPERSEDED-BY-UPSTREAM | STANDS | up cron/scheduler.py run_one_job :2705-2741 (create_execution / finish_execution on dispatch failure) + :3160-3177 (finish_execution on claim reject, mark_execution_running CAS before the run); ledger introduced upstream d9dd05b69d 2026-07-09 (feat(cron): add truthful execution ledger). Fork test tests/cron/test_ticker_execution_ledger.py run on up 1b57acf94a: 5 failed / 1 passed, every failure StopIteration in the test's own source-text helper (`def _process_one_job` is no longer a top-level function upstream) - shape, not behaviour. | Sha the auditor deferred: d9dd05b69d. Fork scheduler.py 9,154 lines vs upstream's split (scheduler_delivery.py) means the drop only happens at a whole-file sync. |
| nopr:bb4e64499c | SUPERSEDED-BY-UPSTREAM | OVERTURNED -> KEEP | Upstream is stricter, not equivalent: up hermes_cli/fallback_config.py:123 scoped_fallback_chain returns None for a pinned owner when declared is None, and up cron/scheduler.py:108 _job_fallback_chain always passes declared=None -> a provider/model-pinned cron job upstream has NO fallback chain at all (e7bff4b6d8 2026-09-23). Upstream has no per-job `fallback` list and no `allow_cross_provider_fallback` opt-in. Live store ~/.hermes/cron/jobs.json (read 2026-09-25): 90 provider-pinned jobs, 17 jobs set allow_cross_provider_fallback: true (daily-journal, morning-digest, x-feed-brief, weekly-review, skill-patch-applier, debug-log-analysis, test-honesty-sweep, parity-drift-watch, weekly-pr-sweep, ...), 845 filter fires in the auditor's window. Both KEEP rows nopr:6ae6cbe84c and #130 return through _filter_fallback_chain_for_pinned_job (fork cron/scheduler.py:788 _resolve_job_fallback_chain). | One seam: bb4e64499c + 6ae6cbe84c + #130. Taking upstream's rule would silently remove the fallback net from the 17 opt-in jobs and every per-job `fallback` list. Trim candidate: retire the HERMES_CRON_ALLOW_CROSS_PROVIDER_FALLBACK env switch (AGENTS rubric). Candidate UPSTREAM offer: per-job fallback list + explicit cross-provider opt-in on top of e7bff4b6d8. |
| #614 | SUPERSEDED-BY-UPSTREAM | STANDS | adv_probe_guard.py on up 1b57acf94a: inert python heredoc containing 'hermes gateway restart' -> blocked=False; executed heredocs (bash <<EOF / sh -s <<EOF) -> blocked=True. Fork origin/main blocks the inert case again (rows #920/#1017 re-scan Python bodies), so #614's intent is already overridden on the fork itself. 9aa0721b23 / 307cc814ad verified ancestors of upstream/main. | Upstream also misses `python3 - <<EOF os.system('hermes gateway restart')` (blocked=False) - that is #920/#1017's row (UPSTREAM), not #614's. |
| #624 | SUPERSEDED-BY-UPSTREAM | STANDS | auditor probe_guard.py replayed on up 1b57acf94a: '#624 dir ref' blocked=False (ok). 9ac1e65b0a verified ancestor. |  |
| #591 | SUPERSEDED-BY-UPSTREAM | OVERTURNED -> UPSTREAM | adv_probe_guard.py on up 1b57acf94a: `cd <dir> && ./proj` where ./proj is an executable whose body is `hermes gateway restart` -> blocked=False on upstream, blocked=True on fork 257ff440ba (benign `cd <dir> && ./stats` -> False on both). Upstream 9ac1e65b0a removes the false positive by never resolving through the cd chain; the fork resolves the launcher and scans it. The fork half that upstream lacks is the hardening the guard exists for (a gateway restart launched from inside its own turn). | False-positive half absorbed; the cd-chain resolution is not. Keep on fork until ported (audit/cron_tools/upstream-591); conflicts with upstream lifecycle_guard.py expected (2,469 vs 1,285 lines). |
| #818 | SUPERSEDED-BY-UPSTREAM | STANDS | auditor probe replayed on up 1b57acf94a: '#818 data file' blocked=False (ok). d46abd445f verified ancestor. |  |
| #348 | SUPERSEDED-BY-UPSTREAM | STANDS | fork tests/cron/test_store_resolves_live_home.py copied as tests/cron/adv_test_store_resolves_live_home.py on up 1b57acf94a: `2 passed in 0.94s` (repo venv python 3.11, HERMES_HOME=mktemp). Upstream shas the auditor deferred: ec0227b435 2026-07-09 fix(cron): isolate profile store paths by context; 6c98eb2d45 2026-07-22; a65a647b04 2026-07-24 (_current_cron_store). |  |
| #351 | DROP | OVERTURNED -> SUPERSEDED-BY-UPSTREAM | Auditor's 'no equivalent guard upstream' is false: up tests/home_io_guard.py (class HomeIOGuard, added 1c8fae6180 2026-09-09) is installed from up tests/conftest.py:1403-1405 and refuses Python filesystem writes from tests into the real Hermes home - the generic, conftest-level form of #351's store-level guard. Fork has no tests/home_io_guard.py (ls: No such file). Fork test on up: 3 failed (AttributeError _guard_against_test_write_to_live_store absent) + the remaining cases trip home_io_guard itself (FileNotFoundError/PermissionError from tests/home_io_guard.py:124 when the test points at /Users/nobody). The incident #351 was filed for is real: ~/.hermes/cron/jobs.json.EMPTY-INCIDENT-20260715-1730 (1.3K, live store replaced by a single 'research-rail-canary' fixture job). | Not a bare DROP: revert branch audit/cron_tools/revert-351 reopens the 2026-07-15 leak until the sync takes tests/home_io_guard.py + the conftest install. Sequence: take home_io_guard first, then revert-351. |
| #126 | SUPERSEDED-BY-UPSTREAM | STANDS | fork tests/cron/test_per_job_reasoning_effort.py copied to up 1b57acf94a as adv_test_per_job_reasoning_effort.py: `14 passed in 2.68s`. Upstream sha the auditor deferred: 4e1dd1a74b 2026-08-20 feat(cron): per-job reasoning_effort override in job definitions. Live: 5 scheduled jobs carry top-level reasoning_effort (morning-digest xhigh, x-feed-brief xhigh, skill-patch-applier xhigh, debug-log-analysis medium, test-honesty-sweep high) - the same key upstream reads. | Retire with it: cron/fork_ext/scheduler_ext.py::resolve_cron_reasoning_config + tests/golden/scheduler_ext + the reasoning half of docs/sync/fork-features.json entry 6 (upstream-intended -> absorbed). |
| #411 | SUPERSEDED-BY-UPSTREAM | OVERTURNED -> KEEP | Upstream d464ae3652 (2026-07-28, feat(cron): user-owned model pins) REMOVED `model` from the model-facing CRONJOB_SCHEMA: the tool exposes only `pinned: bool` ('locks the CURRENT main agent model'); model/provider are a 'CLI-only lane: absent from CRONJOB_SCHEMA and the model dispatch (models don't pick models)' (up tools/cronjob_tools.py:618). The fleet's cron doctrine requires the AGENT to pin the cron slot: ~/.hermes/skills-shared/general/scheduler/SKILL.md:146,208,294,299 (`model={"model":"gpt-6-sol-900k","provider":"openai-codex"}` on every LLM cron; cron-config-lint REPIN_MODEL auto-repins drift). Fork schema tools/cronjob_tools.py:2613-2627 keeps the object form and #411's _coerce_model_override_arg (:962) accepts the flat string agents frequently pass. The auditor's 'problem cannot occur on upstream's schema' holds only because upstream deleted the argument - which the fleet cannot take. | NEEDS-LEAD flag on the larger seam: the agent-settable cron model pin (object schema + coercion) is fork-permanent in practice and has no docs/sync/fork-features.json entry. Every sync will re-delete it. |
| #148 | SUPERSEDED-BY-UPSTREAM | STANDS | Upstream sha the auditor deferred: 7ecbe50d98 2026-09-08 fix(cron): validate script existence (tools/cronjob_job_args.py _validate_cron_script_path). Wording-only delta. |  |
| nopr:f578c82019 | DROP | STANDS | census duplicate of #71 (identical patch-id); row-level only, nothing separate to revert. |  |
| #83 | SUPERSEDED-BY-UPSTREAM | STANDS | get_send_origin/set_send_origin/_SEND_ORIGIN_* have 0 consumers outside the PR paths (git grep origin/main); copy_context().run at up tools/delegate_tool_child_run.py:862 + delegate_tool_dispatch.py:150 verified by the auditor; shas 8e9628e9cf / 52419bda5d / 64957b7e02 ancestors. | Unit with #71 (UPSTREAM); not independently revertable. |
| #88 | SUPERSEDED-BY-UPSTREAM | STANDS | fb6446fc9e ancestor; registry docs/sync/fork-features.json entry 1 lifecycle=absorbed (2026-08-31, measured). DEPENDENT the auditor missed: is_cron_session() has a live consumer outside the PR - tools/terminal_tool.py:179-181 (`session_is_messaging_surface() and not is_cron_session()`, added a09ec2ea99 2026-09-25 messaging foreground cap) inside try/except -> returns False (feature silently off) if the symbol disappears. | At the sync rewire terminal_tool.py to get_session_env('HERMES_CRON_SESSION') (up tools/approval_context.py:126-128 / gateway/session_context.py:173); add '#a09ec2ea99 consumer' to dependents. |
| #583 | SUPERSEDED-BY-UPSTREAM | STANDS | up tools/delegate_tool_toolsets.py:110 strips after all three branches; 3e96c87f37 / d77505c007 ancestors. |  |
| nopr:30a8a063e9 | SUPERSEDED-BY-UPSTREAM | STANDS | up tools/delegate_tool_toolsets.py:14-21 DELEGATE_BLOCKED_TOOLS has no execute_code; part 2 undone by #583 on the fork. |  |
| #415 | SUPERSEDED-BY-UPSTREAM | STANDS | fcce28b5a4 / 1bfbeff4e5 ancestors; _DRAIN_DEADLINE_SECONDS/_DRAIN_MAX_BYTES 0 consumers outside process_registry.py. |  |
| #788 | SUPERSEDED-BY-UPSTREAM | STANDS | 1bfbeff4e5 ancestor (fix(process): reap children after early stdout EOF). |  |
| nopr:4d626ac813 | SUPERSEDED-BY-UPSTREAM | OVERTURNED -> UPSTREAM | Auditor's 'test module collection error on BOTH trees' was an import path (fork test imports the regex from tools.environments.base; upstream moved it to base_session_env). With that one import fixed, fork tests/tools/test_env_snapshot_session_leak.py on up 1b57acf94a: `2 failed, 2 passed` - test_incident_sequence_foreign_identity_does_not_stick finds `declare -x HERMES_HOME="/tmp/foreign_home"` in the snapshot; test_exclude_pattern_shape: up regex (base_session_env.py:29-31) does not match HERMES_HOME. Live probe adv_probe_snapshot.py (LocalEnvironment, same interpreter): cmd1 `export HERMES_HOME=/tmp/foreign_home HERMES_KANBAN_TASK=t_foreign`, cmd2 echo -> up: HOME_=/tmp/foreign_home TASK=t_foreign; fork: HOME_=<process value> TASK=EMPTY. up unset list (base_session_env.py:69-77) covers HERMES_SESSION_*/HERMES_CRON_AUTO_DELIVER_*/HERMES_BROWSER_CONTROL_*/HERMES_DELEGATED_CHILD_CONTEXT/HERMES_CRON_SESSION/HERMES_UI_SESSION_ID only. f2e32ceead absorbed the HERMES_SESSION_* half, not the HERMES_HOME half the 2026-07-10 incident was about. | Blast radius is smaller than in July: both trees now key gateway environments session:<key> (up tools/terminal_tool.py:458-500), so the residual is the shared 'default' env (CLI + cron ticker) and subagents sharing the parent env. Offer upstream the HERMES_HOME= + HERMES_KANBAN_* exclusions (unit with #543, nopr:124a8c0706's regex widening). |
| nopr:124a8c0706 | SUPERSEDED-BY-UPSTREAM | STANDS | mktemp temp-name pin absorbed by 911d380296 (up base_session_env.py:36-37, :88-97). The regex-widening half rides the nopr:4d626ac813 unit (OVERTURNED -> UPSTREAM). |  |
| #543 | SUPERSEDED-BY-UPSTREAM | OVERTURNED -> UPSTREAM | Half absorbed: up c44b423e52 unsets HERMES_DELEGATED_CHILD_CONTEXT + HERMES_CRON_SESSION (base_session_env.py:74-76) but not ${!HERMES_KANBAN_*} (fork base.py:664-669 unsets both). adv_probe_snapshot.py on up 1b57acf94a: HERMES_KANBAN_TASK=t_foreign exported by one command is re-sourced by the next (TASK=t_foreign); fork: TASK=EMPTY. The dispatcher-owned worker markers the PR body names are exactly the un-absorbed half. | Fold into the nopr:4d626ac813 upstream offer (one exclusion-list PR). |
| #603 | SUPERSEDED-BY-UPSTREAM | STANDS | 3d12e86ef1 / 3bbec90f23 / 6a5a6a05d2 ancestors; fleet terminal.backend is `local` on all 14 config.yaml (read-only grep), no modal/daytona/vercel backend configured anywhere -> the patched path never runs on this fleet. |  |
| #345 | SUPERSEDED-BY-UPSTREAM | STANDS | 911d380296 / 0746903679 ancestors; production half (mktemp snapshot publish) at up base_session_env.py:88-97. |  |
| #211 | DROP | STANDS | auxiliary.vision.ytdlp_ssrf_proxy absent from ~/.hermes/config.yaml and all 13 profiles/*/config.yaml (read-only grep, 0 hits for 'ssrf'); SsrfFilteringProxy/ip_is_blocked/_ssrf_proxy_enabled 0 consumers outside the PR paths (git grep origin/main); 0 skill references. Revert branch audit/cron_tools/revert-211 (+20/-1103) keeps yt-dlp ingestion (15 yt_dlp hits remain in tools/vision_tools.py) and removes only proxy wiring. | The redirect-time SSRF hole #211 closes is open on the fork TODAY (proxy never enabled) and stays open after the revert - no change in exposure. Re-offer alongside #209 (upstream PR #58144) if that lands. |
| nopr:f504b1c928 | DROP | NEEDS-LEAD -> DROP pending Ace ruling on registry entry 4 (fork-permanent) | docs/sync/fork-features.json entry 4 'messaging + moa toolsets present' lifecycle=fork-permanent ('The fork intentionally exposes agent-callable messaging and MoA toolsets that upstream does not ship'), tests tests/agent/test_fork_custom_toolsets.py (the revert deletes them). Usage re-measured (read-only state.db, all 14 profiles): send_message tool_calls 19 in ~/.hermes (last 2026-06-23) + 2 aegis (last 06-09), mixture_of_agents 14 (last 2026-07-10), 0 elsewhere - dormant 3 months, auditor's numbers confirmed. 'messaging'/'moa' appear in platform_toolsets of all 14 config.yaml (become unknown-toolset warnings, model_tools.py:454); ~/.hermes/skills-shared/general/notify/SKILL.md:158-168 documents send_message for cross-destination sends; the `moa:` config section is the provider preset (stays). | Drop is sound on usage; the registry ruling is Ace's to retire (task step 3). Revert branch is otherwise safe (+3/-735; _DEFAULT_OFF_TOOLSETS import in tui_gateway/methods_profiles.py:650 survives). |
| nopr:091b3f915d | DROP | NEEDS-LEAD -> follows nopr:f504b1c928 | same unit / same revert branch. |  |
| nopr:7e505547a7 | DROP | NEEDS-LEAD -> follows nopr:f504b1c928 | same unit / same revert branch. |  |
| #786 | SUPERSEDED-BY-UPSTREAM | OVERTURNED -> KEEP | (a) Auditor's fires premise is false: approvals.mode is 'off' at ~/.hermes/config.yaml:584 and profiles/aegis/config.yaml:498, and `mode: false` on argus/athena/cadmus/daedalus/daedalus-astra/-fable/-opus/-sol/marconi/momus - YAML off -> bool False, normalized to "off" in both trees (up tools/approval_context.py:205-206, fork tools/approval.py:3433-3434). 13 of 14 profiles run mode=off (chiron: manual), so the mode=off bypass is the fleet's primary approval bypass and fires on every dangerous-command gate. (b) Doctrine D10a (~/.hermes/skills-shared/coding/fork-parity-doctrine/SKILL.md:839-880): 'any fork-owned policy predicate that the config can widen or narrow is a fork-permanent seam and gets (a) a single helper, (b) an AST guard, (c) a registry entry' - the measured drift class from the #643 sync. Upstream 98a3324821 restores the behaviour (up tools/approval.py:973 _run_approval_gate, :1179, :1257, :488) but in the inline-at-4-sites form D10a documents losing terms at merge; fork keeps is_approval_bypass_active() at 4 sites (:3804/:4104/:4754/:5390) + tests/tools/test_approval_mode_off_bypass.py. | Behaviour parity upstream is real (take 98a3324821), but the helper + AST ratchet are the fork-permanent seam. At sync route upstream's inline sites through the helper. Lead: this seam has no docs/sync/fork-features.json entry although D10a requires one. |
| nopr:ae6ebc87c4 | SUPERSEDED-BY-UPSTREAM | STANDS | fb6446fc9e ancestor; docs/sync/fork-features.json entry 1 lifecycle=absorbed 2026-08-31 with measured probes (parent-in-cron / copy_context child / bare-submit control). |  |
| #387 | DROP | STANDS | Fires corrected: 4 discord action=react tool calls by Apollo (~/.hermes/state.db, 2026-07-20 16:25 .. 2026-07-21 08:46), 0 in all 13 profile state.dbs, 0 in the 66 days since - the auditor's '0 ever' missed the escaped-JSON encoding of tool_calls. _react/_unreact 0 consumers outside tools/discord_tool.py (gateway/relay/adapter.py:_react is the adapter's own method); 0 cron jobs, 0 non-archived skills reference the action. | Dormant since 2026-07-21; revert-387 safe (+1/-177). |
| #5 | SUPERSEDED-BY-UPSTREAM | STANDS | 2c8a932f80 / aed2e156ad ancestors; same message strings upstream (auditor read). |  |
| #98 | SUPERSEDED-BY-UPSTREAM | STANDS | a5f94e93ad ancestor. Fleet ACP use checked: cron acp-harness-regression (monthly) runs ~/.hermes/scripts/test_delegate.py --phase G against EXTERNAL ACP adapters (codex-acp, claude-agent-acp), not Hermes's acp_adapter kanban stamping. CLI kanban workers keep os.environ via get_session_env's CLI fallback. |  |
| #252 | SUPERSEDED-BY-UPSTREAM | STANDS | 0a75616514 ancestor; fork test 4 passed on up (auditor). browser.cdp_url set on 6 profiles. |  |
| #784 | SUPERSEDED-BY-UPSTREAM | STANDS | f8f4d056f5 ancestor (cherry-picked with -x). |  |
| #585 | SUPERSEDED-BY-UPSTREAM | STANDS | all 9 pick shas verified ancestors of upstream/main 1b57acf94a. |  |

## KEEP-UNPROVEN

KEEP rows with no positive fire count and/or whose only original evidence is the commit message. Not overturned - flagged for the lead (an unmeasured 'still needed' is not a KEEP per README section 2).

| key | subject | why flagged | auditor fires | auditor original |
|---|---|---|---|---|
| #689 | refactor(delegation): retire unclaimed SQLite acknowledgement helper (#689) | fires unmeasured/0 | n/a (deletion; nothing to fire) | PR #689 body: zero production callers. |
| nopr:8d20d24ed0 | fix(sync): CI round-3 — durable-route wake target + nousbot contributor mapping | fires unmeasured/0; original = commit message only | n/a (glue, no log line) | commit body: satisfies upstream-new test_apiserver_session_with_id_dispatches_background. |
| #1026 | fix(cron): honour the fleet host-down gate in cron failure delivery (#1026) | fires unmeasured/0 | 0 in window (merged 2026-09-25, same day as audit; gate only fires during a host-down incident); logs: 685 files (~/.hermes/logs agent.log*  | PR #1026 body, card t_8c04a5d2; 2026-09-23 ACE-AI outage 14:01-20:31 PT, qbt-private-missingfiles-monitor paged 26x. |
| nopr:d5b2992f2d | feat(cron): hermes cron run <id> --wait — synchronous single-job execution | original = commit message only | 'Error processing job' 15 + 'Output saved to:' 4 in logs; logs: 685 files (~/.hermes/logs agent.log* 09-24 12:44→09-25, gateway.log* 09-13→0 | commit body; fleet alert templates emit `hermes cron run <name> --wait` as triage step (#507 body). |
| #565 | fix(cron): make manual one-shot runs execute (#565) | fires unmeasured/0 | no log marker; logs: 685 files (~/.hermes/logs agent.log* 09-24 12:44→09-25, gateway.log* 09-13→09-25, profile logs 06-02→09-25) | PR #565 body (root cause: shared _process_one_job + claim_dispatch false -> success=True no-op). |
| #507 | fix(cron): `cron run <name> --wait` accepts a job NAME, not just an ID (#507) | fires unmeasured/0 | no log marker; logs: 685 files (~/.hermes/logs agent.log* 09-24 12:44→09-25, gateway.log* 09-13→09-25, profile logs 06-02→09-25) | PR #507 body: alert templates use the job NAME; operators hit 'Job not found'. |
| #473 | fix(cron): attach job identity to turn telemetry (#473) | fires unmeasured/0 | not measurable: ~/.hermes/turns.db and blackbox.db are 0 B on this host (telemetry store moved); logs: 685 files (~/.hermes/logs agent.log*  | PR #473 body (identity-judgment table). |
| nopr:6ae6cbe84c | feat(cron): per-job fallback chain (same-provider resilience) | original = commit message only | 18 jobs define fallback (17 scheduled); live job stores ~/.hermes/cron/jobs.json + profiles/*/cron/jobs.json read 2026-09-25: 521 jobs, 338  | commit body (#21444 recurring gpt-5.5 silent reject). |
| nopr:05d38f1d0d | feat(cron): per-job api_max_retries override | original = commit message only | 1 job sets api_max_retries; live job stores ~/.hermes/cron/jobs.json + profiles/*/cron/jobs.json read 2026-09-25: 521 jobs, 338 scheduled | commit body (morning-digest on openai-codex). |
| #323 | fix(cron): swallow transient provider-capacity 503s on recurring jobs; name the  | fires unmeasured/0 | 0 log lines (suppression silent; not measurable from logs); logs: 685 files (~/.hermes/logs agent.log* 09-24 12:44→09-25, gateway.log* 09-13 | PR #323 body (skill-patch-applier). |
| nopr:e77a87d63a | refactor(cron): extract scheduler fork helpers | fires unmeasured/0 | n/a (refactor) | commit (refactor, no incident). |
| #1032 | fix(cron): never persist the 'auto' model sentinel in the job store (t_2f1ca8d4) | fires unmeasured/0 | 0 model='auto' rows in live stores (the fix holds); live stores ~/.hermes/cron/jobs.json + profiles/*/cron/jobs.json read 2026-09-25: 521 jo | PR #1032 body, card t_2f1ca8d4. |
| #389 | fix(cron): surface unsafe creation shapes immediately (#389) | fires unmeasured/0 | 0 log lines (warnings are returned in the tool result, not logged); 235 transcript mentions (state.db hits = strings inside session transcri | PR #389 body (fleet lint caught an immortal origin job after the fact). |
| #204 | fix(delegate): boomerang inherit reads _session_messages (gateway live-transcrip | fires unmeasured/0 | no log line; follows #203 usage (see #203 row). 0 direct hits in logs 685 files (~/.hermes/logs agent.log* 09-24 12:44→09-25, gateway.log* 0 | PR #204 body: live gateway E2E returned 'INHERITED: no' after #203 shipped. |

## Corrections to auditor evidence that did NOT change a verdict

- #387: fires '0 ever' -> 4 (Apollo, 2026-07-20..21); still dormant 66 days.
- #88: dependents missing tools/terminal_tool.py:179-181 (a09ec2ea99, 2026-09-25) - rewire to get_session_env('HERMES_CRON_SESSION') at the sync or the messaging foreground cap silently switches off.
- #482 / #348 / #126 / #148: deferred upstream shas resolved (d9dd05b69d, ec0227b435 + 6c98eb2d45 + a65a647b04, 4e1dd1a74b, 7ecbe50d98).
- #126: retire cron/fork_ext/scheduler_ext.py::resolve_cron_reasoning_config + tests/golden/scheduler_ext + registry entry 6's reasoning half together with the row.
- #211: exposure unchanged by the revert (proxy never enabled) - the hole is open today.

## Probe artifacts (not committed; in the worker workspace)

- `adv_probe_guard.py`: lifecycle-guard cases run on fork 257ff440ba and up 1b57acf94a - fork 7/8 expected (inert python heredoc blocked, by #920/#1017 design), up 6/8 (misses `cd <dir> && ./launcher-that-restarts` and `python3 - <<EOF os.system('hermes gateway restart')`).
- `adv_probe_snapshot.py`: LocalEnvironment two-command probe - up re-sources `HERMES_HOME=/tmp/foreign_home` and `HERMES_KANBAN_TASK=t_foreign` into the next command; fork does not.
- fork tests replayed on up 1b57acf94a: test_per_job_reasoning_effort 14 passed; test_store_resolves_live_home 2 passed; test_ticker_execution_ledger 5 failed/1 passed (source-text helper, not behaviour); test_store_live_write_guard 3 AttributeError + 3 home_io_guard trips; test_env_snapshot_session_leak (import path fixed) 2 failed/2 passed.
