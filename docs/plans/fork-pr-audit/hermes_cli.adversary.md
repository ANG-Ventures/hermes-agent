# hermes_cli tranche — ADVERSARY pass (t_86771360)

Input: `audit/hermes_cli/verdicts` @ e10015bc00 (t_2a1bd9cd). Every DROP (8) and SUPERSEDED-BY-UPSTREAM (19) row was challenged: 27 rows. Live trees (`~/.hermes`, runtime, live checkout) and fleet config were read-only. Nothing was reverted or merged. Upstream = `upstream/main` 59004a6235 (worktree `../up`); fork = `origin/main` b029b53035.

Revert-branch note: `git diff origin/main..audit/hermes_cli/revert-<key>` is misleading because origin/main moved 2 commits after the branches were cut (it shows the reverse of unrelated main commits, e.g. kanban_home_index.py). Measured against each branch's merge-base (ee374d6a6d) all five branches touch only the row's own files: revert-802 (3 files, +7/-711), revert-760 (2, -97), revert-712 (4, -296), revert-619 (2, -83), revert-36134d8944 (2, +3/-26). No branch deletes a seam another fork PR or fleet script depends on — with the one exception below (#802).

Method per row: (1) `git grep` of the row's symbols/literals in the fork tree, `~/.hermes/plugins`, `~/.hermes/scripts`, `~/.hermes/fleet`, hooks, launchd, cron job files; (2) fleet config read (`~/.hermes/config.yaml` + 10 profile configs); (3) `fork-features.json`; (4) incidents via kanban.db (read-only) and 30-day logs; (5) every cited upstream sha checked with `git merge-base --is-ancestor <sha> upstream/main` (all 11 cited shas are ancestors) and the by-read symbol re-read on `../up`; (6) revert branch stat vs merge-base; (7) where the fork ships its own regression test, that test was run on `../up` (one interpreter: runtime venv 3.11 via `~/.hermes/scripts/test-gate`).

| key | auditor verdict | adversary result | evidence | notes |
|---|---|---|---|---|
| #802 | DROP | **OVERTURNED → KEEP** (UPSTREAM already filed) | `~/.hermes/plugins/fallback_chain_pool_source/__init__.py:245-247` `from hermes_cli.fallback_config import register_chain_source; register_chain_source(SOURCE_NAME, resolve)` (+ self-register on import :252, test_source.py:207/223); plugin.yaml author Apollo, card t_fb31680e (done, handoff names fork #802 + this plugin as the structural fix); upstream PR NousResearch#118012 **OPEN** (headRefOid bd7a84c714, 704/7, 3 files) | The auditor's "no caller outside fallback_config.py in the fork tree or ~/.hermes/plugins" is wrong: a live fleet plugin imports and registers against the hook. Reverting deletes the import target; the plugin's `register(ctx)` hook then raises ImportError at plugin load. Caveat for the lead: no fleet config carries a `{source: ...}` rung yet (all 10 configs pin literal claude-bpr/openai-codex rungs; t_fb31680e delivered literal rungs), so the hook is wired but not yet driving a chain. It is still not "speculative infrastructure": the consumer exists and is installed. Becomes SUPERSEDED-BY-UPSTREAM when #118012 merges. |
| #760 | DROP | STANDS | `~/.hermes/cache/forensic-native-snapshot-control-20260913/` is gone (only `.MOVED.txt`); `find ~/.hermes/cache -name '*.db'` empty (auditor, re-confirmed); revert-760 vs merge-base: backup.py -57, test_backup.py -40; no fleet script or skill names `forensic-` as a backup exclusion | Trigger gone, exactly Ace's "solves a problem that no longer exists". Recurrence risk is real (the next torn forensic snapshot dropped under `cache/` re-enters the FULL-tier restore-verify walk); the cheaper standing fix is a forensics-skill rule "snapshots live outside $HERMES_HOME or under an already-excluded dir", not a fork patch. |
| #712 | DROP | STANDS (evidence corrected) | `model_catalog.excluded_providers` absent from `~/.hermes/config.yaml` and all 10 profile configs and `~/.hermes/plugins` (re-run); revert-712 vs merge-base: 4 files, -296, own files only. Upstream read corrected: upstream does NOT apply it only at inventory.py:54 — `../up/hermes_cli/model_switch_providers.py:651,659,891,927` run `_skip(seen, excluded, ...)` over overlay AND custom-provider rows, `main_provider_setup.py:883-889` filters the CLI list "like the gateway/TUI", `inventory.py:254` aux pickers honour it | The knob can never fire on this fleet, and upstream has since closed the same class in its own emitters, so the verdict is effectively SUPERSEDED rather than "never configured" — same action either way (revert branch green: test_inventory.py 23 passed per auditor). |
| #619 | DROP | STANDS | `check_ok(f"Interpreter: {sys.executable}")` (4be3c279c1 doctor.py +7); 0 consumers: no `Interpreter:` reader in `~/.hermes/scripts`, skills-shared SKILL.md hits are unrelated prose; `hermes doctor` is not called by any cron job, launchd plist or fleet script; upstream doctor*.py has no `sys.executable` line; revert-619 vs merge-base: doctor.py -7, test -76 | Papercut pc-bf39c22e was the only evidence and nothing was ever built on the line. Note for the lead: it is a 7-line generic doctor improvement upstream lacks — UPSTREAM-eligible if someone wants to carry it; as a fork patch it is unmeasured, so DROP stands. |
| #890 | DROP | STANDS (structural guard is elsewhere) | Warning literal (`_warn_if_sandbox_neutralises_pins`, kanban_db.py:703) 0 hits in 1,741 log files and 0 since merge 09-23; the fleet fixed the class at the SOURCE: `~/.hermes/scripts/git-hooks/pre-commit.d/99-kanban-env-scrub.sh` (card t_84deb81f, "kanban twin of git-env-scrub"), and every script that pins a board now unsets `HERMES_KANBAN_SANDBOX` first (`kanban-diagnostics-watch.sh:15`, `shell_suite_sweep.py:65`, `diagnostics/zombie_block_repro.py:10`, `kanban-gc-dry-run.sh:12-13`); incidents t_58601147 / t_651a97f0 (09-21, fixture cards on the live board) predate both fixes and did not recur | The incident class was real; the runtime warning is the second, silent detector behind a pre-commit gate that already refuses the pattern. No caller, config, test or incident needs the warning. Cost note: the revert is a hand-build (UU kanban_db.py, conflict_syncs 2) for 158 loc in a god-file — the lead may prefer "drop at kanban convergence" over a standalone rebase. |
| #158 | DROP | **OVERTURNED → SUPERSEDED-BY-UPSTREAM** (same action, corrected class) | Auditor's `upstream_state` "upstream web_server emits no HERMES_DASHBOARD_READY" is wrong: `../up/hermes_cli/web_server.py:1355` `_write_dashboard_ready_file(actual_port)` and `:1359` `ready_token = "HERMES_DASHBOARD_READY_HEADLESS" if headless else "HERMES_DASHBOARD_READY"`; readers are `../up/apps/desktop/electron/backend-ready.ts:145` `readDashboardReadyFile` (D9, upstream-owned). Fork readers outside apps/: none (`git grep DASHBOARD_READY` → web_server.py + its test only); `~/.hermes/scripts`, fleet, launchd: none | The 5-line hermes_cli hunk is upstream's own behaviour now, so the row is "take theirs", which also dissolves the unbuilt-revert problem (UU apps/desktop/* — D9 path already taken wholesale each sync). Nothing non-desktop consumes the ready file. |
| nopr:36134d8944 | DROP | STANDS | Bare `gpt-5.5` pins: 0 in config.yaml + 10 profiles, 0 cron jobs (jobs.json all profiles), 0 kanban `model_override='gpt-5.5'`, 0 launchd; fleet aliases are gpt-6-* (`model.aliases` gpt/astra/sol); `~/.hermes/scripts/model-fallback-check.py:103` keeps `gpt-5.5` only as a historical log normaliser; `hermes_cli/codex_models.py:62-65` lists gpt-5.5 in the typed openai-codex downgrade ladder (unaffected by a DIRECT alias); revert-36134d8944 vs merge-base: model_switch.py +1/-8, test -17. The row's own test `test_bare_gpt_5_5_prefers_openai_codex_direct_alias` FAILS on `../up` (expected — upstream has no such alias) | Stale model; the test failing on upstream does not rescue it because nothing on the fleet types bare `gpt-5.5` any more. |
| nopr:8f7823887e | DROP | STANDS (nothing left to revert) | `git grep 'could not verify' -- hermes_cli` → only kanban_survivor files; the row's warning text is already gone from models.py (replaced by #595 d750c1f90f, KEEP); 0 log hits; the unbuilt revert conflicts (UU models.py, UD its test) precisely because #595 rewrote the site | Already absorbed by a fork KEEP. Do not hand-build a revert — it would have to re-introduce the `provider_label` local rebinding #595 sits on. Record as DROP-absorbed-by-#595, no branch, no card. |
| nopr:b5d533baa7 | SUPERSEDED-BY-UPSTREAM | STANDS | Zero net fork delta (dangling import removal aligned to upstream's deletion of `hermes_cli/memory_providers`; the two desktop paths no longer exist on fork) | Sync residue; nothing to challenge. |
| #711 | SUPERSEDED-BY-UPSTREAM | STANDS | 40230dcca7 ancestor of upstream/main; `../up/hermes_cli/main.py:1930` `cmd_cron = _forward_command(..., "cron_command", forward_return=True)` | Same defect (exit status swallowed), fixed by construction upstream. Take theirs at the main.py sync. |
| #638 | SUPERSEDED-BY-UPSTREAM | STANDS | a36221ed91 ancestor; `../up/hermes_cli/doctor_tools.py:202` `_check_docker_backend(terminal_env, running_in_container, ...)`, `:311 running_in_container = _is_container()` | The doctor in-container branch no longer resets terminal_env. Condition: take upstream's `tests/hermes_cli/test_gateway_service.py` with it (the fork's hermetic edits live there). |
| #637 | SUPERSEDED-BY-UPSTREAM | STANDS | 30b0958a69 ancestor, same title, same day; `../up/hermes_cli/auth.py:249` PROVIDER_REGISTRY then `:259 from hermes_cli.config import (  # noqa: E402` | Identical fix upstream. |
| #486 | SUPERSEDED-BY-UPSTREAM | **OVERTURNED → KEEP** (1/3 superseded, 2/3 UPSTREAM-open) | Commit 1 = cherry-pick of upstream 0b69a6ac02 (ancestor; `../up/hermes_cli/kanban_db.py:729 reasoning_effort` column + `normalize_reasoning_effort`:115). Commits 2-3 (CLI + tool exposure) are NOT upstream: `git grep reasoning_effort -- tools/kanban_tools.py hermes_cli/kanban.py` on `../up` → 0 hits (fork: tools/kanban_tools.py:547,594,1641); upstream PRs 79405 and 80167 both **OPEN** (REST, 2026-09-25). Live consumers: `kanban_create(reasoning_effort=...)` is a documented tool param in the fleet (this audit's lead card body uses it), 377 fork tasks carry the column (auditor) | "Upstream has the column" is not "upstream has the tool/CLI write path"; on upstream a worker cannot set per-task effort from `kanban_create` or `hermes kanban set-effort`. SUPERSEDED for the column only; the exposure stays KEEP until 79405/80167 merge (then SUPERSEDED). Fork-kanban registry write still owed (D2b). |
| #455 | SUPERSEDED-BY-UPSTREAM | STANDS | 9ef49cd78f ancestor; `../up/hermes_cli/backup.py:1027` `"kanban/boards"  # ... workspaces/ + attachments/ skipped`, `:1271 if "/workspaces/" in ... or "/attachments/"` | Upstream skips board workspaces in its walk. Fleet workspaces are on `/Volumes/ramscratch/kanban-workspaces` anyway. |
| #400 | SUPERSEDED-BY-UPSTREAM | STANDS (convergence-only, as the auditor says) | c1b0f6f3c1 ancestor; `../up/hermes_cli/kanban_db.py:728 provider_override`, `:934` column, `kanban.py:580 _cmd_set_model`, `:1322 "set-model"` | Provider travels with the model override upstream. No standalone revert exists on a 21,887-line fork kanban_db.py; the verdict is only executable as part of a kanban convergence project. |
| #386 | SUPERSEDED-BY-UPSTREAM | STANDS | `../up/hermes_cli/model_switch.py:203 _load_direct_aliases` reads `load_config()` (:217) with a path/mtime cache key (:257); `_ensure_direct_aliases` :282 no longer populates once | Same behaviour (aliases follow config edits without a restart). |
| #313 | SUPERSEDED-BY-UPSTREAM | **NEEDS-LEAD** (KEEP standalone; SUPERSEDED only with the agent-tranche background_review convergence) | Fork's review path still ARMS the plugins.py whitelist: `agent/background_review.py:1590-1591` imports `set_thread_tool_whitelist, clear_thread_tool_whitelist` (fork), whose storage is the ContextVar this row added (`hermes_cli/plugins.py:6744-6754 _tool_whitelist_var`); upstream's `_review_tool_whitelist@agent/background_review.py:1078` is a different, dispatch-side design (c408601937 ancestor). 'Background review denied non-whitelisted' — 0 hits since the fix (the bug is fixed, on the fork's implementation) | Reverting this row alone (ContextVar → threading.local) re-opens the leak on the fork's own review implementation. It is superseded only if the agent tranche's verdict on fork `agent/background_review.py` is "take upstream's". Cross-tranche coupling → lead. |
| #308 | SUPERSEDED-BY-UPSTREAM | STANDS (convergence-only) | 011ec4513e ancestor; upstream `web_routers/sessions.py` offloads; fork web_server.py is ~20k lines (one file) vs upstream routers; `gui.log` 2026-09-24 still shows `event loop stalled 60.1s / 5.4s / 9.2s (GIL pressure suspected)` — the starvation class persists on the fork despite the offloads; dependents listed by the census (hermes_state*.py, weixin.py, modal.py) do not import anything #308 added (`SessionDBHeavyReadBusy` lives in `session_db_heavy_gate.py`, rows #215/#441) | Converge on upstream's routers is the right call and the only executable one (no standalone revert). The 09-24 stalls say the problem is not solved on the fork either way — that belongs to the web_server convergence owner. |
| #182 | SUPERSEDED-BY-UPSTREAM | STANDS (convergence-only) | Same as #308 (011ec4513e); 3 of its symbols already gone from fork web_server.py (auditor) | Rides with #308. |
| #39 | SUPERSEDED-BY-UPSTREAM | STANDS (convergence-only) | c1b0f6f3c1 ancestor; `_cmd_set_model`/`set-model` + `model_override`/`provider_override` columns on `../up` (see #400) | Same subsystem caveat as #400/#35/#486. |
| #35 | SUPERSEDED-BY-UPSTREAM | STANDS (convergence-only) | 34120a0ae2 ancestor; `../up/hermes_cli/kanban_db_graph.py:45 if initial_status == "blocked": return "blocked"`, `kanban_db.py:2076 _has_sticky_block`, `:2157` recompute respects it; fleet relies on it (139 `blocked` events with `initial_status` in kanban.db; ang-swarm-plan / ang-closeout skills document `--initial-status blocked`) | Behaviour present upstream by read; only executable via kanban convergence. |
| nopr:9c6214b9c5 | SUPERSEDED-BY-UPSTREAM | **OVERTURNED → KEEP** (rides with #713 KEEP; UPSTREAM-eligible) | Ran `parse_model_input` on both trees (runtime venv): `../up`: `openai-codex/gpt-6-sol` from `claude-bpr` → `('claude-bpr', 'openai-codex/gpt-6-sol')` (stays on the current provider, slug kept verbatim); fork → `('openai-codex', 'gpt-6-sol')`. Colon form is handled on both. Fleet worker spawns pass `-m provider/model` (#374 note: 322 `claude-apr/claude-fable-5` lines) | Upstream does not treat `openai-codex/<model>` as a provider switch in the CLI `-m` parser; the fork's #713 (KEEP) generalises this row. Not superseded. |
| nopr:9f9bf61409 | SUPERSEDED-BY-UPSTREAM | **OVERTURNED → KEEP** (UPSTREAM-eligible for the non-aggregator case) | Fork's own test file run on `../up` (test-gate, runtime venv): `test_colon_provider_model_switches_provider_before_catalog_logic`, `test_colon_provider_model_does_not_get_normalized_on_old_provider`, `test_slash_provider_model_switches_from_non_aggregator_provider` all FAIL (4 failed, 2 passed). Re-probed with `detect_provider_for_model` UNMOCKED (new file `tests/hermes_cli/test_zz_inline_adv_probe.py`, untracked, not committed): on `../up` `switch_model("openai-codex:gpt-6-sol", current_provider="claude-api-proxy")` → target `claude-api-proxy`, model `openai-codex/gpt-6-sol`, provider_changed False; `"openai-codex/gpt-6-sol"` from `claude-bpr` → stays on `claude-bpr`; same probe on fork: 3 passed (switches to openai-codex). Upstream `_convert_vendor_colon_slug` (model_switch.py:1287-1305) only rewrites `vendor:model` → `vendor/model` slug and, on an aggregator, treats every `left:right` as a slug by design (#9748) | The by-read claim (#45006 "typed model routes to the declaring provider") covers models declared in USER provider config, not a provider-qualified `/model provider/model` from a plugin provider — which is the fleet's daily case (every profile runs on claude-bpr/claude-api-proxy). Dropping this regresses `/model openai-codex/gpt-6-sol` to a bogus model on the current provider. |
| nopr:ca61765850 | SUPERSEDED-BY-UPSTREAM | STANDS | `../up/hermes_cli/backup_sqlite.py:31 _safe_copy_db(src, dst, *, timeout_seconds=10.0)`, `:57 busy_deadline` progress callback | Upstream bounds the copy; the fork-specific size of the bound is #778 (KEEP, live 3.5 GB state.db). |
| nopr:d68a4b0fcc | SUPERSEDED-BY-UPSTREAM | STANDS | Both resolvers read the same registry: fork `_resolve_plugin_provider` → `from providers import get_provider_profile` (providers/__init__.py:208); upstream `_plugin_profile_pdef@hermes_cli/providers.py:223` → `providers.get_provider_profile` (:158); fleet plugins register via `ProviderProfile(`/`register_provider(` (13/8 hits in `~/.hermes/plugins/model-providers`) | Same registry shape, so upstream's resolver sees the fleet's claude-* plugin providers. (Side note, not this row: importing `hermes_cli.models` on `../up` with the live plugins dir logs `claude-bpr: skipped models registry injection: cannot import name '_PROVIDER_MODELS'` — the fleet plugins inject into a fork seam; that is #816's territory, KEEP.) |
| #359 | SUPERSEDED-BY-UPSTREAM | STANDS | `../up/hermes_constants.py:991 VALID_REASONING_EFFORTS = (..., "xhigh", "max", "ultra")`, consumers read the constant; fleet profiles' `reasoning_effort: max/xhigh` are in the upstream set | Zero fork delta for 14 conflict files. |
| #391 | SUPERSEDED-BY-UPSTREAM | STANDS | 283c4f058c ancestor; `../up/setup.py:87 _root_py_modules()` → `:99 setup(..., py_modules=_root_py_modules())`; `pyproject.toml:796` comment forbids re-adding the list | hermes_state_ext / hermes_undo ship by construction once the fork takes upstream's setup.py derivation. |

Totals: 27 challenged. **STANDS 21, OVERTURNED 5, NEEDS-LEAD 1.**

Overturned: #802 → KEEP (live plugin consumer, upstream PR open); #158 → SUPERSEDED-BY-UPSTREAM (class correction, same action); #486 → KEEP (tool/CLI exposure fork-only, upstream PRs open); nopr:9c6214b9c5 → KEEP; nopr:9f9bf61409 → KEEP (upstream RED by the fork's own test + unmocked probe).
Needs-lead: #313 (coupled to the agent tranche's verdict on fork `agent/background_review.py`).

Auditor evidence corrections carried into the JSON (verdict unchanged unless listed above): #712 upstream_state (upstream filters custom/overlay rows too), #158 upstream_state (upstream writes the ready file), #802 "no consumer" (plugin found), #8f7823887e (warning text already replaced by #595).

## KEEP-UNPROVEN

KEEP rows whose `evidence.fires` is `0 distinctive log/event hits` or whose `evidence.original` is only a direct commit, AND whose notes back the KEEP with neither a live config knob, a registry ruling, nor a measured hot path. 32 KEEP rows have fires=0 or a commit-only original; 29 of them are backed by a live knob/registry/measured consumer in the auditor's notes (model.aliases, model.picker, 17 claude-apx/bpx lanes, 4,459 labelled turns, live sparseimage/chrome-* dirs, blackbox/LCM live, fork-features.json) and are NOT flagged. Flagged:

| key | subject | why unproven |
|---|---|---|
| #980 | fix(model): accept Discord 'name:provider/model' on every surface; refuse unknown provider | evidence.fires says 0 but the note claims "1 real '/model name:' line" — one hit in 5 months is not a measured need, and the row is generic (auditor says upstream-able). Either measure it or file it UPSTREAM. |
| #492 | fix(models): dots->hyphens only for Anthropic MODELS, not Anthropic-wire relays | Backing is "relay providers are live", but no fleet config names an Anthropic-wire model with a dot (`claude-opus-5-5`, `claude-fable-5-1`); the guarded rewrite has no live input, so the fix is inert on this fleet. Needs a model id with a dot on a relay, or DROP/UPSTREAM. |
| nopr:07d219143b | fix(kanban): a COUNT in a card body is a timestamped claim, not current state | `_rule_stale_quantity_in_body` (kanban_diagnostics.py) has a live runner (`~/.hermes/scripts/kanban-diagnostics-watch.sh`), but the measured fires are 2 `cancelled` events on 2026-07-27 only and no diagnostic emission was counted. Registry write also owed (D2b). |

Not flagged but worth the lead's eye: nopr:a184062e7b (banner cache key; 'commits behind' 1,111 lines is upstream's literal, not the fix's), nopr:aa0f725eb8 (keychain modal suppression; 5 literal lines, browser-automation profiles exist).
