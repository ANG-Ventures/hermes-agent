# hermes_cli tranche — verdicts (auditor t_2a1bd9cd)

Generated from `hermes_cli.verdicts.json` by the auditor's gen script. Route disclosure: the card asks for Fable; this run 
was served on `claude-opus-5-5` (dispatcher route substitution, reason overloaded — see card events).
Fire window: log files 2026-05-10..2026-09-25 (1741 files; agent/gateway logs only 2026-07-29..2026-09-25); kanban.db task_events 2026-06-07..2026-09-25. Log literals matched with `rg -F -o` over the added string literals of each row (generic identifiers such as env-var and column names excluded); 
kanban.db/boards event kinds were attributed to the row whose diff first introduced the kind (`git log -S`).

Kanban subsystem note: the fork's kanban (hermes_cli/kanban_db.py 21,887 lines) and upstream's (split kanban_db*.py, 4,580-line kanban_db.py, 260 kanban commits since the 08-07 merge-base) are now two different implementations. Rows whose behavior upstream also has are marked SUPERSEDED-BY-UPSTREAM but are only realizable through a kanban convergence project, not by per-row reverts. Measured fork-kanban rows are KEEP with a registry write owed (docs/sync/fork-features.json names no kanban feature).

Rows banked: 177/177 — DROP 11, KEEP 130, SUPERSEDED-BY-UPSTREAM 19, UNRESOLVED 13, UPSTREAM 4

| PR | problem+evidence | upstream RED/GREEN (how) | upstream fix sha | fire count (window) | cost | verdict | branch |
|---|---|---|---|---|---|---|---|
| #158 | feat(desktop): backend runs from runtime deploy tree by default (+ config override) (#158) — Route the desktop app's Python backend at the **runtime deploy tree** (`~/.hermes/runtime/hermes-agent` + its venv) by default, (PR body via REST (merged 2026-07-01T14:35:45Z); shas 53cce7f872) | read-only: upstream web_server emits no HERMES_DASHBOARD_READY; the only readers are apps/desktop/electron (fork and upstream) | none | 0 distinctive log/event hits | 507 loc, 1(3) | **DROP** — D9: the hermes_cli hunk (5 lines, web_server ready file) exists only for the desktop Electron backend-ready.cjs; the apps/ part is already D9 DROP. dashboard.out.log prints the line but nothing non-desktop reads it. | - |
| nopr:b5d533baa7 | fix(sync): CI round-1 fixes — dead import of upstream-deleted module + keychain-guard false anchor — - hermes_cli/web_server.py: drop the unused import of hermes_cli.memory_providers. Upstream DELETED that module and mov (PR body via REST (merged direct commit); shas b5d533baa7) | GREEN by read: upstream deleted hermes_cli/memory_providers; the row only removed the fork's dangling import (web_server.py) and patched desktop files that no longer exist on fork (2 paths missing) | none (row is sync-residue aligning fork to upstream) | 0 distinctive log/event hits | 116 loc, 1(3) | **SUPERSEDED-BY-UPSTREAM** — Zero net fork delta on hermes_cli after this row; nothing to revert. | - |
| #1059 | fix(kanban): refuse mid-turn provider failover for a card-pinned worker (#1059) — #1055 refuses the AUTH-time fallback for a pinned worker, but runtime failover in try_activate_fallback only recorded worker_route_substit (PR body via REST (merged 2026-09-25T11:25:33Z); cards t_4fe0700a, t_7b7cb5aa, t_ed0289e3; shas b029b53035) | read-only: _append_run_event absent from up/hermes_cli/kanban*, _is_owning_worker absent from up/hermes_cli/kanban*, apply_pin_refusal_to_result absent from up/hermes_cli/kanban* | none | log literals 'PHASE=kanban_worker_route_substituted ta' 540, 'Fallback skip:' 466, 'PHASE=kanban_worker_route_pin_refused ta' 30 (41 files); kanban.db events worker_route_pin_refused 14 (2026-09-25..2 | 743 loc, 4(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1057 | fix(kanban pr-gate): MERGED is not DEPLOYED — hold deploy-premised cards until the merge is in the live tree (t_289e8020) (#1057) — **Bug:** gate_auto_resolved unblocked t_2789cbae on `ANG-Ventures/hermes-home#605 merged (PR body via REST (merged 2026-09-25T10:48:35Z); cards t_2789cbae, t_289e8020, t_917bb9e1; shas 26a75d7c0b) | read-only: _awaiting_deploy_sentence absent from up/hermes_cli/kanban*, _deploy_checks absent from up/hermes_cli/kanban*, _deploy_marker absent from up/hermes_cli/kanban* | none | log literals '~/.hermes/runtime/hermes-agent' 83, 'MERGED, awaiting deploy of' 1 (29 files); DB/config: 'MERGED, awaiting deploy of' 1 line | 512 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1051 | fix(kanban): remap worker assignee='default' to kanban.default_assignee (#1051) — Change (worker path only): - kanban_worker_policy.resolve_worker_assignee: when running as a dispatched worker and assignee=='default', re (PR body via REST (merged 2026-09-25T06:46:22Z); cards t_4424c05d, t_4d0fef51; shas 7b6249d198) | read-only: configured_default_assignee absent from up/hermes_cli/kanban*, resolve_worker_assignee absent from up/hermes_cli/kanban* | none | kanban.db events assignee_remapped 1 (2026-09-25..2026-09-25); DB/config: config kanban.default_assignee=daedalus; 1 assignee_remapped event | 117 loc, 2(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1050 | fix(kanban): claim-guard owner identity uses a clock-step-immune start token (t_21dfa673) (#1050) — `_owner_identity` compared psutil's epoch `create_time()` with integer wall-clock event stamps. psutil re-bases that epo (PR body via REST (merged 2026-09-25T05:37:01Z); cards t_09180e10, t_21dfa673, t_3a06ba8f; shas 4861a0824b) | read-only: _owner_identity absent from up/hermes_cli/kanban*, _pid_start_token absent from up/hermes_cli/kanban*, _real_pid_started_in_claim absent from up/hermes_cli/kanban* | none | DB/config: claim guard owner identity (#946 path) | 311 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1040 | test(kanban): make progress-stall policy tests probe-deterministic (t_5457397a) (#1040) — **Problem.** `tests/hermes_cli/test_kanban_progress_stall.py` waited for the REAL `ps` probe to read a freshly-spawned child as 0. (PR body via REST (merged 2026-09-25T02:49:23Z); cards t_5457397a; shas 7fd339db72) | read-only: _cpu_active_in_table absent from up/hermes_cli/kanban*, _process_cpu_table absent from up/hermes_cli/kanban* | none | log literals 'pid=,ppid=,pcpu=' 18 (1 files) | 164 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1039 | fix(kanban): spelling-blind mount-root match in workspace admission (t_800d50d2) (#1039) — ## Bug (reproduced on main 67202e80f6) `_validate_workspace_admission` matched a task's `workspace_path` to a recorded mount root (PR body via REST (merged 2026-09-25T02:58:02Z); cards t_800d50d2, t_ee808d83; shas 4945f74ae2) | read-only: _lexical_root_anchor absent from up/hermes_cli/kanban*, _recorded_root_alias absent from up/hermes_cli/kanban*, _spelling_fold absent from up/hermes_cli/kanban* | none | DB/config: config kanban.workspaces_root on a RAM-disk mount (live) | 261 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1034 | fix(kanban): malformed recorded landed receipt HOLDs on reclamation instead of escaping _hold (t_ad32cdab) (#1034) — Reproduced on main 67202e80f6 with the Argus probe: P10 (refs=null) TypeError; P12a (landed=null) TypeE (PR body via REST (merged 2026-09-25T03:15:26Z); cards t_60592755, t_ad32cdab; shas c0c2d2b7a1) | read-only: _recorded_landed_claims absent from up/hermes_cli/kanban*, _recorded_refs absent from up/hermes_cli/kanban* | none | log literals 'recorded repository missing' 5 (5 files); DB/config: 1,375 task_workspace_survivors rows | 228 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1020 | fix(kanban): stale workspaces-root pin no longer HOLDs every completion (#1020) — - `kanban_db.workspace_root_candidates()`: returns the union of the pin, the configured root/<board> and the legacy root. It never raises. (PR body via REST (merged 2026-09-25T07:02:30Z); cards t_1ba5b6b5; shas d42d004bed) | read-only: workspace_root_candidates absent from up/hermes_cli/kanban* | none | DB/config: config kanban.workspaces_root (live) | 135 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1018 | fix(kanban survivor): hold ignored orphan bytes on in-place repo replacement and unbound cleanup (t_60592755) (#1018) — ## P8: a recorded repo re-initialised in place over ignored bytes Repo key `a` is recorded at SHA A. (PR body via REST (merged 2026-09-25T02:13:31Z); cards t_60592755; shas 6735098c8b) | read-only: _authoritative_repositories absent from up/hermes_cli/kanban*, _holds_commit_by_stat absent from up/hermes_cli/kanban*, _replaced_orphans absent from up/hermes_cli/kanban* | none | log literals ') are vouched for only by an unbound' 16, 'operator survivor, which authorises one ' 16, 're-assert a survivor bound to the card o' 16 (4 files); DB/config: 1,375 task_workspace_survivor | 407 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #1011 | fix(plugins): keep chained/prefixed diff out of lossy pre_tool_call rewrites (#1011) — Measured with the real rtk 0.42.2: `cd X && diff`, `test && diff`, `a; diff`, `a \|\| diff`, `env X=1 diff`, `sudo diff` are all rewrit (PR body via REST (merged 2026-09-25T09:19:03Z); cards t_22ce5d93, t_757191ff; shas 7a0d33c0fe) | read-only: no rtk hook upstream | none | 0 distinctive log/event hits | 216 loc, 1(1) | **KEEP** — rtk pre_tool_call rewrite is live on this host (the terminal tool rewrites via rtk). Fleet-permanent: needs a fork-features.json entry. | - |
| #1002 | feat(kanban): reviewer-swarm gates — per-profile cap mapping, load-gated spawns, review round cap, milestone-only review (#1002) — procs, the gateway 15 min from turn start to first model call, its shutdown watchdog forc (PR body via REST (merged 2026-09-25T01:32:59Z); cards t_74ab6744; shas 53d766bee3) | read-only: LoadGate absent from up/hermes_cli/kanban*, _cap_for absent from up/hermes_cli/kanban*, _sample_spawn_pause absent from up/hermes_cli/kanban* | none | log literals '); resumes below' 88, '> pause_above=' 88, 'Optional[dict]' 2 (29 files); kanban.db events review_skipped 1 (2026-09-25..2026-09-25); DB/config: config kanban.review_policy=milestone_onl | 650 loc, 4(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #994 | fix(kanban): end runs left 'running' on done/archived cards (#994) — **Bug:** every reclaim sweep (release_stale_claims, detect_crashed_workers, enforce_max_runtime, reconcile_orphaned_running) selects `tasks WHERE statu (PR body via REST (merged 2026-09-25T02:01:15Z); cards t_cb91bfc4; shas d2ca1e5f66) | read-only: _opt_int_value absent from up/hermes_cli/kanban*, end_orphaned_terminal_runs absent from up/hermes_cli/kanban* | none | log literals 'kanban reaper: ended run' 12 (14 files); kanban.db events orphaned_terminal_run_ended 17 (2026-09-24..2026-09-25); DB/config: 17 orphaned_terminal_run_ended events | 233 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #989 | fix(kanban,launchd): clamp macOS workers to utility QoS; gateway plist ProcessType=Interactive (t_14c130aa) (#989) — nice(2) only reorders threads inside one Darwin scheduling class. Measured on the M3 Ultra with sudo ta (PR body via REST (merged 2026-09-24T21:24:39Z); cards t_14c130aa; shas d98a9bb8af) | read-only: worker_darwin_qos_prefix absent from up/hermes_cli/kanban* | none | log literals 'PHASE=worker_spawn task=' 2163, 'darwin_policy=' 829 (6 files) | 190 loc, 4(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #987 | feat(kanban): post-#951 — worker cards inherit parent home; list home-first + --all; pings carry home; task_events actor provenance (#987) — ## 1. Worker-created cards inherit the parent's home (`hermes_cli/kanban_db.py` (PR body via REST (merged 2026-09-25T00:58:24Z); cards t_f59538cf; shas ec50416ce8) | read-only: _ambient_session_env absent from up/hermes_cli/kanban*, _cmd_home_lint absent from up/hermes_cli/kanban*, _event_actor absent from up/hermes_cli/kanban* | none | log literals '(caller session:' 1 (15 files); kanban.db events takeover 227 (2026-09-24..2026-09-25); DB/config: 227 takeover events | 1617 loc, 5(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #985 | fix(kanban): budget per-tick relay spawns against eligible subs (#985) — - Bound each dispatch tick's successful pool spawns to `eligible_count * kanban.pool_spawns_per_eligible` (default 2; 0 restores legacy unlimited a (PR body via REST (merged 2026-09-24T21:50:15Z); cards t_082e7650; shas 97c493c5d5) | read-only: charge_pool absent from up/hermes_cli/kanban*, configured_pool_spawns_per_eligible absent from up/hermes_cli/kanban*, pool_budget absent from up/hermes_cli/kanban* | none | DB/config: 'eligible_count' 24 lines | 292 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #983 | fix(kanban): reclaim pid-less claim when local claimer is dead (#983) — A gateway may die after claiming a task but before stamping `worker_pid`. The worker sweep repeatedly treats the missing PID as unprovable, extends  (PR body via REST (merged 2026-09-25T00:14:30Z); cards t_09180e10, t_9d7e0142; shas a33a28aa9b) | read-only: _dead_claimer_release_at absent from up/hermes_cli/kanban* | none (upstream PR NousResearch#121730 (open)) | DB/config: reclaim path; 98 reclaim_refused events | 450 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #982 | feat(kanban): home = session lineage (home_ids) + kanban.home_guard (default refuse) (#982) — Gateway chats rotate their session id without changing chat (resume_pending_expired, session_switch, most /new). The successor (PR body via REST (merged 2026-09-24T16:40:29Z); cards t_de1f0aa7; shas 813b750c9e) | read-only: home_guard_mode absent from up/hermes_cli/kanban*, home_ids absent from up/hermes_cli/kanban* | none | DB/config: home guard: config consumer; 1,455 session-stamped tasks | 342 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #980 | fix(model): accept Discord 'name:provider/model' on every surface; refuse unknown provider (#980) — Pasting Discord's native /model rendering (`/model name:claude-bpx-5/claude-fable-5-1`) into Telegram kept the current p (PR body via REST (merged 2026-09-25T01:23:21Z); shas c01b93080e) | RED by read: no 'name:' Discord-rendering parse in hermes_cli/model_switch.py | none | 0 distinctive log/event hits | 380 loc, 2(3) | **KEEP** — 1 real '/model name:' line in agent/gateway logs. Generic — upstream-able later. | - |
| #965 | fix(kanban): scratch workspace under a dangling gitfile has no survivor (#965) — **Bug.** The tripwire gitfile at `~/.hermes/kanban/workspaces/.git` (`gitdir: /nonexistent/...`) makes git exit 128 from every scratch work (PR body via REST (merged 2026-09-24T14:39:25Z); cards t_82a5c853, t_9db37763; shas ac549ea199) | read-only: _dangling_gitfile absent from up/hermes_cli/kanban* | none | log literals 'not a git repository' 173 (31 files) | 174 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #962 | WIP: verify merged survivor SHAs and record no-ref closeouts (#962) — - `--survivor-ref` accepts a real merged SHA reachable from the default branch, including extensionless GitHub clone URLs; remote failures name comman (PR body via REST (merged 2026-09-25T00:39:36Z); cards t_3684ae00, t_6d221fe6, t_fc150853; shas 7395fb3a43) | read-only: _landed absent from up/hermes_cli/kanban*, _merged_tree_matches absent from up/hermes_cli/kanban*, neutral_cwd absent from up/hermes_cli/kanban* | none | log literals 'https://github.com/' 696, 'capture failed' 13, '--filter=blob:none' 2 (287 files) | 762 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #960 | fix(kanban): judge goal deliverables before completion; fail open for operator errors (#960) — <!-- codesmith:footer --> --- <a href="https://app.blacksmith.sh/ANG-Ventures/codesmith/hermes-agent/pr/960?autoLogin=true&re (PR body via REST (merged 2026-09-24T12:30:36Z); cards t_c4e23682; shas 5d879a7e05) | read-only: goal_judge_available absent from up/hermes_cli/kanban*, kanban_handoff_rejection absent from up/hermes_cli/kanban* | none | DB/config: 17 goal_mode tasks | 453 loc, 5(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #956 | fix(kanban): reject recycled PIDs as prior worker owners (#956) — <!-- codesmith:footer --> --- <a href="https://app.blacksmith.sh/ANG-Ventures/codesmith/hermes-agent/pr/956?autoLogin=true&ref=codesmith_pr_footer"><pictu (PR body via REST (merged 2026-09-24T18:29:07Z); cards t_089fcb25; shas 7defc24315) | read-only: _real_pid_started_in_claim absent from up/hermes_cli/kanban* | none (upstream PR NousResearch#121177 (open)) | log literals 'kanban dispatcher:' 9431, 'kanban dispatcher STUCK:' 622, 'card(s) continuously' 608 (51 files); DB/config: 'kanban dispatcher STUCK:' 622 lines | 238 loc, 4(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #953 | kanban dispatcher: pool-health default probe + escalating rate-limit backoff + tick circuit (t_32f44156) (#953) — Kanban workers keep spawning into an exhausted claude relay pool. Measured over 7d (2026-09-22): **3,706 ` (PR body via REST (merged 2026-09-24T15:34:55Z); cards t_32f44156; shas 6ca7afa0fb) | read-only: _box_rejection absent from up/hermes_cli/kanban*, _note_probe_state absent from up/hermes_cli/kanban*, _notify_rate_limit_circuit absent from up/hermes_cli/kanban* | none (upstream PR NousResearch#119994 (open)) | log literals 'http://127.0.0.1:18811/health' 105, 'kanban provider health:' 41, 'reports no capacity (' 14 (54 files); kanban.db events dispatch_lane_route 410 (2026-09-24..2026-09-24), dispatch_provi | 1649 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #952 | fix(kanban): resume dependency-wait PR and page stranded ready cards (#952) — <!-- codesmith:footer --> --- <a href="https://app.blacksmith.sh/ANG-Ventures/codesmith/hermes-agent/pr/952?autoLogin=true&ref=codesmith_pr_fo (PR body via REST (merged 2026-09-24T12:42:35Z); cards t_7d7ff489; shas d8e7e8af82) | read-only: _GuardStuckNotifier absent from up/hermes_cli/kanban*, _cmd_requeue absent from up/hermes_cli/kanban*, _guard_stuck_cards absent from up/hermes_cli/kanban* | none | log literals 'kanban dispatcher:' 9431, 'Check profile health (venv, PATH, creden' 37, '`hermes kanban list --status ready`.' 37 (53 files); kanban.db events requeued 5 (2026-09-24..2026-09-24); DB/co | 860 loc, 5(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #951 | feat(kanban): home-session card ownership — session stamping, --home, foreign-session mutation guard (#951) — Every session could see and mutate every card. `tasks.session_id` existed and `list --session` filtered on it, (PR body via REST (merged 2026-09-24T14:48:09Z); cards t_8283d7e0; shas ee7b8d9096) | read-only: ForeignSessionMutationError absent from up/hermes_cli/kanban*, MutationActor absent from up/hermes_cli/kanban*, _caller_profile absent from up/hermes_cli/kanban* | none | log literals '(caller session:' 1, '). Status/ownership changes belong to th' 1, ': its home session is' 1 (26 files); DB/config: 1,455 tasks session-stamped | 1267 loc, 4(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #950 | fix(kanban): pin agree-checks + managed-scratch containment spelling-blind (t_ee808d83 r3, #948 follow-up) (#950) — #948 was squash-merged at the r2 head (06aa43db46). That merge contains the N1 regression, so this PR is (PR body via REST (merged 2026-09-24T13:56:45Z); cards t_ee808d83; shas b42006a0e7) | read-only: _bounded_cwd_probe absent from up/hermes_cli/kanban*, _existing_ancestor absent from up/hermes_cli/kanban*, _path_identity absent from up/hermes_cli/kanban* | none | log literals 'cwd probe cannot be bounded in this cont' 1 (1 files) | 913 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #949 | fix(kanban): cohort-death guard + signal receipts for externally-ended workers (t_0c1ebbae) (#949) — Changes: - cli.py kanban SIGTERM path: writes a last-words line to the run log and an exit receipt (128+signum, exit_cl (PR body via REST (merged 2026-09-24T01:55:24Z); cards t_0c1ebbae; shas a89383d5dc) | read-only: _cohort_death_ids absent from up/hermes_cli/kanban*, _page_cohort_death absent from up/hermes_cli/kanban* | none | log literals '— terminated from' 116, 'protocol violation' 15, '(no failure counted):' 6 (95 files); kanban.db events cohort_death 38 (2026-09-25..2026-09-25); DB/config: 38 cohort_death runs | 337 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #948 | fix(kanban): gc uses one cached machine-wide cwd scan, not per-path lsof +D (t_ee808d83) (#948) — **Change**: `_process_cwd_within` ran `lsof -a -d cwd -Fn +D <ws>` once per candidate whenever a live dir:home card existe (PR body via REST (merged 2026-09-24T01:55:24Z); cards t_ee808d83; shas 06aa43db46) | read-only: _gc_remove_workspaces absent from up/hermes_cli/kanban*, _kernel_path absent from up/hermes_cli/kanban*, _loose_spelling absent from up/hermes_cli/kanban* | none | log literals '/System/Volumes/Data' 761 (10 files) | 489 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #947 | feat(kanban): hermes kanban clone - reference clones against shared fleet mirrors (t_dad1edd7) (#947) — Workers clone fleet repos by hand into scratch workspaces (159 clones on 2026-09-23, 39 of hermes-home, each a full  (PR body via REST (merged 2026-09-24T09:28:37Z); cards t_dad1edd7; shas f2fffde3ac) | read-only: _Forward absent from up/hermes_cli/kanban*, _build_prefix absent from up/hermes_cli/kanban*, _discard_dead_builds absent from up/hermes_cli/kanban* | none | log literals 'extensions.preciousObjects' 2, 'gc.pruneExpire' 1 (14 files); DB/config: 'hermes kanban clone' is the documented worker clone path (this card used it) | 873 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #946 | fix(kanban): claim guard sees open prior runs and every spawn of a run (#946) — ## Defects (Argus round 4, t_3a06ba8f) - **B-1:** `_prior_worker_still_alive` only scanned runs with `ended_at IS NOT NULL`. A ready or revi (PR body via REST (merged 2026-09-24T21:52:16Z); cards t_3a06ba8f; shas c83be6578e) | read-only: _owner_identity absent from up/hermes_cli/kanban*, _pid_create_time absent from up/hermes_cli/kanban*, _real_pid_started_in_claim absent from up/hermes_cli/kanban* | none | log literals 'ORDER BY id DESC' 5 (6 files); DB/config: claim guard; 98 reclaim_refused events | 691 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #943 | Detect heartbeat-only Kanban stalls and route capped pools (#943) — <!-- codesmith:footer --> --- <a href="https://app.blacksmith.sh/ANG-Ventures/codesmith/hermes-agent/pr/943?autoLogin=true&ref=codesmith_pr_footer"><pic (PR body via REST (merged 2026-09-23T20:34:37Z); shas 03de88f45a) | read-only: _run_progress_at absent from up/hermes_cli/kanban*, _worker_cpu_active absent from up/hermes_cli/kanban*, apply_dispatch_fallback absent from up/hermes_cli/kanban* | none | log literals 'pid=,ppid=,pcpu=' 18 (78 files); kanban.db events dispatch_provider_fallback 150 (2026-09-24..2026-09-25); DB/config: 150 dispatch_provider_fallback events | 654 loc, 7(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #941 | fix(kanban): include git operation and exit code in survivor holds (#941) — - Re-port #859 onto current fork/main by content. `_git()` now persists the Git subcommand and returncode in a survivor hold, not the opaque `gi (PR body via REST (merged 2026-09-23T20:29:09Z); cards t_1e7a5864; shas 1483eb236f) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 1,375 task_workspace_survivors rows | 24 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #940 | fix(kanban): home-rooted dir: cards stop owning every scratch workspace; gc reaps old done workspaces (t_b99d8978) (#940) — - `~/.hermes/kanban/workspaces`: 364 dirs. 307 belong to `done` cards and hold **65.2 GB**. Anot (PR body via REST (merged 2026-09-23T19:21:42Z); cards t_47842b32, t_70e40af8, t_91826004, t_b99d8978; shas e338e79f72) | read-only: _process_cwd_within absent from up/hermes_cli/kanban* | none | log literals '(survivor/liveness gates are evaluated o' 2, 'workspace candidate(s); nothing removed' 2 (13 files); DB/config: config kanban.workspaces_root (live) | 315 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #924 | fix(kanban): re-port canonical survivor binding onto current main (#924) — - Re-port #796's SHA-rewriting mirror / canonical-live-tree survivor onto fork/main 034f8d1142, rather than replaying its conflicting commits. A  (PR body via REST (merged 2026-09-23T20:33:07Z); shas 6902fa64a0) | read-only: _canonical_repos absent from up/hermes_cli/kanban*, _canonical_survivor absent from up/hermes_cli/kanban*, _carries_recovery_artifact absent from up/hermes_cli/kanban* | none | log literals '--path-format=absolute' 3, '--untracked-files=all' 2, 'isolated remote sync' 2 (18 files); DB/config: 1,375 task_workspace_survivors rows | 1941 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #921 | fix(kanban): fail closed on unprovable worker liveness (#921) — Fail closed on host-local Kanban reclaims when worker PID is missing or a signalled worker survives. A `reclaim_refused` event leaves the claim/run with its (PR body via REST (merged 2026-09-23T19:49:10Z); cards t_09180e10, t_3a06ba8f; shas 4b2dcd10a2) | read-only: _abort_lost_claim_spawn absent from up/hermes_cli/kanban*, _claim_still_held absent from up/hermes_cli/kanban*, _prior_worker_still_alive absent from up/hermes_cli/kanban* | none | kanban.db events reclaim_refused 98 (2026-09-24..2026-09-24); DB/config: 98 reclaim_refused events | 1246 loc, 4(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #914 | perf(kanban): key the budget ledger sum on the window, not the board path (#914) — board_spend_usd() cached on (board_db_path, window_hours), but the expensive work inside it — _sum_ledgers() opening every profiles/*/bla (PR body via REST (merged 2026-09-23T11:02:07Z); shas 2f8403f49d) | read-only: _all_ledger_card_costs absent from up/hermes_cli/kanban*, _ledger_card_costs absent from up/hermes_cli/kanban*, _sum_ledgers absent from up/hermes_cli/kanban* | none | DB/config: budget ledger consumer of #900 | 253 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #907 | fix(kanban): refuse destructive writes when pin contradicts requested board (t_e1c2dae5) (#907) — <!-- codesmith:footer --> --- <a href="https://app.blacksmith.sh/ANG-Ventures/codesmith/hermes-agent/pr/907?autoLogin=true (PR body via REST (merged 2026-09-23T16:23:33Z); cards t_d2b884e7, t_e1c2dae5; shas 7d13c8a433) | read-only: EnumeratedBoardSlug absent from up/hermes_cli/kanban*, KanbanPinDivergenceError absent from up/hermes_cli/kanban*, _is_enumerated absent from up/hermes_cli/kanban* | none | 0 distinctive log/event hits | 1149 loc, 2(2) | **UNRESOLVED** — Guard emits no log line/event and no DB column distinguishes it; firing is unmeasurable read-only. Needs a repro on the fork's kanban (upstream's kanban lacks the path, so an upstream RED/GREEN run is not meaningful). | - |
| #901 | fix(kanban): bound and prune the survivor walk; ask git before walking a home (#901) — ## Measured before the fix \| \| \| \|---\|---\| \| unpruned walk of `~/.hermes` \| **850,853 dirs / 227.5 s** (idle disk, ran to completion) (PR body via REST (merged 2026-09-23T11:02:07Z); cards t_6c46905a, t_f5ebd9db; shas aac08c7323) | read-only: _is_repo_on_disk absent from up/hermes_cli/kanban*, _over_budget absent from up/hermes_cli/kanban*, _registered_nested absent from up/hermes_cli/kanban* | none | log literals 'survivor_unavailable:' 578, 'repository enumeration' 49, '-- the workspace is too large to' 48 (51 files) | 718 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #900 | feat(kanban): fan-out brakes — worker-created cards park in triage + per-board 24h USD ceiling (#900) — Two independent brakes on autonomous Kanban fan-out. **The incident (2026-09-22):** ~200 human-carded alert-remediat (PR body via REST (merged 2026-09-23T03:31:56Z); shas 3d6dc9a4c2) | read-only: _board_task_ids absent from up/hermes_cli/kanban*, _budget_cfg absent from up/hermes_cli/kanban*, _cmd_budget absent from up/hermes_cli/kanban* | none | log literals 'work kanban task' 3153, ': getattr(res,' 2, 'Optional[dict]' 2 (39 files); kanban.db events parked_by_policy 436 (2026-09-22..2026-09-25); DB/config: config kanban.worker_created_status= | 1271 loc, 6(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #897 | kanban: 'superseded' terminal outcome for an already-satisfied premise (t_90178503) (#897) — A worker that verifies its card's work **already landed on main** has no honest verb. `complete_task` demands evidence of work  (PR body via REST (merged 2026-09-23T10:45:20Z); cards t_0c5ac29a, t_90178503, t_e484ec2d, t_e587758d; shas 5f26ca8cf7) | read-only: EmptySupersedeError absent from up/hermes_cli/kanban*, _protocol_violation_history absent from up/hermes_cli/kanban*, _run_model_turn_completed absent from up/hermes_cli/kanban* | none | log literals '[hermes-kanban-run-boundary' 2444, 'kanban complete' 173, 'gave up (retries exhausted)' 32 (778 files); DB/config: 31 task_runs with outcome 'superseded' | 1020 loc, 6(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #890 | fix(kanban): SANDBOX=1 neutralising an explicit path pin is no longer silent (#890) — `HERMES_KANBAN_SANDBOX=1` outranks every `HERMES_KANBAN_*` path pin and said nothing about it. The inverse case — a pin escaping a red (PR body via REST (merged 2026-09-23T01:58:37Z); cards t_4b247923, t_84deb81f, t_adec8aba; shas 23af01e58b) | read-only: upstream has no HERMES_KANBAN_SANDBOX | none | 0 distinctive log/event hits | 158 loc, 2(2) | **DROP** — Never fires: the warning literal has 0 hits across 1,741 log files (2026-05-10..09-25). Test-harness-only path. | - |
| #889 | fix(cli): a printed hint must survive a real SHELL, not just shlex.split (#889) — whitespace half, but decided "safe to print bare" with `shlex.split(value) == [value]`. `shlex.split` is a tokenizer: it models quoting an (PR body via REST (merged 2026-09-23T01:41:27Z); cards t_c9e1a012; shas fb52b05a3b) | read-only: _is_shell_literal absent from up/hermes_cli/kanban*, _manual_rollback_remedy absent from up/hermes_cli/kanban*, hint_value absent from up/hermes_cli/kanban* | none | log literals '`git clone --shared' 8, '&& git reset --hard' 2 (7 files) | 419 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #888 | fix(kanban): survivor capture cost is per-REMOTE-REF, starving the kanban_complete tool at 420s (#888) — The kanban_complete TOOL timed out at 420s three times on card t_a49e8a28 and wrote nothing (task stayed `running`) (PR body via REST (merged 2026-09-23T01:39:37Z); cards t_a49e8a28; shas 3489dd81f7) | read-only: _first_containing absent from up/hermes_cli/kanban*, _present_commits absent from up/hermes_cli/kanban*, _rev_list absent from up/hermes_cli/kanban* | none | log literals 'fatal: bad object' 1 (1 files) | 727 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #886 | fix(kanban): close the 4 P1 FleetReview recorded on #848's head 0757be86 (#886) — **1 — a MENTION no longer satisfies the completion path** (`_verified_explicit`) A title/body match was marked `unbound` and still *return (PR body via REST (merged 2026-09-23T01:44:44Z); cards t_1bcd8aec; shas 61964bac42) | read-only: AmbiguousRef absent from up/hermes_cli/kanban*, _bound absent from up/hermes_cli/kanban*, _unbound_keys absent from up/hermes_cli/kanban* | none | log literals 'survivor_unavailable:' 578, 'operator override' 297, 'Unbound survivor accepted for task' 296 (73 files) | 1097 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #879 | fix(cli): a printed --flag hint must parse as pasted, for any repo key or path (#879) — `--survivor-pr <key>=owner/repo#N`. For a key beginning with `-` argparse binds the token as an option and refuses (`expected one ar (PR body via REST (merged 2026-09-22T18:03:19Z); cards t_c9e1a012; shas 9f11bc8e11) | read-only: hint_arg absent from up/hermes_cli/kanban* | none | 0 distinctive log/event hits | 225 loc, 0(0) | **UNRESOLVED** — Guard emits no log line/event and no DB column distinguishes it; firing is unmeasurable read-only. Needs a repro on the fork's kanban (upstream's kanban lacks the path, so an upstream RED/GREEN run is not meaningful). | - |
| #877 | fix(kanban): PR-gate must not resolve a bare #N against prose or a contradicted repo (#877) — `repo_context()` has two legs. #870 hardened the WORKSPACE leg with two guards. This closes the BODY leg — `_body_repo_choice( (PR body via REST (merged 2026-09-22T18:03:19Z); cards t_5a2114f4, t_9a74e029, t_cb701eee, t_e36cd2b3; shas 4a70d55316) | read-only: _body_repo_choice absent from up/hermes_cli/kanban*, repo_exists absent from up/hermes_cli/kanban* | none | DB/config: PR-gate (#808) live | 389 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #874 | fix(guards): deny the root HERMES_HOME actually resolves to, not <home>/.hermes (t_5bfcbf14) (#874) — The live-board and state.db guards decide "is this production?" from _production_state_roots(), which only ever yielde (PR body via REST (merged 2026-09-23T03:27:59Z); cards t_5bfcbf14, t_c64b9d44, t_f27a9b00; shas 30c9104127) | read-only: LiveBoardWriteRefused absent from up/hermes_cli/kanban*, _assert_live_board_write_allowed absent from up/hermes_cli/kanban*, _deployed_hermes_home_root absent from up/hermes_cli/kanban* | none | log literals 'a production root' 2 (8 files) | 1007 loc, 2(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #872 | fix(kanban): a broken object store names the repo, the lender and a remedy (#872) — Fail-closed is **unchanged** — nothing is skipped, only explained. - `_explain_broken_object_store` classifies a capture that ALREADY fa (PR body via REST (merged 2026-09-22T16:11:51Z); cards t_7863ffc6, t_85cc093e, t_a49e8a28, t_de2e348e; shas 7086f98b46) | read-only: _advertised_head absent from up/hermes_cli/kanban*, _alternates absent from up/hermes_cli/kanban*, _explain_broken_object_store absent from up/hermes_cli/kanban* | none | log literals 'kanban survivor:' 184, 'borrows objects from' 36, 'inside the same workspace;' 36 (21 files) | 518 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #870 | fix(kanban): PR-gate must not resolve a bare #N against an ENCLOSING repo (#870) — with no repo of its own silently answers for whatever contains it. Every `workspace_kind=scratch` card lives under ~/.hermes, which is it (PR body via REST (merged 2026-09-22T13:58:18Z); cards t_7595755b, t_cb701eee; shas 51fb726175) | read-only: _body_corroborates absent from up/hermes_cli/kanban*, _is_repo_toplevel absent from up/hermes_cli/kanban*, _remotes_for absent from up/hermes_cli/kanban* | none | DB/config: PR-gate (#808) live, 52 gate_auto_resolved | 395 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #858 | fix(kanban): gate repair_db(), the second rw door to the live board (t_880f5d17) (#858) — #853 put `_assert_live_board_write_allowed()` at `connect()`. `connect()` is not the only read-write door to a `kanban_db_path()`- (PR body via REST (merged 2026-09-23T11:49:36Z); cards t_4b247923, t_84deb81f, t_880f5d17, t_adec8aba; shas 956e92f201) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | 0 distinctive log/event hits | 112 loc, 1(2) | **UNRESOLVED** — Guard emits no log line/event and no DB column distinguishes it; firing is unmeasurable read-only. Needs a repro on the fork's kanban (upstream's kanban lacks the path, so an upstream RED/GREEN run is not meaningful). | - |
| #856 | fix(kanban): log git's returncode and stderr when survivor capture fails (#856) — NOT a survivor defect, and NOT a RAM-disk / racy-clean-index race. Root cause is outside this repo: `~/.hermes/fleet/ramscratch-env.sh` ex (PR body via REST (merged 2026-09-22T08:23:19Z); cards t_169d6e46, t_721c9203, t_ac0e595c; shas 8cc6d832a3) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 1,375 task_workspace_survivors rows | 67 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #854 | feat(kanban): append-only mutation journal + replay, outside the kanban home (#854) — Two wipes, 2026-09-20 13:52 and 2026-09-21 09:58, took `~/.hermes/kanban` whole: 64 boards / 2,525 tasks. They were restored from an 1 (PR body via REST (merged 2026-09-22T07:23:47Z); cards t_357330bf; shas cd2d2fee41) | read-only: ReplayResult absent from up/hermes_cli/kanban*, _journal_board_slug absent from up/hermes_cli/kanban*, _note_failure absent from up/hermes_cli/kanban* | none | log literals 'unknown task(s)' 2 (3 files); DB/config: journal root ~/.local/state/hermes exists | 913 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #848 | fix(kanban): bind an explicit survivor claim to the card that names it (#848) — `_verified_explicit()` called `verify_pr()` / `verify_ref()` with `mined_for` UNSET, so the one check that ties a PR to a card never ran on  (PR body via REST (merged 2026-09-22T16:10:07Z); cards t_de2e348e; shas 8300bd7513) | read-only: RemoteUnavailable absent from up/hermes_cli/kanban*, _claimant absent from up/hermes_cli/kanban*, _reusable absent from up/hermes_cli/kanban* | none | log literals 'https://github.com/' 696, 'survivor_unavailable:' 578, 'operator override' 297 (315 files); DB/config: 1,375 task_workspace_survivors rows | 1170 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #844 | kanban: exit 126/127 from a worker is INFRASTRUCTURE, not a task failure (t_263e7303) (#844) — - `_classify_worker_exit` / `_classify_run_exit` emit kind `infra_unavailable` - `detect_crashed_workers` requeues **without  (PR body via REST (merged 2026-09-22T02:03:26Z); cards t_25dbd0f7, t_263e7303, t_59c0886e, t_671fd52c; shas dbd0de7187) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | log literals 'without counting a failure' 3, 'no failure counted' 2 (5 files) | 326 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #842 | kanban: reclamation consults the RECORDED survivor instead of re-deriving from stale bases (#842) — A card that completes successfully via a verified `--survivor-pr` ends up `status=done` with its workspace permanently H (PR body via REST (merged 2026-09-22T13:50:00Z); cards t_49c1dec1, t_a49e8a28, t_f31d9d5a; shas 953d5efb97) | read-only: _all_sidecar_repositories absent from up/hermes_cli/kanban*, _merge_refs absent from up/hermes_cli/kanban*, _patch_carry absent from up/hermes_cli/kanban* | none | log literals 'that no inferred survivor vouches for;' 70, 'against the remote' 69, 'missing repositories (' 16 (41 files); DB/config: 1,375 task_workspace_survivors rows | 1718 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #837 | kanban: make the operator survivor reachable when a recorded repo vanishes (#837) — `preserve()` records `bases` at dispatch from the git checkout then in the workspace. When that checkout is later gone but the workspace (PR body via REST (merged 2026-09-22T01:50:12Z); shas e9b406c59b) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 1,375 task_workspace_survivors rows | 242 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #833 | fix(kanban): keep ALL subprocess I/O out of the PR-gate dispatch lock (#833) — #808's finding 4 was *"GitHub calls execute inside `_dispatch_tick_lock` — bound total wall time and prove the lock hold cannot include netwo (PR body via REST (merged 2026-09-21T20:15:33Z); shas a3e3c169b9) | read-only: SandboxEscape absent from up/hermes_cli/kanban*, _blocked_gate_refs absent from up/hermes_cli/kanban*, _db_is_sandboxed absent from up/hermes_cli/kanban* | none | DB/config: PR-gate (#808) live | 817 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #831 | fix(kanban): refuse a non-spawnable review assignee at request-review (#831) — 2026-09-21: 10 cards sat in `status=review` with `assignee='reviewer'` — a literal string, not a profile. One waited 2h+. Nothing alerted: `d (PR body via REST (merged 2026-09-22T01:47:02Z); shas e8666698e6) | read-only: _collect_review_awaiting_human absent from up/hermes_cli/kanban*, _notify_script_path absent from up/hermes_cli/kanban*, _print_review_awaiting_human absent from up/hermes_cli/kanban* | none | log literals 'is refused; pass a different reviewer or' 6, 'is the implementer — same-actor review' 6, 'is not an installed profile and is not' 4 (19 files); kanban.db events review_stale_alerted 151  | 1054 loc, 4(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #829 | feat(kanban): batch set-model selectors + time-boxed lane-model override (#829) — Putting one batch of ~11 kanban workers on an open provider (apr pool capped) had two honest options: 11 invocations of `set-model`, or ed (PR body via REST (merged 2026-09-24T05:35:12Z); cards t_343f0279, t_55547259, t_f08b7589; shas a7942d7989) | read-only: BatchRouteWrite absent from up/hermes_cli/kanban*, LaneModelOverride absent from up/hermes_cli/kanban*, ModelOverride absent from up/hermes_cli/kanban* | none | log literals '(applies on next dispatch)' 2, ': route applied but reclaim failed:' 1 (4 files); DB/config: lane_model_overrides table exists (0 rows now); 5 'lane-model' log lines; 217 model_override_ | 4729 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #823 | fix(kanban): gate flagship worker models (#823) — Enforces the shared `fable`/`astra` flagship-model ban at Kanban CLI create/set-model, DB mutation, dashboard API, and dispatch boundaries. Explicit `--allow-flagship` re (PR body via REST (merged 2026-09-21T16:10:09Z); shas 2b13c99e1a) | read-only: banned_worker_model_substrings absent from up/hermes_cli/kanban*, flagship_model_error absent from up/hermes_cli/kanban*, flagship_model_match absent from up/hermes_cli/kanban* | none | log literals 'flagship override:' 7, '' is orchestrator-only (Ace 2026-09-17).' 3, 'PHASE=kanban_flagship_refused task=' 3 (22 files); DB/config: flagship ban: 'model_override' 746 lines; --allow-flag | 632 loc, 4(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #822 | fix(git): disable optional locks in bounded probes (#822) — - disable Git optional index refresh locks for every bounded internal Git subprocess - pass `--no-optional-locks` explicitly to bounded `git status` probes - ad (PR body via REST (merged 2026-09-21T19:58:21Z); shas edb28801ce) | RED by read: only hermes_cli/source_check.py:48 sets GIT_OPTIONAL_LOCKS upstream | none (upstream PR 118111 open) | log literals '--untracked-files=all' 2 (1 files) | 165 loc, 0(0) | **KEEP** — Filed upstream as #118111; drop when it merges. | - |
| #819 | fix(plugins): isolate concurrent hook callbacks (#819) — - stop treating ordinary concurrent plugin callback invocations as timed out - arm callback suppression only after an invocation actually exceeds its budget - pres (PR body via REST (merged 2026-09-21T19:56:57Z); shas 846ba492a1) | read-only: upstream plugins.py has no concurrent-callback isolation | none | log literals 'timed out after' 4730, 'pre_tool_call plugin callback' 907, 'suppressed after callback' 222 (91 files) | 725 loc, 1(1) | **KEEP** — Fires: 'timed out after' 4,730 lines, 'pre_tool_call plugin callback' 907 lines. | - |
| #816 | fix(models): make CANONICAL_PROVIDERS plugin auto-extend LAZY (fixes circular import for plugins importing models surfaces) (#816) — `hermes_cli/models.py` called `providers.list_providers()` **inside its own top-level i (PR body via REST (merged 2026-09-21T16:02:10Z); shas e8249c38cb) | read-only: upstream keeps eager discovery | none | 0 distinctive log/event hits | 992 loc, 2(3) | **KEEP** — Registered in docs/sync/fork-features.json (lifecycle upstream-intended). | - |
| #811 | post-merge FleetReview on #785: fix 6 findings, dispute 1, partial 1 (kanban deletion gates) (#811) — **Verdicts: 6 TRUE_BUG (fixed) · 1 FALSE (disputed with repro) · 1 PARTIAL (waste real, harm false).** Nothing rose to (PR body via REST (merged 2026-09-22T17:39:19Z); cards t_ac7d68d4; shas fe44db2aa8) | read-only: _conn_is_board absent from up/hermes_cli/kanban*, _reserve_slug absent from up/hermes_cli/kanban*, _slug_candidates absent from up/hermes_cli/kanban* | none | log literals '<unreadable-board-db>' 1 (1 files) | 817 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #809 | kanban: block on failure #1 when a card's workspace anchor can never resolve (#809) — A worktree card anchored on a BARE repo fails in `_resolve_worktree_workspace` with a **deterministic** error — the repo's shape does  (PR body via REST (merged 2026-09-21T14:21:34Z); cards t_b4c2d053; shas df86b248e9) | read-only: _unusable_workspace_reason absent from up/hermes_cli/kanban* | none | log literals 'is not inside a git repo and does not po' 1 (10 files) | 451 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #808 | kanban: auto-resolve blocks whose gate PR has already merged (#808) — A worker that needs a sibling PR landed before it can continue blocks with a reason like *"merge PR #787 then unblock me"*. Nothing in the dispatcher  (PR body via REST (merged 2026-09-21T14:13:01Z); cards t_0f12e325, t_18593332, t_3ef97a63, t_65cb6f93; shas bc11e5d83a) | read-only: GateOutcome absent from up/hermes_cli/kanban*, PrRef absent from up/hermes_cli/kanban*, _PrefetchResult absent from up/hermes_cli/kanban* | none | log literals 'kanban PR-gate:' 3631, 'taking no action' 3612, 'gate satisfied' 23 (24 files); kanban.db events gate_auto_resolved 52 (2026-09-21..2026-09-25); DB/config: 52 gate_auto_resolved events | 1645 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #804 | fix(kanban): fail closed when scratch mount is unavailable (#804) — - add canonical `kanban.workspaces_root` and `kanban.workspaces_root_require_mount` config keys - preserve per-board scratch placement under a configure (PR body via REST (merged 2026-09-23T06:53:41Z); cards t_7c3cbb38; shas f3aacf336e) | read-only: WorkspaceUnavailable absent from up/hermes_cli/kanban*, _WorkspaceAdmission absent from up/hermes_cli/kanban*, _WorkspaceRefusalOutageNotifier absent from up/hermes_cli/kanban* | none | log literals 'kanban dispatch:' 377, 'kanban dispatcher tick [' 50, 'SELECT payload FROM task_events WHERE ta' 2 (21 files); kanban.db events stranded_by_mount_loss 13 (2026-09-24..2026-09-25); DB/con | 1002 loc, 4(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #803 | fix(kanban): release dependency-backed creation holds (#803) — - tag creation-time blocked events so dependency resolution can distinguish them from explicit worker/operator blocks - preserve parentless `initial_status=b (PR body via REST (merged 2026-09-21T14:02:53Z); shas a2aa7349e1) | read-only: _format_parent_satisfied_sticky_summary absent from up/hermes_cli/kanban*, _latest_block_source absent from up/hermes_cli/kanban*, find_parent_satisfied_sticky_blocks absent from up/hermes_ | none | log literals 'parents_done_sticky=' 16012 (11 files); DB/config: 74 dependency_wait events; 'parents_done_sticky=' 16,012 lines | 257 loc, 5(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #802 | feat(fallback): let a chain entry derive its rungs from a registered source (#802) — A `fallback_providers` chain is a hand-written list of `{provider, model}` rungs. That is fine while the rungs are stable, and it rots  (PR body via REST (merged 2026-09-21T14:00:24Z); cards t_fb31680e; shas 99c3354333) | read-only: register_chain_source has no upstream counterpart | none | 0 distinctive log/event hits | 718 loc, 0(0) | **DROP** — Dead hook: zero consumers — no caller of register_chain_source/registered_chain_sources outside fallback_config.py in the fork tree or ~/.hermes/plugins, and no `source:` fallback entry in ~/.hermes/config.yaml. Speculative infrastructure. | - |
| #800 | fix(kanban): expose board tick-lock holder on skipped dispatch (#800) — Observability-only change: records pid, monotonic acquisition time, and acquire site on the board .dispatch.lock. A skipped tick returns this best-e (PR body via REST (merged 2026-09-21T18:34:34Z); shas a07543ea71) | read-only: _read_dispatch_lock_holder absent from up/hermes_cli/kanban*, format_dispatch_lock_skip absent from up/hermes_cli/kanban* | none | log literals '; acquire site=' 5, 'hermes_cli.kanban_db:_dispatch_tick_lock' 5, 'skipped: board dispatcher lock held by p' 5 (8 files) | 332 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #798 | fix(kanban): park a review card — block accepts review, unblock restores it, triage-resolve names the verb (#798) — Three one-line-class fixes so a **finished-but-unclosable** card can be parked without a hand DB edit. M (PR body via REST (merged 2026-09-21T18:51:29Z); cards t_3fd73eba, t_6d967509, t_86d437a7, t_a635b7fc; shas 86fc9bbb99) | read-only: fork review parking path | none | 0 distinctive log/event hits | 156 loc, 1(2) | **UNRESOLVED** — Silent state transition; no event distinguishes it. | - |
| #795 | fix(kanban): accept a VERIFIED external survivor when the implementation is not in the workspace (#795) — The completion gate only inspected the workspace, so a card whose deliverable was an already-merged PR was refused (PR body via REST (merged 2026-09-21T08:15:01Z); shas fdedf3fc2e) | read-only: _loose_files absent from up/hermes_cli/kanban*, _remote_urls absent from up/hermes_cli/kanban*, _verified_explicit absent from up/hermes_cli/kanban* | none | log literals 'https://github.com/' 696, 'git@github.com:' 110, 'against the remote' 69 (295 files); kanban.db events workspace_survivor 1157 (2026-09-20..2026-09-25) | 763 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #794 | fix(kanban): survivor baseline skips unreadable dirs instead of killing the dispatch tick (#794) — workspace rooted at a real home contains dirs the gateway user cannot read (2026-09-20: ~/.hermes/var/skills-portal/caddy (PR body via REST (merged 2026-09-21T02:47:08Z); shas e6b4ff9da2) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 1,375 task_workspace_survivors rows | 52 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #785 | fix(kanban): containment guards must reject path == root before rmtree (#785) — `Path.relative_to` **succeeds on an equal path** — `p.relative_to(p)` returns `Path('.')` rather than raising. So every containment guard bu (PR body via REST (merged 2026-09-21T08:15:01Z); cards t_63fb42f9; shas 9b2d17bafd) | read-only: _audit_workspace_deletion absent from up/hermes_cli/kanban*, _board_has_live_cards absent from up/hermes_cli/kanban*, _durable_audit_log_path absent from up/hermes_cli/kanban* | none | log literals 'Refusing to remove workspace' 363, 'owner-has-live-run owner=' 353, 'strict descendant of a kanban-managed wo' 320 (38 files) | 2473 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #783 | fix(kanban): preserve implementation survivors before completion and cleanup (#783) — - Gate code-task completion on a clean HEAD verified on a remote, or a durable binary implementation.patch attachment; patch-only hand (PR body via REST (merged 2026-09-20T22:35:55Z); shas 6c400e25f9) | read-only: SurvivorUnavailable absent from up/hermes_cli/kanban*, _attachment_dir absent from up/hermes_cli/kanban*, _durable_remote absent from up/hermes_cli/kanban* | none (upstream PR NousResearch#117564 (open)) | log literals 'Workspace HELD for task' 286, '--path-format=absolute' 3, '--untracked-files=all' 2 (58 files); kanban.db events workspace_held 708 (2026-09-20..2026-09-25), workspace_survivor 1157 (202 | 1007 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #781 | fix(kanban): require an open parent for dependency auto-resume (#781) — Only take the todo/auto-resume dependency path when an unfinished blocking parent exists. Otherwise retain kind=dependency but use the existing stic (PR body via REST (merged 2026-09-21T02:30:38Z); cards t_4e750964; shas 1363f30100) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 74 dependency_wait events | 65 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #778 | fix(backup): size the SQLite safe-copy bound so a 3 GB state.db is not dropped from --quick snapshots (#778) — `hermes backup --quick` shipped snapshots with **no `state.db`** (repro 2/2 on 2026-09-20 at 09:24 and 09:31) (PR body via REST (merged 2026-09-21T02:10:36Z); cards t_0e556835, t_c46cca4e; shas 4a60d6bdeb) | RED by read: _safe_copy_db default timeout_seconds=10.0@hermes_cli/backup_sqlite.py:31 — fixed, not sized | none | 0 distinctive log/event hits | 326 loc, 2(2) | **KEEP** — Live: ~/.hermes/state.db is 3.5 GB and fleet-backup-local runs a nightly quick backup (last 2026-09-25 01:52). Upstream-able. | - |
| #774 | fix(config): stop warning on ${env:} refs resolved before .env loads (#774) — Every `hermes <subcommand>` (`kanban`, `config`, ...) printed: ``` Config ref '${env:HERMES_WEBHOOK_SECRET}': HERMES_WEBHOOK_SECRET is not set (PR body via REST (merged 2026-09-21T01:56:42Z); shas f3a0be3e03) | RED by read: warning still emitted@hermes_cli/config.py:1618 | none (upstream PR 117476 open) | log literals 'is not set (check ~/.hermes/.env)' 83, 'keeping the literal placeholder' 83, ', Path.home() /' 5 (13 files) | 419 loc, 1(2) | **KEEP** — Warning literal 83 lines in window. Filed upstream #117476. | - |
| #773 | fix(gateway): stop per-message synchronous work on the event loop (config lock, realpath, atomic writes, psutil) (#773) — The gateway stalls for seconds at a time while handling inbound messages. Discord logs `latency_ex (PR body via REST (merged 2026-09-20T23:58:28Z); shas dc147f7006) | partial: upstream caches raw config (_RAW_CONFIG_CACHE@hermes_cli/config.py:181) but not the gateway per-message paths this row moved | none | log literals 'Drop the per-process identity caches. Fo' 2 (8 files) | 1520 loc, 5(3) | **KEEP** — Symptom class still live: 203 'latency_exceeded' lines in agent/gateway logs (2026-07-29..09-25). | - |
| #771 | fix(kanban): run worker gateways at background CPU priority (#771) — A kanban worker is batch work. It shares a host with RESIDENT gateways that must answer a human in real time, and nothing stopped it from outbidding th (PR body via REST (merged 2026-09-20T20:42:08Z); shas dbca58aec4) | read-only: _apply_background_priority absent from up/hermes_cli/kanban*, _build_worker_priority_preexec absent from up/hermes_cli/kanban*, _host_load_suffix absent from up/hermes_cli/kanban* | none | log literals 'PHASE=worker_spawn task=' 2163, '; dumping all thread stacks and exiting ' 29, 'so the service supervisor can restart it' 29 (14 files) | 457 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #760 | fix(backup): exclude cache/forensic-*/ and cache/**/*.db from the backup walk (#760) — The FULL tier walks every `*.db` under `$HERMES_HOME` and restore-verifies each one. `cache/forensic-native-snapshot-control-20260913 (PR body via REST (merged 2026-09-20T15:15:23Z); cards t_b05aa8c5; shas 9de489575c) | read-only: upstream backup.py has no forensic-cache exclusion | none | 0 distinctive log/event hits | 97 loc, 2(2) | **DROP** — Trigger gone: ~/.hermes/cache/forensic-native-snapshot-control-20260913 was moved (only a .MOVED.txt remains) and `find ~/.hermes/cache -name '*.db'` returns nothing. | - |
| #720 | fix(model): resolve `model.aliases` on every model-string entry point, not just /model (#720) — ## The class `model.aliases` (e.g. `grok: xai-oauth/grok-4.6`) resolved ONLY in the interactive `/model` command. #713 added (PR body via REST (merged 2026-09-19T13:15:55Z); cards t_e22b707c; shas fe6da50511) | read-only: DIRECT_ALIASES referenced only in hermes_cli/model_switch.py + oneshot.py upstream | none | 0 distinctive log/event hits | 634 loc, 4(3) | **KEEP** — Same live config as #713. | - |
| #713 | fix(cli): resolve -m <alias> / <provider>/<model> like /model instead of silently falling back (#713) — `hermes chat -m <x>` sent the raw `-m` string straight to the current provider. Two everyday shorthands broke silent (PR body via REST (merged 2026-09-19T07:48:16Z); shas f3c316be2b) | read-only: upstream cli.py has no -m alias resolution (alias only in /model + oneshot) | none | 0 distinctive log/event hits | 213 loc, 2(3) | **KEEP** — model.aliases configured (gpt/astra/sol); kanban spawns pass -m strings. | - |
| #712 | fix(model): honour excluded_providers for user-defined and injected rows (#712) — `model_catalog.excluded_providers` was applied only to the **built-in** provider rows — `list_authenticated_providers` sections 1, 2 and 2 (PR body via REST (merged 2026-09-19T11:50:13Z); shas d74b3d99c8) | read-only: upstream applies excluded_providers only in inventory.py:54 | none | 0 distinctive log/event hits | 296 loc, 4(3) | **DROP** — Never configured: `model_catalog.excluded_providers` appears in no config.yaml (root or any profile). The filter can never fire on this fleet. | - |
| #711 | fix(cron): propagate `hermes cron run --wait` failure exit status (#711) — `main()` saw None and exited 0 for every cron failure. Observed live: `hermes cron run <missing> --wait` printed "Job not found" with EXIT=0, and (PR body via REST (merged 2026-09-19T11:50:13Z); shas 772c4c0ed7) | GREEN by read: cmd_cron = _forward_command(..., forward_return=True)@hermes_cli/main.py:1930 | 40230dcca7 | 0 distinctive log/event hits | 124 loc, 1(1) | **SUPERSEDED-BY-UPSTREAM** — Upstream's forwarder factory returns cron_command's exit status. | - |
| #707 | fix(kanban): HERMES_KANBAN_DB pin vs explicit board arg — scoped, per-call-site contradiction guard (#707) — **Symptom:** `HERMES_KANBAN_DB` (the dispatcher→worker board pin) silently wins over an explicit `--board`/boar (PR body via REST (merged 2026-09-17T12:31:34Z); cards t_58bd9cff; shas 5d12ce573d) | read-only: _board_db_path_ignoring_pin absent from up/hermes_cli/kanban*, _enumeration_depth absent from up/hermes_cli/kanban*, _pin_contradiction_call_site absent from up/hermes_cli/kanban* | none | log literals 'kanban: ignoring phantom board dir' 920441, '(kanban.db with 0' 920440, '; otherwise remove the directory.' 920440 (88 files) | 819 loc, 5(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #706 | fix(kanban): alias retired board slugs; phantom-board guard must read the WAL (#706) — **Symptom:** after renaming board `ban-forensics` -> `account-health`, a phantom `ban-forensics` board regenerated every ~10s because (PR body via REST (merged 2026-09-17T11:00:50Z); cards t_a1e6e877; shas a0fe7c9640) | read-only: _board_db_is_empty absent from up/hermes_cli/kanban*, _read_board_aliases absent from up/hermes_cli/kanban*, board_aliases_path absent from up/hermes_cli/kanban* | none | log literals 'kanban: ignoring phantom board dir' 920441, '(kanban.db with 0' 920440, '; otherwise remove the directory.' 920440 (71 files) | 330 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #685 | fix(kanban): move the chat_type invariant to the add_notify_sub choke point (#685) — Right after #684 deployed, I drove the **real writers** against a scratch DB on the live runtime: ``` add_notify_sub(raw 'channel') ->  (PR body via REST (merged 2026-09-13T03:37:35Z); shas 0173575a72) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: notify subs chat_type group/thread/dm live | 162 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #682 | fix(gateway): canonicalize BOTH sides of the routing-lane comparison (#682) — A `kanban_notify_subs` row spelled `chat_type='channel'` for a Discord **guild** chat could never match its own chat's live routing entry. The (PR body via REST (merged 2026-09-12T22:00:24Z); shas 59e091ee69) | read-only: _effective_lane absent from up/hermes_cli/kanban* | none | DB/config: notify subs chat_type group/thread/dm live | 460 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #665 | fix(kanban): upstream capacity overload (529/503/overloaded) preserves retries like a quota wall (#665) — **Measured (live runtime b59959509c):** `_POOL_EXHAUSTED_PATTERNS == ['no eligible sub']`; real crash text `"API c (PR body via REST (merged 2026-09-10T02:48:55Z); cards t_1811c4d0; shas 90108f811e) | read-only: _read_receipt absent from up/hermes_cli/kanban*, _run_exit_class absent from up/hermes_cli/kanban*, is_pool_exhausted_exit absent from up/hermes_cli/kanban* | none | log literals 'try again later' 23552, 'sub pool capped' 42, 'exited rate-limited (' 2 (159 files); DB/config: 'try again later' 23,552 lines; 4,338 rate_limited runs | 355 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #664 | fix(kanban): stale last_failure_error no longer poisons the respawn guard; age-bound blocker_auth; name the error on defer (#664) — `check_respawn_guard` honored `tasks.last_failure_error` **indefinitely**. Observed 2026 (PR body via REST (merged 2026-09-10T00:21:01Z); cards t_53316ee6; shas 8f2f4bee8a) | read-only: _fmt_respawn_guard_detail absent from up/hermes_cli/kanban*, _respawn_guard_eligible_at absent from up/hermes_cli/kanban*, _respawn_guard_failure_reset_after absent from up/hermes_cli/kanba | none | DB/config: 18,361 respawn_guarded lines; 26,889 respawn_guarded events | 401 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #655 | fix(kanban): preserve quota exits and defer exhausted providers (#655) — Fixes: provider quota walls being counted as Kanban worker crashes and tripping the task-failure circuit breaker. Incident measurements supplied wi (PR body via REST (merged 2026-09-09T01:05:06Z); shas 2e85eb52c2) | read-only: WorkerExit absent from up/hermes_cli/kanban*, _classify_run_exit absent from up/hermes_cli/kanban*, _counter_repair absent from up/hermes_cli/kanban* | none | kanban.db events deferred 630 (2026-09-24..2026-09-25); DB/config: 630 deferred events; 4,338 rate_limited runs | 1165 loc, 6(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #654 | fix(kanban): respawn guard honors ALL operator requeue verbs, not three of five (#654) — active_pr guard, but hard-listed only changes_requested / unblocked / review_reopened. Two operator verbs were missing: triage_reso (PR body via REST (merged 2026-09-08T19:18:02Z); cards t_e0c917fc; shas e8cc9d6b7b) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | kanban.db events reopened 165 (2026-08-14..2026-09-25), triage_resolved 569 (2026-08-08..2026-09-25); DB/config: 569 triage_resolved + 165 reopened events | 108 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #653 | fix(kanban): honor post-PR requeues in respawn guard (#653) — Fixes `active_pr` blocking explicit fix-round requests while the worker's existing PR is still open. Incident: 2026-09-07, `clanker-voice-backlog`, task `t_80 (PR body via REST (merged 2026-09-08T08:08:53Z); cards t_800b8189, t_8838e935; shas 01e77decb6) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 26,889 respawn_guarded events | 214 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #638 | fix(ci): make 3 tests container-hermetic + stop doctor skipping backend sections in containers (#638) — **Real product bug found**: doctor's in-container branch reset any non-docker `terminal_env` to local, silently skip (PR body via REST (merged 2026-08-24T22:20:23Z); shas 3f99f15f34) | GREEN by read: _check_docker_backend(terminal_env, running_in_container, ...)@hermes_cli/doctor_tools.py:202 keeps backend sections in-container | a36221ed91 | 0 distinctive log/event hits | 18 loc, 1(1) | **SUPERSEDED-BY-UPSTREAM** — 18-loc CI/container-hermetic fix; upstream restructured doctor into doctor_tools/doctor_platform. | - |
| #637 | fix(auth): defer hermes_cli.config import until after PROVIDER_REGISTRY exists (#637) — Fix: one choke-point import move (below the registry definitions) instead of touching all 37 provider plugins. Ordering contract doc (PR body via REST (merged 2026-08-24T21:05:28Z); cards t_bea61cdd; shas 3b6f94f04f) | GREEN by read: `from hermes_cli.config import (  # noqa: E402`@hermes_cli/auth.py:259 (import deferred below PROVIDER_REGISTRY) | 30b0958a69 | 0 distinctive log/event hits | 87 loc, 1(2) | **SUPERSEDED-BY-UPSTREAM** — Upstream landed the same fix under the same title (2026-08-24). Drop ours at next sync, take theirs. | - |
| #636 | fix(kanban): close the ambient-env reads the OWNER_PID anchor left open (#636) — **1. Read-side sites still resolving the dispatcher's pins straight from `os.environ`.** A non-owning process attributes its writes to the  (PR body via REST (merged 2026-08-31T23:30:01Z); shas 71db398e05) | read-only: _is_dispatcher_owned_worker_process absent from up/hermes_cli/kanban*, _owned_worker_task_id absent from up/hermes_cli/kanban*, _owns_dispatcher_run absent from up/hermes_cli/kanban* | none | kanban.db events reopen_child_fanout 25 (2026-09-10..2026-09-25); DB/config: 25 reopen_child_fanout events | 580 loc, 4(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #620 | fix(models): strip relay routing prefix before model-listing lookup (#620) — A relay/pool provider takes a model string shaped `name:<sub>/<model-id>` — the `name:<sub>/` prefix pins **which upstream subscription** serve (PR body via REST (merged 2026-08-20T01:31:42Z); shas ac92718abb) | not probed | none | 0 distinctive log/event hits | 58 loc, 2(2) | **UNRESOLVED** — no judgment recorded | - |
| #619 | feat(doctor): surface the absolute runtime interpreter path (#619) — but never the interpreter PATH — which is the thing a caller actually needs when writing a cron/launchd job or a verification script. Reported as paper (PR body via REST (merged 2026-08-20T00:56:52Z); shas 4be3c279c1) | read-only: upstream doctor has no interpreter-path line | none | 0 distinctive log/event hits | 83 loc, 0(0) | **DROP** — Never measured in use (papercut pc-bf39c22e only); 0 conflict files, so the drop is cheap either way. `hermes doctor` is not run by any script in ~/.hermes/scripts. | - |
| #617 | feat(cron): add `hermes cron list --json` for machine-readable job records (#617) — script inspecting cron state had two bad options: scrape the table past ANSI color and box characters, or read `~/.hermes/cron/jobs.json (PR body via REST (merged 2026-08-19T23:51:47Z); shas c23a31bc2c) | RED by read: hermes_cli/subcommands/cron.py has no --json; cron_list@hermes_cli/cron.py:168 prints the table only | none | 0 distinctive log/event hits | 153 loc, 1(1) | **UPSTREAM** — Consumer exists: skills-shared/general/scheduler SKILL.md + references use `hermes cron list --json`. Generic; 0 conflict files. | - |
| #595 | fix(models): honest model-verification warning + transient-probe retry (#595) — Observed live (2026-08-16, Discord `/model` switch to `claude-fable-5` on claude-apr): the harness printed > Note: could not verify claude-f (PR body via REST (merged 2026-08-16T23:39:27Z); shas d750c1f90f) | read-only: no transient-probe classifier upstream | none | 0 distinctive log/event hits | 288 loc, 2(2) | **KEEP** — Relay providers live; literals 12 lines/4 files. | - |
| #593 | fix(cli): hide numeric failover lanes in `hermes model`, the third picker surface (#593) — `hermes model` (hermes_cli/main.py) was the only provider picker that applied no lane filter. It rendered 69 rows, 32 of them `cl (PR body via REST (merged 2026-08-16T22:50:10Z); shas 569a950404) | read-only: upstream has no failover-lane filter | none | 0 distinctive log/event hits | 30 loc, 1(1) | **KEEP** — 17 claude-apx/bpx lanes in ~/.hermes/config.yaml. Fleet-permanent picker policy: registry entry needed. | - |
| #590 | [verified] warn on kanban dispatch file collisions (#590) — Warns (never blocks) when a card being dispatched touches files already in scope of an **in-flight** card. ## Why Apollo dispatched `t_e2096e27` (extend the cov (PR body via REST (merged 2026-08-18T19:46:05Z); cards t_6f8721d6, t_e2096e27; shas 832c216c48) | read-only: _changed_files_from_value absent from up/hermes_cli/kanban*, _check_dispatch_file_collisions absent from up/hermes_cli/kanban*, _dispatch_event_payload_seen absent from up/hermes_cli/kanban | none | log literals 'dispatch continues' 2938, 'KANBAN DISPATCH COLLISION WARNING:' 2937, 'KANBAN DISPATCH COLLISION CHECK PARTIAL:' 1682 (16 files); kanban.db events dispatch_collision_peer_warning 632 (202 | 789 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #588 | fix(kanban): stop #568's creator binding from vetoing worker-card wake evidence (#588) — Root cause of the 2026-08-12 phantom session. See commit message for full anatomy. Creator binding now narrows by stamp shape inste (PR body via REST (merged 2026-08-13T02:23:33Z); shas 7a10bba517) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 660 notify subs | 259 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #586 | fix(kanban): make worker authority non-transitive across process boundaries (#586) — A dispatcher-spawned Kanban worker's `HERMES_KANBAN_*` variables are **ambient process environment**, so every child inherits them — an (PR body via REST (merged 2026-08-12T22:11:48Z); cards t_09b90233, t_381d6d0a; shas d6b61dee4b) | read-only: _cmd_reopen absent from up/hermes_cli/kanban*, claim_kanban_worker_authority absent from up/hermes_cli/kanban*, owns_kanban_worker_authority absent from up/hermes_cli/kanban* | none | log literals 'environment for task' 4684, 'refusing to mutate' 9, ': this process inherited the Kanban' 7 (28 files); kanban.db events reopened 165 (2026-08-14..2026-09-25); DB/config: 165 reopened ev | 624 loc, 4(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #579 | fix(kanban): zero-byte board .db files must not exist, and must not read as empty boards (#579) — A zero-byte `.db` is worse than a missing one. `sqlite3.connect()` on an **existing** zero-byte file succeeds even with `m (PR body via REST (merged 2026-08-11T08:55:21Z); shas c5ce393d8c) | RED by read: _validate_sqlite_header@hermes_cli/kanban_db_connect.py:264 lets zero-byte files pass | none | 0 distinctive log/event hits | 493 loc, 2(2) | **UNRESOLVED** — Guard is silent; no zero-byte board exists now (could be the guard working or the trigger gone). | - |
| #568 | fix(kanban): repair alt-key routing identity without guessing (#568) — Follow-up to #566. Closes the two remaining alt-key routing blind spots: - `notify-repair` now carries coherent `(user_id, user_id_alt, scope_id)` ev (PR body via REST (merged 2026-08-11T02:52:09Z); shas 4a88c5f556) | read-only: _WakeRoutingIdentity absent from up/hermes_cli/kanban*, _routing_participant_index absent from up/hermes_cli/kanban*, effective_routing_lane absent from up/hermes_cli/kanban* | none | log literals 'incomplete subscription(s).' 2 (3 files); DB/config: 660 notify subs (discord group 64/thread 6, telegram dm 62 on default board) | 1396 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #566 | fix(kanban): carry the wake's full key identity as data so alt-id and Slack chats can't phantom (#566) — #562 (card 5) closed the phantom whose cause was MISSING EVIDENCE: an identity-less `kanban_notify_subs` row now re (PR body via REST (merged 2026-08-11T00:20:41Z); shas 333501d563) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 485 notify subs carry scope_id | 599 loc, 6(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #555 | fix(kanban): stop identity-less notify subs minting a phantom 2nd session (#555) — A notify-sub row with an empty `user_id` makes the wake injector rebuild the creator's scope with `user_id=None`, and `build_session_key( (PR body via REST (merged 2026-08-10T09:46:38Z); cards t_9cf4069c; shas 99300bf825) | read-only: _cmd_notify_repair absent from up/hermes_cli/kanban*, _routing_participant_index absent from up/hermes_cli/kanban*, backfill_notify_sub_user_ids absent from up/hermes_cli/kanban* | none | DB/config: 11 notify subs still have empty user_id (the repaired class persists) | 871 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #545 | feat(kanban): attribute comments to their run and session (#545) — Two concurrent sessions running the **same profile** (`apollo`) issued contradictory intent on one card — one parked it, a sibling granted a waiver and r (PR body via REST (merged 2026-08-10T04:15:44Z); shas e1c4772942) | read-only: _comment_from_row absent from up/hermes_cli/kanban*, _inline_comment_provenance absent from up/hermes_cli/kanban*, _resolve_current_session_id absent from up/hermes_cli/kanban* | none | DB/config: 6,363 comments with run_id; 12,260 with session_ref | 1373 loc, 3(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #537 | fix(config): stop `config set compression.<lcm_knob>` crying wolf (#537) — ⚠ 'compression.skew_floor' is not a recognized config key — it was saved anyway, but Hermes may not read it. Hermes does read it. The LCM context (PR body via REST (merged 2026-08-10T00:58:14Z); shas 6f66e6be57) | read-only: upstream config_defaults lacks the LCM compression knobs | none | 0 distinctive log/event hits | 127 loc, 1(2) | **KEEP** — LCM is the live context engine (plugins tranche #480). | - |
| #536 | fix(backup): exclude staging DISK IMAGES from the full-tier walk (#536) — `var/subvps-staging.sparseimage` — a case-sensitive APFS sparse image that `sub-vps-backup-pull.py` **auto-creates and attaches as rsync staging s (PR body via REST (merged 2026-08-10T00:51:58Z); shas ee8b966a02) | read-only: no sparseimage exclusion upstream | none | 0 distinctive log/event hits | 83 loc, 1(2) | **KEEP** — Trigger present: ~/.hermes/var/subvps-staging.sparseimage exists. | - |
| #522 | fix(kanban): stop zombie goal loops on ownership loss (#522) — - Stop Kanban goal loops when the task's `current_run_id` no longer matches the worker's dispatcher run, before another judge or continuation turn. - Track c (PR body via REST (merged 2026-08-09T05:54:21Z); shas ba27326584) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | log literals 'kanban goal loop: task' 28, 'was unreachable' 6 (10 files); DB/config: 17 goal_mode tasks; 'goal loop' 37 log lines | 178 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #519 | fix(kanban): ignore provenance links in triage stranding reports (#519) — - restrict `find_stranded_by_triage()` to normalized blocking links - restrict `block_task()` escalation events to children linked by blocking edg (PR body via REST (merged 2026-08-09T02:37:38Z); shas ca02782c44) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 693 derived-from links (provenance links excluded from stranding) | 103 loc, 1(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #515 | fix(kanban): guard delete_task against live workers; add HERMES_KANBAN_SANDBOX (#515) — ## Defect 1 — `delete_task()` had no live-claim guard `delete_task()` deleted a `running` task holding a live `worker_pid` exactly a (PR body via REST (merged 2026-08-10T00:42:33Z); shas cc839ab87c) | read-only: TaskRunningError absent from up/hermes_cli/kanban*, _kanban_path_override absent from up/hermes_cli/kanban*, _warn_if_override_escapes_hermes_home absent from up/hermes_cli/kanban* | none | log literals 'the live board.' 15 (33 files) | 441 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #514 | feat(kanban): a DEPLOY must never be gated on a DISCOVERY — add a non-blocking link kind (#514) — `t_693d7cc4` — DEPLOY the `metadata.user_id` top-level-scope fix, already merged and green — was linked as a child of `t_7 (PR body via REST (merged 2026-08-08T23:20:54Z); cards t_08cca32f, t_693d7cc4, t_76bab703, t_7ef87c06; shas d86e166ee4) | read-only: _fmt_links absent from up/hermes_cli/kanban*, child_links absent from up/hermes_cli/kanban*, normalize_link_kind absent from up/hermes_cli/kanban* | none | DB/config: 693 'derived-from' task_links rows | 763 loc, 3(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #513 | fix(kanban): give `triage` a supported exit and make downstream stranding loud (#513) — `triage` is where `block_task`'s unblock-loop breaker parks a card after an automated unblocker has failed `BLOCK_RECURRENCE_LIMIT`  (PR body via REST (merged 2026-08-08T21:12:54Z); cards t_6a380f09, t_dcbd3e43, t_f471e2b2; shas 6c4436712c) | read-only: _cmd_triage_resolve absent from up/hermes_cli/kanban*, _print_stranded_by_triage absent from up/hermes_cli/kanban*, _print_triage_banner absent from up/hermes_cli/kanban* | none | log literals 'kanban dispatcher [' 27772, '. Resolve with' 9395, '`hermes kanban triage-resolve <id> --to' 9395 (26 files); kanban.db events triage_resolved 569 (2026-08-08..2026-09-25), triage_strand | 861 loc, 3(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #492 | fix(models): dots->hyphens only for Anthropic MODELS, not Anthropic-wire relays (#492) — `normalize_model_for_provider()` applies Anthropic's **dots→hyphens** rewrite based purely on the **provider** being in `_DOT_TO_HY (PR body via REST (merged 2026-08-08T00:04:04Z); shas ae31d8b99c) | RED by read: provider-only dots->hyphens@hermes_cli/model_normalize.py:225 | none | 0 distinctive log/event hits | 91 loc, 0(0) | **KEEP** — Anthropic-wire relay providers are live. | - |
| #491 | fix(kanban): respawn guard fail-closed forever on a JSON-quoted PR URL (#491) — A worker posts its handback as JSON: ```json "pr": "https://github.com/o/r/pull/3", ``` `_RESPAWN_GUARD_PR_URL_RE` captures `https://github. (PR body via REST (merged 2026-08-08T01:35:01Z); shas cee682986b) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 26,889 respawn_guarded events | 39 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #490 | fix(kanban-diagnostics): a clean block breaks the crash streak (recovered card stops paging CRITICAL) (#490) — `_rule_repeated_crashes` counts a trailing run of `crashed` outcomes, and only `completed`/`reclaimed` broke  (PR body via REST (merged 2026-08-07T23:20:37Z); cards t_c348a9f0; shas fd0e388aa5) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 707 crashed runs across boards | 54 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #486 | feat(kanban): per-task reasoning effort (DB + dispatcher + CLI + tool) (#486) — ## Three commits, three layers 1. **`feat(kanban): let a task pin its own thinking depth`** — cherry-pick of upstream NousResearch `0b69a6ac (PR body via REST (merged 2026-08-07T21:46:18Z); cards t_435e018a; shas be845cc4df) | GREEN by read: fork PR body says commit 1 is a cherry-pick of upstream 0b69a6ac02 'feat(kanban): let a task pin its own thinking depth'; upstream tasks.reasoning_effort present | 0b69a6ac02 | log literals '(applies on next dispatch)' 2, 'Set model override on' 1, 'Set reasoning effort on' 1 (9 files) | 1018 loc, 7(3) | **SUPERSEDED-BY-UPSTREAM** — 377 fork tasks carry reasoning_effort (used). Fork's extra CLI/tool exposure is upstream PRs 79405/80167 (both still open upstream). Fork kanban is a divergent subsystem (fork hermes_cli/kanban_db.py 21,887 lines vs upstream's split kanban_db*.py, kanban_db.py | - |
| #479 | fix(kanban): identify dispatcher chat workers (#479) — Kanban worker turns record an **empty `chat_name`**, so `tokens.ace` renders them as channel `(unknown)`. PR #471 was written to fix exactly this and merged — but th (PR body via REST (merged 2026-08-07T04:44:27Z); cards t_6074d4fe; shas 91462543dd) | read-only: same as #471 | none | 0 distinctive log/event hits | 208 loc, 2(2) | **KEEP** — Follow-up to #471 that made the label actually land; same 4,459 labelled turns. | - |
| #475 | fix(kanban): restore fork contracts clobbered by 11cffc4d5 + catalog test-hermeticity gate (#475) — The commit carried upstream file bodies across wholesale, silently reverting fork-only features its parity NOTEs did not (PR body via REST (merged 2026-08-07T01:21:07Z); cards t_bd593a25; shas b2eb4beef8) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none (upstream PR NousResearch#80564 (closed)) | log literals 'Set model override on' 1 (21 files) | 330 loc, 3(3) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #471 | fix(oneshot): identify kanban worker turns (#471) — - derive one-shot chat identity from the existing kanban dispatcher environment - label worker turns as `kanban / <board> / <task_id>` while using the task id as `chat_ (PR body via REST (merged 2026-08-07T03:42:50Z); shas 5bd89c0317) | read-only: upstream oneshot does not derive chat identity from kanban env | none | 0 distinctive log/event hits | 82 loc, 0(0) | **KEEP** — Fires: 4,350 turns in profiles/daedalus/blackbox/turns.db + 109 in blackbox/turns.db carry chat_name 'kanban / <board> / <task>' (tokens.ace attribution). | - |
| #466 | fix(gateway): mark /curator cli_only — advertised with no gateway handler (#466) — The gateway advertises `/curator` (in `GATEWAY_KNOWN_COMMANDS`, printed by /help) but `gateway/run.py` has no dispatch branch for it. Bec (PR body via REST (merged 2026-08-03T09:37:38Z); shas ea3274ed95) | RED by read: CommandDef('curator', ...)@hermes_cli/commands.py:245 has no cli_only and gateway/ has no curator handler (only _housekeeping_curator@gateway/run.py:4640) | none | 0 distinctive log/event hits | 81 loc, 1(3) | **UPSTREAM** — Generic: the gateway advertises /curator with no dispatch. | - |
| #455 | fix(backup): skip ephemeral kanban board workspaces in the full-zip walk (#455) — A full fleet backup produced thousands of warnings, every one of them under `kanban/boards/*/workspaces/<task-id>/...`: ``` kanban/boards/ (PR body via REST (merged 2026-07-27T10:32:18Z); cards t_2a6fd40c; shas caac7e792f) | GREEN by read: hermes_cli/backup.py:1027 '"kanban/boards",  # non-default boards (workspaces/ + attachments/ skipped as regenerable)' | 9ef49cd78f | log literals '[Errno 2] No such file or directory' 535 (56 files) | 87 loc, 2(2) | **SUPERSEDED-BY-UPSTREAM** — Upstream skips board workspaces in its walk. The fork's 535 '[Errno 2]' hits are generic ENOENT lines, not this row's. | - |
| #441 | feat(tui_gateway): bound heavy session reads (#441) — A single Discord voice message posted its 🎙️ transcript echo **twice** — two identical messages 4.3s apart. The gateway log confirms the same `.ogg` was transcribed t (PR body via REST (merged 2026-07-26T13:27:38Z); shas 098693861c) | read-only: same as #215 | none | 0 distinctive log/event hits | 474 loc, 5(3) | **DROP** — Duplicate re-land of #215 (identical stats: 474 loc, same symbols). Drop with #215. | - |
| #437 | fix(kanban): reclaim must not launder a blocked card into ready (#437) — A kanban card created with `--initial-status blocked` did not hold — the dispatcher grabbed and spawned it anyway. On the `parity-doctrine` board t (PR body via REST (merged 2026-07-27T05:55:09Z); cards t_26c2f60a, t_2a6fd40c, t_792df1f5; shas ba181e785e) | read-only: fork-only kanban path (fork kanban_db.py diverged from upstream's split modules) | none | DB/config: 42 blocked cards on default board | 219 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #426 | fix(tests): remove two xfail markers left by the 2026-07-23 parity merge (#426) — **t_05fcd58f — route-aware fast-mode adoption.** `test_fast_route_capability` is an AST enforcement test demanding every request-enforceme (PR body via REST (merged 2026-07-26T03:42:36Z); cards t_05fcd58f, t_78a2fa21; shas bab50617fc) | read-only: no resolve_fast_mode_capability upstream | none | 0 distinctive log/event hits | 217 loc, 2(3) | **KEEP** — /fast is fork-permanent in fork-features.json. | - |
| #400 | fix(kanban): preserve provider in model overrides (#400) — - split valid `provider/model` task overrides into explicit `--provider provider -m model` worker arguments while preserving bare and malformed model strings ver (PR body via REST (merged 2026-07-18T23:53:39Z); shas 4616200f94) | GREEN by read: provider_override field@hermes_cli/kanban_db.py:728, set via _cmd_set_model@kanban.py:580 | c1b0f6f3c1 | 0 distinctive log/event hits | 197 loc, 2(2) | **SUPERSEDED-BY-UPSTREAM** — 885 fork tasks carry provider_override, so the behavior is used; upstream carries provider alongside model natively. Fork kanban is a divergent subsystem (fork hermes_cli/kanban_db.py 21,887 lines vs upstream's split kanban_db*.py, kanban_db.py 4,580 lines; 26 | - |
| #386 | fix(model): hot-reload model.aliases without a gateway restart (#386) — `_ensure_direct_aliases()` populated the process-global `DIRECT_ALIASES` dict **once** (`if not DIRECT_ALIASES`) and never refreshed. A running gate (PR body via REST (merged 2026-07-18T16:17:17Z); shas 0fbfe99ecc) | GREEN by read: _load_direct_aliases@hermes_cli/model_switch.py:203 re-reads load_config() on each call (#83612) | none found by -S (present by read) | 0 distinctive log/event hits | 256 loc, 1(2) | **SUPERSEDED-BY-UPSTREAM** — Upstream reloads aliases from config per call; the once-only DIRECT_ALIASES population is gone. | - |
| #384 | fix(kanban): prevent PR-state budget starvation (#384) — - cache `MERGED` and `CLOSED` GitHub PR states for the dispatcher process lifetime - rotate the five-query dispatch budget past persistent `OPEN`/unknown prefixes  (PR body via REST (merged 2026-07-18T18:21:13Z); shas 2fe31ac69e) | read-only: _invalidate_pr_state_cache absent from up/hermes_cli/kanban*, _load_pr_state_cache absent from up/hermes_cli/kanban*, _pr_state_cache_key absent from up/hermes_cli/kanban* | none | DB/config: '.pr-state-cache.json' 9 lines | 564 loc, 2(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #336 | fix(kanban): clear respawn guard for closed PRs (#336) — - resolve recent GitHub PR comments through `gh pr view` before applying the `active_pr` respawn guard - clear the guard only when every referenced PR is merged/cl (PR body via REST (merged 2026-07-15T16:58:42Z); shas 172bdd3295) | read-only: _PrStateResolver absent from up/hermes_cli/kanban*, _format_respawn_guarded_summary absent from up/hermes_cli/kanban*, _parse_github_pr_url absent from up/hermes_cli/kanban* | none | log literals 'state,mergedAt' 1 (14 files); DB/config: 26,889 respawn_guarded events | 441 loc, 6(2) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| #328 | fix(picker): desktop/TUI model picker honors model.picker hide+order + hides failover lanes (#328) — The desktop Models page (`/api/model/options`) and TUI `model.options` build their picker via `build_models_payload()`  (PR body via REST (merged 2026-07-14T12:14:59Z); shas 6deba6f390) | read-only: same as #240 | none (upstream PR 64324 closed) | 0 distinctive log/event hits | 130 loc, 4(3) | **KEEP** — TUI model.options consumes the same prefs; live config uses model.picker. | - |
| #326 | fix(auth): clear stale xai-oauth last_auth_error on successful token save (#326) — **Observed 2026-07-14:** a stale 07-13 `invalid_grant` ("Refresh token has been revoked") record was still sitting in `auth.json` while g (PR body via REST (merged 2026-07-14T11:18:43Z); shas ae07cf84c0) | read-only: upstream writes last_auth_error@hermes_cli/auth_xai.py:401; clear-on-success not verified | none | 0 distinctive log/event hits | 44 loc, 1(2) | **UNRESOLVED** — No fire signal; needs a repro against upstream auth_xai. | - |
| #313 | fix(plugins): isolate background-review tool whitelist per-task (threading.local -> ContextVar) (#313) — Foreground (and cron) turns intermittently get tools denied with: ``` Background review denied non-whitelisted tool (PR body via REST (merged 2026-07-12T14:52:55Z); shas f179cb822b) | GREEN by read: _review_tool_whitelist@agent/background_review.py:1078 is DISPATCH-side and per review agent; upstream has no threading.local whitelist to leak across pooled threads | c408601937 | 0 distinctive log/event hits | 512 loc, 1(1) | **SUPERSEDED-BY-UPSTREAM** — The bug class (threading.local whitelist leaking onto later foreground turns) cannot occur in upstream's structure. Fork's ContextVar block (fork plugins.py:6738) goes away when the fork takes upstream's background_review. | - |
| #308 | fix(dashboard): offload 10 blocking SessionDB calls off the event loop (#308) — **Evidence (acceptance criteria from the handoff):** - 341 stall warnings in gui.log, worst 2535s (~42min). Correlated timestamps: **2493/25 (PR body via REST (merged 2026-07-12T05:36:14Z); shas bdffdc047c) | GREEN by read: hermes_cli/web_routers/sessions.py (18 offloads) + web_routers/*.py (upstream offloads SessionDB calls per router) | 011ec4513e | log literals 'Session not found' 20 (3 files) | 723 loc, 2(3) | **SUPERSEDED-BY-UPSTREAM** — Desktop-program row (D9); `_do` helper already dead on fork. Converge on upstream's routers. | - |
| #291 | perf(dashboard): lightweight profile listing for session-list + cron aggregators (10s -> ~1s) (#291) — `/api/profiles/sessions?profile=all` (fired on every desktop sidebar refresh) takes **10–12s** with 7 profiles; `/api (PR body via REST (merged 2026-07-11T05:20:17Z); shas ae31b9b088) | read-only: upstream web_routers/profiles.py offloads (13 sites) but keeps full list_profiles() | none | 0 distinctive log/event hits | 111 loc, 2(3) | **UNRESOLVED** — Desktop-origin perf row; dashboard is live (ai.hermes.dashboard, port 9119) but no HTTP access log to measure /api/profiles/sessions traffic. | - |
| #276 | fix(kanban): decompose inherits board default_workdir + sibling worktree isolation (#276) — The Kanban **auto-decomposer** ignores the board's `default_workdir`, so decomposed child tasks land in disjoint per-task **scra (PR body via REST (merged 2026-07-10T23:17:14Z); shas 77f3c40ee5) | RED by read: hermes_cli/kanban_decompose.py has 0 default_workdir references | none (upstream PR 42660 open, 61907 closed) | 0 distinctive log/event hits | 577 loc, 2(3) | **UPSTREAM** — Already filed upstream as NousResearch#42660 (open). Fork use is thin (1 'decomposed' event in kanban.db). Keep until 42660 lands, then drop. | - |
| #240 | feat(picker): config-driven /model picker hide + order (model.picker) (#240) — Two optional `model.picker` config knobs to tailor the interactive `/model` dropdown — **display only**, doesn't affect which providers are u (PR body via REST (merged 2026-07-08T18:25:16Z); shas 306e24a600) | read-only: upstream has no model.picker (upstream PR 64324 closed unmerged) | none | 0 distinctive log/event hits | 191 loc, 1(2) | **KEEP** — Live config uses model.picker.hide/order. | - |
| #239 | fix(picker): match RENAMED failover-lane slugs (claude-apx-N / claude-bpx-N) (#239) — PR #233 hid the `/model` picker failover lanes with `^claude-(?:api-proxy\|bridge)-f\d+$` — but the fleet's failover-provider slugs wer (PR body via REST (merged 2026-07-08T17:09:27Z); shas 72f20564d1) | read-only: upstream picker unfiltered | none | 0 distinctive log/event hits | 92 loc, 1(2) | **KEEP** — Matches the live claude-apx-N/claude-bpx-N slugs. | - |
| #233 | feat(picker): hide numbered Claude failover lanes from the /model picker (#233) — The interactive `/model` picker (Discord select menu — **hard-capped at 25 options, no pagination**; Telegram/Matrix share the same source (PR body via REST (merged 2026-07-08T10:49:03Z); shas adf3b84049) | read-only: upstream picker unfiltered | none | 0 distinctive log/event hits | 120 loc, 1(2) | **KEEP** — Same live lane set as #593; Discord picker is hard-capped at 25. | - |
| #215 | feat(tui_gateway): bound heavy session reads (#215) — Phase 2 of docs/desktop/2026-07-05-session-list-loop-starvation-PRD.md (D-4/D-5/INV-4/INV-6). Shared per-loop async SessionDB heavy-read gate (default 2, config dashb (PR body via REST (merged 2026-07-07T03:00:39Z); shas 66d98d36c1) | read-only: upstream has no SessionDB heavy-read gate; upstream offloads session reads per router (web_routers/sessions.py) | none | 0 distinctive log/event hits | 473 loc, 5(3) | **DROP** — Desktop-program row (D9, PRD docs/desktop/2026-07-05-session-list-loop-starvation). Desktop client retired; gate's literals 4 lines/3 files. Re-landed as #441 after a sync; drop both. | - |
| #200 | fix(backup): exclude self-nested offsite tars + managed browser profile from full backup (#200) — Excludes three large directory classes from the full-tier `hermes backup -o` walk, and fixes a browser-profile prefix over (PR body via REST (merged 2026-07-05T11:22:25Z); shas 104eef56ad) | read-only: no offsite/browser-profile exclusion upstream | none | 0 distinctive log/event hits | 82 loc, 2(2) | **KEEP** — 'offsite-restore-drill' 85 lines; chrome-* profile dirs exist under ~/.hermes. | - |
| #182 | fix(dashboard): offload session read endpoints (#182) — Lane A2 of docs/desktop/2026-07-02-dashboard-eventloop-starvation-PRD.md (v1.0). Remaining 8 sites offloaded; Semaphore(2) for heavy reads (D-3); thread-assert test (PR body via REST (merged 2026-07-03T02:17:49Z); shas c4eed2aa76) | GREEN by read: upstream moved session routes to hermes_cli/web_routers/sessions.py with 18 to_thread/threadpool offloads | 011ec4513e | 0 distinctive log/event hits | 936 loc, 2(3) | **SUPERSEDED-BY-UPSTREAM** — Desktop-program row (D9). 3 of its symbols (_read_session_detail, _read_sessions_page, _rename_session_record) are already gone from fork web_server.py. Converge on upstream's routers. | - |
| #166 | fix(models): dedup namespaced curated vs bare live ids in provider_model_ids merge (#166) — Every model for `claude-api-proxy`, `claude-api-proxy-f1..f5`, `claude-bridge`, `claude-bridge-f1..f5` (and any plugin provider  (PR body via REST (merged 2026-07-02T04:55:45Z); shas 9c479d1349) | read-only: no _canonical_model_key upstream | none | 0 distinctive log/event hits | 173 loc, 2(2) | **UNRESOLVED** — Display dedup; no log signal. Needs a picker repro on upstream. | - |
| #135 | fix(model-picker): don't duplicate the current model when its catalog entry is namespaced (#135) — The `/model` picker (Discord dropdown, screenshot from Ace) listed **`claude-opus-4-8` twice** when it was the current mo (PR body via REST (merged 2026-07-01T03:14:05Z); shas 9ea23030a8) | read-only: no _same_model upstream | none | 0 distinctive log/event hits | 94 loc, 1(2) | **UNRESOLVED** — Display dedup; no log signal. | - |
| #115 | fix(backup): exclude swiftui-docs (Apple IP) from backup zip (#115) — Adds `swiftui-docs` to `_EXCLUDED_DIRS` in `hermes_cli/backup.py` so Apple's Xcode `AdditionalDocumentation` can never enter the backup zip. ## Why `h (PR body via REST (merged 2026-06-29T17:46:41Z); shas d6bd86d17a) | read-only: upstream backup has no swiftui exclusion | none | 0 distinctive log/event hits | 40 loc, 2(2) | **DROP** — Trigger gone: ~/.hermes/swiftui-docs does not exist. | - |
| #39 | feat(kanban): per-task model override write-path (#39) — Spec: `~/.hermes/plans/2026-06-13_kanban-per-task-model-override-SPEC.md` (v1.0, 3 Opus review passes). **No `gateway/run.py` change** — `_default_spawn` already h (PR body via REST (merged 2026-06-14T11:31:03Z); shas e76da44b58) | GREEN by read: _cmd_set_model@hermes_cli/kanban.py:580 + `set-model` verb (kanban.py:1322); tasks.model_override/provider_override columns (kanban_db.py:727-728) | c1b0f6f3c1 | log literals 'kanban spawn task=' 707, 'Set model override on' 1 (13 files) | 528 loc, 4(3) | **SUPERSEDED-BY-UPSTREAM** — Per-task model write-path exists upstream (per-task model dropdown, set/override worker model+provider). Fork kanban is a divergent subsystem (fork hermes_cli/kanban_db.py 21,887 lines vs upstream's split kanban_db*.py, kanban_db.py 4,580 lines; 260 upstream k | - |
| #35 | fix(kanban): make --initial-status blocked sticky against recompute_ready (#35) — `hermes kanban create --initial-status blocked` is documented as the way to park a card for human ops (the R3 gate), but on any board with (PR body via REST (merged 2026-06-12T23:00:41Z); shas 0d71b3f918) | GREEN by read: initial_status=='blocked'@hermes_cli/kanban_db_graph.py:45 + _has_sticky_block@kanban_db.py:2076 (blocked-at-creation event reason 'initial_status', kanban_db.py:1408) | 34120a0ae2 | 0 distinctive log/event hits | 53 loc, 2(2) | **SUPERSEDED-BY-UPSTREAM** — Upstream parks initial_status=blocked cards with a sticky block event. Fork kanban is a divergent subsystem (fork hermes_cli/kanban_db.py 21,887 lines vs upstream's split kanban_db*.py, kanban_db.py 4,580 lines; 260 upstream kanban commits since the 08-07 merg | - |
| #33 | fix(browser): add --remote-allow-origins to CDP debug launch for Chrome 111+ (#33) — Chrome/Chromium **111+** rejects CDP WebSocket handshakes with **HTTP 403** unless the connecting origin is explicitly allowed (the Dev (PR body via REST (merged 2026-06-11T19:46:04Z); shas 34dcdda5e0) | RED by read: launch args@hermes_cli/browser_connect.py:765 lack --remote-allow-origins | none | 0 distinctive log/event hits | 25 loc, 0(0) | **UNRESOLVED** — Fleet uses debug-Chrome profiles, but no log shows a 403 handshake in window; need a live CDP repro to grade. | - |
| nopr:07d219143b | fix(kanban): a COUNT in a card body is a timestamped claim, not current state — Adds a `stale_quantity_in_body` diagnostic: when a non-terminal card's own prose quotes a count of MUTABLE state (conflicts, "N behind", que (PR body via REST (merged direct commit); shas 07d219143b) | read-only: _rule_stale_quantity_in_body absent from up/hermes_cli/kanban* | none | log literals 'working tree clean' 5 (4 files); kanban.db events cancelled 2 (2026-07-27..2026-07-27) | 250 loc, 0(0) | **KEEP** — Fork-kanban behavior, measured live. Not named in docs/sync/fork-features.json: verdict carries a registry write (fork-parity-doctrine D2b) — add under a 'fork kanban subsystem' entry with lifecycle fork-permanent, or upstream-intended where an upstream PR is  | - |
| nopr:36134d8944 | fix(model): route bare gpt-5.5 to Codex OAuth — Ace expects '/model gpt-5.5' to mean the subscription-backed OpenAI Codex route, not OpenAI API or an aggregator slug. Add a built-in direct alias for bare 'gpt-5.5' -> pro (PR body via REST (merged direct commit); shas 36134d8944) | read-only: upstream has no bare gpt-5.5 alias | none | 0 distinctive log/event hits | 29 loc, 1(2) | **DROP** — Stale model: fleet aliases are now gpt-6-* (config model.aliases gpt/astra/sol); 0 `/model gpt-5.5` lines in agent logs. | - |
| nopr:7cb39d1319 | fix(backup): exclude browser-automation profiles + bulk media from full backup — The full `hermes backup` hung AND ballooned on a live fleet host: 1. HANG: it called the SQLite safe-copy on ~/.hermes/chrome-*/...db (Chro (PR body via REST (merged direct commit); shas 7cb39d1319) | read-only: no chrome-* exclusion upstream | none | 0 distinctive log/event hits | 70 loc, 1(2) | **KEEP** — 5+ ~/.hermes/chrome-* profiles exist; literal 'chrome-profile' 11 lines. | - |
| nopr:8a8b81638c | fix(recap): backtick-wrap Files touched paths to stop gateway auto-attach — The /status session recap lists touched file paths via _shortened_path, which emits a bare absolute or ~-relative path for any file outside the  (PR body via REST (merged direct commit); shas 8a8b81638c) | RED by read: _shortened_path@hermes_cli/session_recap.py:95 returns bare absolute/~ paths | none | 0 distinctive log/event hits | 69 loc, 0(0) | **UPSTREAM** — Generic: bare paths in recap trigger the gateway's outbound media auto-attach. | - |
| nopr:8f7823887e | fix(models): name provider + endpoint in anthropic_messages fallback warning + fix provider_label shadowing — Manual re-apply of a02a28941 onto v0.15.1 (validate_requested_model moved to ~3343). Improves the Anthropic-Me (PR body via REST (merged direct commit); shas 8f7823887e) | read-only: upstream validate path has no 'could not verify' wording | none | 0 distinctive log/event hits | 97 loc, 1(2) | **DROP** — Warning-text cosmetics; 0 log hits for the row's literals; superseded in spirit by #595's classifier. | - |
| nopr:9c6214b9c5 | fix: parse openai-codex slash model switches —  (PR body via REST (merged direct commit); shas 9c6214b9c5) | GREEN by read: typed provider/model routing@hermes_cli/model_switch.py:926-934 (#45006) | none found by -S (behavior present by read) | 0 distinctive log/event hits | 27 loc, 2(2) | **SUPERSEDED-BY-UPSTREAM** — 27-loc parse tweak for openai-codex slash switches; upstream's switch path routes a typed model to the declaring provider. | - |
| nopr:9f9bf61409 | fix(model-switch): support inline provider syntax —  (PR body via REST (merged direct commit); shas 9f9bf61409) | GREEN by read: typed-model -> declaring-provider routing@hermes_cli/model_switch.py:926 (#45006) | none found by -S (behavior present by read) | 0 distinctive log/event hits | 231 loc, 1(2) | **SUPERSEDED-BY-UPSTREAM** — Inline provider syntax is handled upstream; fork helpers _parse_inline_provider_model etc. duplicate it. | - |
| nopr:a184062e7b | fix(banner): include local HEAD in update-check cache key — An in-place `git pull`/rebase moves HEAD without changing VERSION or HERMES_REVISION (both None for source installs tracking a fork). The update-check cache key (PR body via REST (merged direct commit); shas a184062e7b) | read-only: upstream banner cache keyed without local HEAD | none | log literals 'commits behind' 1111 (51 files) | 77 loc, 2(2) | **KEEP** — 'commits behind' 1,111 lines. | - |
| nopr:aa0f725eb8 | fix(browser): suppress macOS "Keychain Not Found" modal on debug-Chrome auto-launch — The native browser-toolset auto-launch (`/browser connect` → try_launch_chrome_debug, and the macOS `open -a "Google Chrome"` fallback (PR body via REST (merged direct commit); shas aa0f725eb8) | RED by read: debug launch args@hermes_cli/browser_connect.py:765 lack --use-mock-keychain | none | log literals '--password-store=basic' 2 (3 files) | 48 loc, 0(0) | **KEEP** — Literals 5 lines/3 files. | - |
| nopr:ca61765850 | fix(backup): bound _safe_copy_db so a live 1GB+ DB can't deadlock the full backup — The full `hermes backup -o` deadlocked for 12.5h on a Mac Studio whose live gateway holds a 1.46GB state.db in WAL mode. _safe_copy_db c (PR body via REST (merged direct commit); shas ca61765850) | GREEN by read: _safe_copy_db(src, dst, *, timeout_seconds=10.0)@hermes_cli/backup_sqlite.py:31 with busy_deadline progress callback (:57) | none found by -S (deadline present by read) | 0 distinctive log/event hits | 27 loc, 1(2) | **SUPERSEDED-BY-UPSTREAM** — Upstream bounds the SQLite backup copy. See #778 for the fork-specific SIZE of that bound (kept). | - |
| nopr:d68a4b0fcc | fix(model-switch): resolve provider-module plugin providers in switch path — resolve_provider_full() (the /model switch + --provider resolver) consulted only the models.dev catalog, Hermes overlays, and config.yaml provi (PR body via REST (merged direct commit); shas d68a4b0fcc) | GREEN by read: _plugin_profile_pdef@hermes_cli/providers.py:222 resolves registered provider-plugin profiles in the switch path | none found by -S (behavior present by read) | log literals 'Unknown provider' 47 (47 files) | 72 loc, 1(2) | **SUPERSEDED-BY-UPSTREAM** — Upstream resolves plugin providers natively; fork _resolve_plugin_provider is redundant. The 'Unknown provider' literal (47 hits) is upstream's own message. | - |
| #797 | fix(usage): carry UNKNOWN through the persisted schema, cumulative totals and the Langfuse export (#797) — **1. Persisted schema** (rows 1-8, 41-45, 59-68). `sessions` and `session_model_usage` gain the five `USAGE_UNKNO (PR body via REST (merged 2026-09-23T17:28:00Z); cards t_083ad45e; shas 2605de02cf) | read-only: UNKNOWN usage semantics are fork Blackbox/Langfuse | none | 0 distinctive log/event hits | 1510 loc, 7(3) | **KEEP** — Blackbox is a live fork plugin (plugins tranche). | - |
| #494 | fix(kanban): goal-loop block path must pass the ownership guard (#494) — A **zombie run** — one the dispatcher already closed and replaced — could block a card that a **live successor run** owns and is actively working.  (PR body via REST (merged 2026-08-08T01:27:12Z); shas 2bc92c8e17) | read-only: fork goal-loop ownership path | none | 0 distinctive log/event hits | 151 loc, 2(2) | **KEEP** — 'goal loop' 37 lines; 17 goal_mode tasks in kanban.db. | - |
| #374 | fix(cli): fail loud on -m provider/model when provider does not resolve (no silent strip onto default provider) (#374) — A kanban worker spawned with `-m claude-apr/claude-fable-5` on a profile **without** the `plugins/m (PR body via REST (merged 2026-07-16T23:51:46Z); shas 47aad5565f) | read-only: no vendor-namespace guard upstream | none | 0 distinctive log/event hits | 251 loc, 1(2) | **KEEP** — 'claude-apr/claude-fable-5' 322 lines (worker spawn -m strings). | - |
| #359 | fix(reasoning): align non-desktop max parity (#359) — Hermes's central reasoning parser already accepts `max`, but several generic non-Desktop consumers still stopped at `xhigh` or older subsets. This aligns those surfac (PR body via REST (merged 2026-07-16T03:42:51Z); shas 4739500b8d) | GREEN by read: VALID_REASONING_EFFORTS = (..., 'xhigh', 'max', 'ultra')@hermes_constants.py:991 is the single parser upstream | none (upstream consumers read the constant) | 0 distinctive log/event hits | 478 loc, 14(3) | **SUPERSEDED-BY-UPSTREAM** — Row aligned non-desktop surfaces to the constant; upstream already derives from it. 50 files / 14 conflict files (3 syncs) — high cost for zero fork delta. | - |
| #335 | fix(undo): strip injected gateway markers from /undo·/redo tail preview (#335) — The `/undo` and `/redo` confirmation shows a second line — `↦ Now at — <who>: "<preview>"` — so you can see where the thread landed. In pra (PR body via REST (merged 2026-07-14T17:14:33Z); shas a8f2409d0a) | read-only: /undo is fork-only (hermes_undo.py absent upstream) | none | log literals 'Triggering message id' 68 (4 files) | 115 loc, 2(2) | **KEEP** — /undo is fork-permanent in fork-features.json; 'Triggering message id' 68 lines. | - |
| #235 | feat(merge): reciprocal thread links + footer-consistent message counts (#235) — Makes `/merge` match `/branch` for **navigability** (reciprocal thread links) and **count semantics** (footer-consistent), addressing two t (PR body via REST (merged 2026-07-08T11:51:36Z); shas 142f107c46) | read-only: /merge fork-only | none | 0 distinctive log/event hits | 204 loc, 2(3) | **KEEP** — fork-permanent (registry). | - |
| #234 | feat(branch/merge): reciprocal parent<->child links in thread branches (#234) — Two navigability improvements to the Discord `/branch` + `/merge` thread flow, both requested by Ace. ### 1. Child thread intro links back t (PR body via REST (merged 2026-07-08T11:26:12Z); shas a255daf148) | read-only: /branch,/merge fork-only | none | 0 distinctive log/event hits | 129 loc, 2(3) | **KEEP** — fork-permanent (registry). | - |
| #232 | feat(merge): mark source session as merged + terse accurate merge note (#232) — ## 1. Mark the SOURCE session as merged `/merge` recorded the merge on the **target** (the `_merged_into` idempotency ledger) but wrote noth (PR body via REST (merged 2026-07-08T07:21:46Z); shas 8ff30b2e58) | read-only: /merge is fork-only | none | 0 distinctive log/event hits | 237 loc, 2(3) | **KEEP** — /merge fork-permanent in fork-features.json; locale keys have a consumer (gateway/slash_commands.py). | - |
| #18 | fix(compress): two-line readout — Chat size + Full request size (#18) — Reworks the `/compress` token readout per request into **two clearly labelled lines** instead of one ambiguous estimate: ``` 🗜️ Compressed: 181 → 18 (PR body via REST (merged 2026-06-08T00:38:59Z); shas e7653ef718) | read-only: /compress readout keys fork-only | none | 0 distinctive log/event hits | 325 loc, 3(3) | **UNRESOLVED** — Chat output is not logged ('Chat size:' 0 lines); /compress usage unmeasurable read-only. | - |
| #17 | fix(compress): show real measured context alongside estimate; estimate over full transcript (#17) — `/compress` reported two token numbers that disagreed by ~10x with no explanation (flagged by diagnosing the difference  (PR body via REST (merged 2026-06-08T00:10:32Z); shas 9137c67e61) | read-only: same as #18 | none | 0 distinctive log/event hits | 183 loc, 3(3) | **UNRESOLVED** — Same as #18. | - |
| nopr:ce5f851789 | fix(i18n): add /undo tail-preview keys to all non-English locales — CI (test_i18n.py::test_catalog_keys_match_english) caught that the new gateway.undo.{now_at,now_at_notext,now_empty,party.*} keys existed only in en.yam (PR body via REST (merged direct commit); shas ce5f851789) | read-only: gateway.undo.* keys fork-only | none | 0 distinctive log/event hits | 120 loc, 0(0) | **KEEP** — Locale key-set parity for /undo (registry fork-permanent). | - |
| #631 | test: fail on empty parameter batteries (#631) — From greenhouse card t_47585116. Empty parametrize batteries now fail collection; restores the 22-plugin/44-case platform interface battery. Worker-verified: 44/44 pass, r (PR body via REST (merged 2026-08-21T01:07:10Z); cards t_47585116; shas 5d857baf01) | read-only: upstream pyproject has no empty_parameter_set_mark | none | 0 distinctive log/event hits | 5 loc, 1(2) | **KEEP** — 5-loc CI gate restoring a 44-case battery; measured at collection. | - |
| #391 | fix(packaging): declare hermes_state_ext + hermes_undo in py-modules (#391) — The running gateway survived only because it runs with cwd on `sys.path`. Any neutral-cwd import — e.g. `find_spec("gateway.run")` in the AC-1 (PR body via REST (merged 2026-07-18T17:26:47Z); shas b2b548f5cf) | GREEN by read: pyproject.toml:796 'py-modules list here drifted ... Do not add the list back' — upstream derives root py-modules from the tree in setup.py | 283c4f058c | 0 distinctive log/event hits | 103 loc, 1(2) | **SUPERSEDED-BY-UPSTREAM** — Fork still carries the explicit py-modules list (fork pyproject.toml:441). Take upstream's derivation; hermes_state_ext/hermes_undo then ship by construction. | - |
