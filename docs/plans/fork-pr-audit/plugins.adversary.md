# plugins tranche — ADVERSARY pass (t_00d8eeba)

Input: audit/plugins/verdicts @ 6c8ec8b510 (t_caabb1fa). Every DROP / SUPERSEDED-BY-UPSTREAM row was challenged. Live trees and fleet config were read-only. Nothing was reverted or merged.

| key | auditor verdict | adversary result | evidence | notes |
|---|---|---|---|---|
| #146 | DROP | OVERTURNED → KEEP | plugins/memory/mem0/__init__.py:1309-1310 ('QMD stays configured + untouched for instant rollback'); gbrain_recall.py:60,:251 reuse qmd_recall helpers; all 9 fleet mem0.json enable mem0_qmd; gbrain host placement still being ruled (kanban t_1d5cd8f5, 2026-09-20) | A DROP cannot execute: #248 (KEEP) imports qmd_recall helpers, and the code documents the QMD leg as the live rollback for gbrain. Retiring the rollback is Ace's call; until he does, KEEP. If retired, an authored cleanup that keeps the shared helpers, not a revert. |
| #44 | DROP | OVERTURNED → KEEP | plugins/blackbox/card.py:124-150 _tokens_out_line (introduced by 930d367238) feeds card.py:208 render_card, which /cost uses via commands.py:56-59 _render_card -> card.render; store.py:394 cites _tokens_out_line as a contract; blackbox is fork-identity by ruling | It is not dead code. The finished/unfinished split added here is the Tokens line of the /cost card (a registered command, __init__.py:571-577), not only the alert card. Later commits build on it (the revert conflicts). Blackbox is fork-permanent, so no parity is gained and a DROP needs Ace. Whether the alerts toggle is dormant is a separate question and not this row. |
| #168 | SUPERSEDED-BY-UPSTREAM | STANDS | ledger SKILL.md:34 (DD-1 landmine: _fresh_tail_start same-named def); target_ratio: 0.2 set in ~/.hermes/config.yaml:198 + 9 profile configs; lcmup has 0 target_ratio hits | Matches ledger DD-1. Condition: all 10 fleet configs set compression.target_ratio, and upstream does not read it. The ~5-line target_ratio -> fresh_tail_max_tokens bridge in the auditor's note is REQUIRED, and _fresh_tail_start must be deleted explicitly or the fork def silently wins. |
| #107 | SUPERSEDED-BY-UPSTREAM | NEEDS-LEAD (KEEP (split per ledger DD-2)) | ledger SKILL.md:36-37 ('ensure_messages_dedup_columns bundles two concerns: the dedup column (drop) and the timestamp-provenance columns (keep). Split before deleting'); ~/.hermes/lcm.db: 977,334 rows with superseded_by NOT NULL (read-only count 2026-09-25); hermes_state.py 13 timestamp_* hits vs 0 upstream; incident t_9cdad21b (2026-09-25 01:02) -> #1053 same day | It contradicts the ledger and does not say why. The auditor says to converge on upstream observed_at for the timestamp half, but the ledger says to KEEP the timestamp-provenance columns (no upstream landing site, DC-2). The dedup half has a real data consumer: 977k soft-hidden replay copies are hidden only by the fork's superseded_by read filter (store.py:102). Adopting upstream reconcile.py prevents NEW copies but un-hides the old ones unless there is a purge or migration. The class also produced a new incident today (#1053). A lead must choose a purge/migration plan before DD-2 runs. |
| nopr:8b332be03c | SUPERSEDED-BY-UPSTREAM | NEEDS-LEAD (SUPERSEDED-BY-UPSTREAM after config migration) | fork __init__.py:186,218-221,276-277 sends X-API-Key from admin_api_key/MEM0_ADMIN_API_KEY; upstream __init__.py:97-99,229 reads only api_key/MEM0_API_KEY; all 9 fleet mem0.json: admin_api_key set, api_key absent, host set, ca_bundle set | Taking upstream's SelfHostedBackend as-is means no fleet profile sends an API key (upstream _backend.py:71 omits it when empty), which gives a 401 unless the server is AUTH_DISABLED. The ca_bundle key is also lost until the upstream-59ed0e41a1 branch lands. Supersession is real only after a config migration (admin_api_key -> api_key in 9 files) plus 59ed. That needs to be planned; it is not a silent drop. |
| #434 | SUPERSEDED-BY-UPSTREAM | OVERTURNED → KEEP | 8521235b11 --stat: all 6 sites are fork-only files (hermes_cli/kanban_db.py fork parts, plugins/blackbox/routing.py:70, plugins/context_engine/lcm/diagnostics.py:52, lcm/engine.py, mem0/live_e2e_capture.py, telegram adapter); upstream e7d9e07dbc cannot touch fork-only code | Upstream fixed the class in ITS files. The fork-only call sites still need this patch, and dropping it restores Path.home() bypasses (routing.py and engine.py still have residual Path.home() fallbacks). The auditor's own note says to keep the fork sites pointed at the helper, which is a KEEP. The only upstream-overlap part is the kanban_db.py conflict. |
| #171 | SUPERSEDED-BY-UPSTREAM | OVERTURNED → KEEP (until mem0 re-base) | fork mem0/__init__.py:20,56-60 _DaemonThreadPoolExecutor, :636,:1640-1651 prefetch executor lifecycle; dependent #824 f2b051b926 graded KEEP | The verdicts are inconsistent: #824 (KEEP) retires the executors that #171 introduced, so #171 cannot be SUPERSEDED while #824 is KEEP. Upstream's executor shape only applies after the whole mem0 re-base onto the Mem0Backend ABC. Grade both as one unit at that re-base, not separately. |
| nopr:531f1e8c64 | SUPERSEDED-BY-UPSTREAM | OVERTURNED → UPSTREAM | upstream ../up/plugins/memory/mem0/_backend.py:44-49 PlatformBackend: MemoryClient(api_key=api_key), no bounded client; fork __init__.py:719-734 passes httpx.Limits(max_connections=10, keepalive_expiry=30) via client= | The upstream-fix claim is wrong for the defect this commit fixes. The unbounded SDK httpx pool (CLOSE_WAIT fd leak) is on the cloud/Platform path, and upstream PlatformBackend still builds MemoryClient with the SDK default client. Upstream's bounded single client is only SelfHostedBackend. Fleet impact is nil (all 9 profiles are self-host, host set), so it is not a fork KEEP either. It is a valid upstream fix for PlatformBackend. |
| #64 | SUPERSEDED-BY-UPSTREAM | STANDS | upstream __init__.py:147 is_available = api_key or host; _backend.py:71 omits key when empty | Runtime holds. Minor: upstream schema :155 marks api_key required unless mode=='oss', and the fleet mem0.json has no mode (defaults to platform), so `hermes memory setup` would prompt for a key. That is setup-UX only, not a runtime defect. Also depends on the nopr:8b332be03c key migration. |
| nopr:3087d451ce | SUPERSEDED-BY-UPSTREAM | STANDS | upstream __init__.py:247 search filters user_id only; _backend.py:10 _add_kwargs user_id+agent_id; 019103bfbc pins it | Same contract, and upstream has a test for it. The fleet's pin_user_id (nopr:a1a1a1065e, KEEP) sets the value, not the scope shape. No consumer found. |
| nopr:5ae50ce919 | SUPERSEDED-BY-UPSTREAM | OVERTURNED → UPSTREAM | same as nopr:531f1e8c64 (upstream _backend.py:49 unbounded MemoryClient) | Earlier version of the same bounded-pool fix. It rides with 531f as an upstream PlatformBackend fix, not superseded. |
| #149 | SUPERSEDED-BY-UPSTREAM | OVERTURNED → KEEP | fork plugins/kanban/dashboard/dist/index.js 'Scheduled' x2 vs upstream 0; upstream hermes_cli/kanban_db.py:103 HAS status 'scheduled' but its dashboard has no column; fork dashboard ships dist only (no src) | Only the 'review' half is superseded. The 'scheduled' half labels a fork-only dashboard column over a status upstream also has. Taking upstream's dist wholesale would drop the column, not just the label. 0 conflicts, 4 LOC, so it costs nothing to carry. |

Totals: 12 challenged. STANDS 3, OVERTURNED 7, NEEDS-LEAD 2.

## KEEP-UNPROVEN

These are KEEP rows where `evidence.fires` is empty or unmeasured, or where `evidence.original` is only a commit message, and no live knob or hot path backs the claim. The verdict is unchanged; each row still needs a firing measurement or an explicit design-need argument.

| key | subject | why unproven |
|---|---|---|
| nopr:051c2076e1 | fix(lcm): detect and repair content-drifted messages_fts triggers | fires 0 in window, original commit-only; repair path last needed in June |
| nopr:efe50db808 | fix(lcm): toggleable Prong-A identifier fidelity — fixes baseline-repro loop | fires 0, original commit-only; toggle is default-ON via LCM_IDENTIFIER_FIDELITY env (escalation.py:437-443), so it runs, but its effect (fewer merged identifiers) is unmeasured |
| #903 | test(lcm): regression layer for the boot-scan class + a loud signal when engine  | stall signal never tripped in window; test layer only |
| #509 | fix(compaction): only announce a compaction that actually compacts (#509) | no-op compaction class not observed in window's 201 compress lines |
| #251 | mem0: bypass salience gate for corrections (#251) | 0 own log lines; loose 'correction' 7 |
| #320 | fix(mem0): recover orphaned inflight capture rows on gateway startup (#320) | 0 own log lines; 'orphan' 134 shared with blackbox |
| #192 | fix(mem0): search_meta_filtered must send a non-empty query (live auto-capture 4 | 0 log lines; absence of the 400 is the only evidence |
| #117 | docs(mem0): document why dedup search omits agent_id (server treats it as a resu | comment-only; n/a |
| #407 | feat(capture): transient-narration gate on world-fact staging + dropped-log (gbr | guards the staging half, but mem0.json mem0_capture_router.staging_mode=false (read 2026-09-25), so the gated path is disabled |
| #193 | fix(mem0): size-cap oversized turns + dead-letter deterministic provider rejects | 0 own log lines; loose 'dead-letter' 2,793 lines are mostly 'never dead-lettered' drain text |

I did not flag silent KEEPs that have a proven live path or a knob that is set (#996, #465, #887, #531, #508, #613, #825, #102, #393, #431, and the commit-only rows with strong fires: nopr:8b869633a4, nopr:b9e00148c7, nopr:6622f41055, nopr:a1a1a1065e).

## Findings for lead

- #107 / DD-2: the auditor's 'converge on upstream observed_at' contradicts the ledger (keep the timestamp-provenance columns). 977,334 soft-hidden rows in ~/.hermes/lcm.db depend on the superseded_by read filter. DD-2 needs a purge or migration plan before it runs.
- mem0 re-base: every fleet mem0.json uses admin_api_key and ca_bundle, and upstream reads neither. The re-base needs a config migration step.
- mem0 PlatformBackend fd-leak (531f/5ae5): this is a real upstream bug. Candidate upstream PR.
- #44 and blackbox: the ALERT dormancy question is separate from the card text, which /cost uses. Blackbox is still missing from fork-features.json (auditor D2b).
