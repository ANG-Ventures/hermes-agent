# plugins tranche — verdicts (auditor t_caabb1fa)

Generated from `plugins.verdicts.json` by the auditor's bank script. Route disclosure: the card asks for Fable.
Run 10300 (mechanical triage only: triage.json, PR bodies, log grep) was substituted to `claude-opus-5-5` by the
dispatcher (`worker_route_substituted`, reason=overloaded); every verdict was judged in run 10398 on `claude-fable-5-1`.
Fire-count window: `~/.hermes/logs` + `~/.hermes/profiles/*/logs`, file mtimes 2026-05-08..2026-09-25
(agent.log/gateway.log families only reach back to 2026-07-29 after rotation). Log literals were matched with
`grep -rcF`; a row whose patch emits no log line is graded by its subsystem's live state (config flag / DB rows).

Rows banked: 49/84

| PR | problem+evidence | upstream RED/GREEN (how) | upstream fix sha | fire count (window) | cost | verdict | branch |
|---|---|---|---|---|---|---|---|
| #6 | Fork-identity telemetry plugin: per-turn cost/token/tool ledger + /cost. Zero outbound network calls (checked: no http/socket in plugins/blackbox; writes only ~/.hermes/blackbox/turns.db). fork-features.json has NO blackbox entry (registry gap, flagged). — PR #6 body: per-turn telemetry + /cost; the plugin is the fleet's only cost ledger. | read-only: no plugins/blackbox upstream. The delegate seam (parent turn lineage) exists upstream at tools/delegate_tool.py:293 parent_turn_id=getattr(parent_agent,'_current_turn_id') — upstream shipped it 0d9b7132ff (2026-06-03, one day before #6). | 0d9b7132ff (seam only; plugin has no upstream counterpart) | DB: 47,147 turns all-time, 10,916 in last 30d (turns.db ts_start 2026-08-26..2026-09-25); plugins.enabled contains blackbox in ~/.hermes/config.yaml; log literal 10 lines | loc 2741, conflict 4(3), deps #9, #10, #20, #30, #40, #141, #154, #202, #432, #444, #660, #799, #905, #912, #915, #955, #972, #1056 | KEEP | - |
| #141 | /usage shows the rich last-turn card + per-category context breakdown. — PR #141 body; D7 census named this card as one of two unextracted call-site clusters (444 god lines). | read-only: upstream gateway/slash_commands.py has a plain /usage (no blackbox card); comp_* columns are plugin-only. | none | gateway.log+gateway.log.1: 267 '/usage' invocations; DB: comp_skills_tokens populated 10,670/10,916 turns in 30d | loc 1428, conflict 3(3), deps - | KEEP | - |
| #968 | kanban-home-cards plugin: open home cards once at session start (t_13a135f5). — PR #968 body, card t_13a135f5. | read-only: no plugins/kanban-home-cards upstream; home sessions/home cards are a fork workflow concept. | none | log literal 125 lines / 7 files (gateway.log, agent.log.1); plugins.enabled contains kanban-home-cards | loc 1038, conflict 0(0), deps - | KEEP | - |
| #432 | Event-driven new-model pricing sentinel: first sight of an unpriced model alerts once. — PR #432 body (pricing-sentinel spec 2026-07-25). | read-only: no seen_unpriced_models table / sentinel upstream; upstream agent/usage_pricing.py has has_known_pricing()@698 but no first-seen alerting. | none | DB seen_unpriced_models: 4 rows, alerted_at set on all 4; latest 2026-09-23T10:45 (claude-opus-5-5 on claude-apx-14) | loc 882, conflict 2(2), deps #154 | KEEP | - |
| #154 | Self-heal unpriced turns: vendor fallback + reprice + zero=$0. — PR #154 body (tokens.ace unpriced rows). | read-only: upstream agent/usage_pricing.py (35K) has _models_dev_pricing_entry@500 / _unknown_cost@631 — a different fallback chain; no reprice command. | none (upstream evolved its own pricing chain; fork's usage_pricing.py is 136K — that file is the agent tranche's problem) | reprice manifests written through 2026-09-23 (~/.hermes/blackbox/reprice-run-20260923-043136-*.json); DB cost_status 30d: estimated 10,662 / included 246 / partial 8; blackbox.reprice_enabled: true | loc 612, conflict 2(2), deps #660 | KEEP | - |
| #1056 | run_conversation early returns never reached finalize_turn, so on_session_end never fired and turn_api_calls rows had no parent turns row (262/262 non-200 turns parentless). — PR #1056 body, card t_6c09f0c2, measured 262/262 vs 275/2106. | read-only: upstream restructured — finalize_turn@agent/turn_finalizer.py:714-722 fires on_session_end with completed/failed/interrupted flags and conversation_loop.py:1643 is the single post-loop chokepoint (1 early `return {` at :625 vs the fork's ~40). The fork-shape bug is structurally absent upstream. | upstream turn_finalizer chokepoint (no single sha; finalize_turn extraction) | DB 7d: 17 backstop rows (final_text='' AND terminal_error set) — landed 2026-09-25; log literal 18 lines / 9 files | loc 590, conflict 4(3), deps - | KEEP | - |
| #799 | Per-call subscription attribution store (turn_api_calls). — PR #799 body (sub attribution for the relay pools). | read-only: no per-call ledger upstream; fleet relay pools are fork-private. | none | DB turn_api_calls: 20,215 rows, ts 2026-09-23..2026-09-25 (all within window) | loc 578, conflict 0(0), deps #955, #972, #1056 | KEEP | - |
| #444 | Session depth + CLI invocation correlation columns. — PR #444 body. | read-only: depth exists upstream as agent/agent_init.py:545 _delegate_depth (9f408989c4, 2026-05-16, pre-dates the fork PR). cli_invocation_id is fork-only (agent/turn_finalizer.py:1045, agent/codex_runtime.py:764). | 9f408989c4 (depth only) | DB 30d: depth populated 10,916/10,916; cli_invocation_id populated 0/10,916 | loc 382, conflict 5(3), deps - | KEEP | - |
| #1014 | kanban-dashboard: home-session facet + home channel on cards. — PR #1014 body (2026-09-25). | read-only: upstream plugin_api.py:1138 _configured_home_channels() already hydrates home channels (1c7c7c3c5f, 2026-05-04); the home-SESSION facet has no upstream counterpart (home sessions are fork-only). | 1c7c7c3c5f (home-channel half only) | landed 2026-09-25; no log literal yet (0 lines) — too new to measure | loc 368, conflict 1(3), deps - | KEEP | - |
| #20 | Decompose context window (last-call cache split) + bind session_key for plugin commands. — PR #20 body. | read-only: seam half absorbed — gateway/run_inbound.py:1070 `_plugin_context.session_key = self._session_key_for_source(source)`. | 5943347a2a (2026-09-12, fix(gateway): bind session context for plugin slash command handlers) | DB 30d: last_cache_read populated 10,670/10,916 | loc 199, conflict 3(3), deps #141 | KEEP | - |
| #202 | Composite <provider>/<model> id leaked into the model column; fold into split pair at record time. — PR #202 body. | read-only: plugin-internal (record.py); no upstream counterpart. | none | DB 30d: 43 rows with '/' in model — 41 are legitimate openrouter org/model ids, 2 residual leaks on claude-apr (anthropic/claude-opus-4.6, name:claude-bpx-11/claude-fable-5-1) | loc 195, conflict 1(1), deps - | KEEP | - |
| #40 | Widen composition call blobs (comp_calls_json). — PR #40 body. | read-only: no counterpart; seam in agent/conversation_loop.py. | none | DB 30d: comp_calls_json populated 10,670/10,916 | loc 194, conflict 2(3), deps #141 | KEEP | - |
| #10 | Subagent cost rollup in /cost session + per-turn retention sweep. — PR #10 body. | read-only: plugin-internal; no counterpart. | none | DB 30d: 828 is_subagent=1 turns; retention_days: 3650 configured; log literal 5 lines | loc 194, conflict 1(1), deps - | KEEP | - |
| #580 | Chronos armed one-shots whose fire_at was already in the past; the external scheduler fires those immediately, so a stale next_run_at re-ran a scheduled job within seconds of every gateway restart (observed 2026-08-11: an `0 */8` report delivered 7 messages in ~17h, 3 re-fires minutes after a real run). — PR #580 body (2026-08-11 live re-fire observation, quoted in the docstring of _arm_one_shot). | RED (live): copied fork tests/plugins/test_chronos_cron.py into ../up and ran `pytest ::test_arm_one_shot_refuses_a_fire_at_in_the_past ::test_arm_one_shot_sends_provision` with the repo .venv against upstream/main 59004a6235 -> 1 failed (AssertionError: armed a one-shot whose fire_at is in the past ... Left contains one more item: {'job_id': 'stale', ...}), 1 passed (control). Upstream _arm_one_shot@plugins/cron_providers/chronos/__init__.py:101-114 has no past check. | none (git log upstream/main -S'Chronos not arming job' -- plugins/cron_providers/chronos/ = 0 hits; latest upstream touch e56ec8c9f7 is the 403 invalid_client fix, unrelated) | 0 log lines for the guard's info line across ~/.hermes/logs + profiles/*/logs (window 2026-05-08..2026-09-25; gateway.log family only reaches back to ~07-29). Boot-time reconcile condition: fires only when a job has a stale next_run_at at restart; not hit since the logs rotated. Needed-ness rests on the upstream RED + the 08-11 incident, not on a fire count. | loc 184, conflict 2(1), deps - | UPSTREAM | - |
| #9 | Card fixes: chat fields, cache %, alerts toggle, 3d price cache. — PR #9 body. | read-only: no counterpart. | none | DB 30d: chat_name populated 10,928; alerts: blackbox.alerts_enabled=false, sum(alerted)=0 all-time | loc 159, conflict 4(3), deps #44 | KEEP | - |
| #905 | Index turns(ts_start) so rolling-window reads SEARCH not SCAN. — PR #905 body (measured scan). | read-only: plugin-internal. | none | live DB has idx_blackbox_turns_ts_start | loc 94, conflict 0(0), deps - | KEEP | - |
| #915 | Create turns indexes after the column migration (index creation raced the ALTER). — PR #915 body, card t_71ae3a75. | read-only: plugin-internal. | none | live DB carries all 7 idx_blackbox_* indexes | loc 66, conflict 0(0), deps - | KEEP | - |
| #972 | Roll up served subs from the joined call ledger. — PR #972 body. | read-only: plugin-internal. | none | DB 7d: served_subs_json populated 319/4,997 turns | loc 55, conflict 0(0), deps - | KEEP | - |
| #660 | reprice_unpriced honors a snapshot-sourced entry on a notional codex route. — PR #660 body. | read-only: plugin-internal. | none | reprice runs continue (manifest 2026-09-23) | loc 45, conflict 0(0), deps - | KEEP | - |
| #955 | turn_id join between turns and the per-call ledger was misaligned. — PR #955 body; #1056 body measures the post-#955 state (275/2106 clean turns parentless vs 262/262 failed). | read-only: plugin-internal. | none | DB 7d: 8,109 parentless api-call rows remain (mostly pre-#1056 failed turns); clean-turn join works | loc 31, conflict 1(1), deps #1056 | KEEP | - |
| #44 | Alert card output -> finished/unfinished wording. — PR #44 body (cost-alert card text). | read-only: plugin-internal (card.py); no counterpart. | none | never: blackbox.alerts_enabled=false in ~/.hermes/config.yaml, sum(turns.alerted)=0 across all 47,147 turns all-time; the other caller (/cost via commands._render_card -> card.render) has 0 real invocations in gateway.log..log.3 (window 2026-09-20..09-25; the single '/cost' grep hit is prose 'benefits/costs'). | loc 30, conflict 1(1), deps - | DROP | - |
| #912 | Index turns(profile): skill-stats miner DISTINCT profile read was a 107s SCAN. — PR #912 body (measured 107s). | read-only: plugin-internal. | none | live DB has idx_blackbox_turns_profile | loc 21, conflict 0(0), deps - | KEEP | - |
| #30 | Show message count on the conversation-history line. — PR #30 body. | read-only: plugin-internal + agent/model_metadata.py seam; no counterpart. | none | DB 30d: comp_history_message_count populated 10,681/10,916 | loc 19, conflict 2(3), deps - | KEEP | - |
| #149 | Title-case the 'review' + 'scheduled' column labels in the dashboard dist. — PR #149 body (cosmetic). | GREEN by read: ../up/plugins/kanban/dashboard/dist/index.js carries "Review" (1 hit); upstream has no scheduled column at all (0 matches). | 16accefd2f (2026-07-03 feat(kanban): first-class review handoff lifecycle) | n/a (label text) | loc 4, conflict 0(0), deps - | SUPERSEDED-BY-UPSTREAM | - |
| #480 | Re-vendor hermes-lcm v0.21.0-rc2 (from v0.16.2) + announce the engine_preflight_maintenance compaction arm. Fixes a compaction at ~345k tokens vs a 750k threshold with nothing in chat explaining it (naive should_compress_preflight in LCM 0.19). — PR #480 body (session evidence, 345,087 tokens vs 750,000 threshold); VENDORED_FROM.txt records source commit 10cbb78 / tag v0.21.0-rc2. | read-only (D1a: reference is stephenschoettler/hermes-lcm, not NousResearch): lcmup @ 8d1b1e6 (v1.0.0-rc.1, 2026-09-03) — 1277/1277 def/class names added by this row exist there (it IS upstream content). The announce half: lcmup engine.py:518 still sets `self.emit_automatic_compaction_status = False` unconditionally, so upstream's own banner stays inert (that half is graded under #497). | n/a — this row is the vendored copy of upstream itself; upstream has since moved 10cbb78 -> 8d1b1e6 (v1.0.0-rc.1) | LCM is the live engine: config.yaml `engine: lcm`, ~/.hermes/lcm.db 6.48 GB (mtime 2026-09-25); 51 'LCM compaction #' lines + 265 'Context compacted' lines in the log window (2026-07-29..2026-09-25); this row's own log literals 9 lines / 4 files. | loc 53799, conflict 3(3), deps #497, #508, #509, #531, #966, #996, #1023, #1053, #887, #902, #903, #465, #825 | KEEP | - |
| nopr:8b869633a4 | Vendor the LCM context engine (v0.16.2) under plugins/context_engine/lcm + normalize wrapped tool schemas. — Commit body (2026-06-16); PRD-6 LCM adoption. | read-only: 496/504 added symbols exist in lcmup 8d1b1e6; the 8 misses are fork-relocated (backup_database/rotate_backup_database now in plugins/context_engine/lcm/maintenance.py, imported at command.py:51) or fork-only (_WrappedToolEngine, focus briefs). NousResearch side: agent/context_engine.py ABC exists upstream with should_compress_preflight@../up/agent/context_engine.py:156 — the ABC seam this row added is absorbed. | NousResearch ABC absorbed (agent/context_engine.py present upstream); LCM content = upstream's own | same as #480 — live engine; this row's log literals 44 lines / 1 file | loc 15792, conflict 3(3), deps #480 | KEEP | - |
| #497 | Below-threshold compactions announce themselves and name the cause; #480's banner shipped inert because LCM forces emit_automatic_compaction_status=False, and the host had no cause plumbing. — PR #497 body (346,375 tokens vs 750,000; two defects that survived #480). | read-only: lcmup engine.py:518 still `emit_automatic_compaction_status = False` unconditionally (defect 1 present upstream). NousResearch side: ../up/agent/conversation_compression.py:4006 _announce_compression_start — upstream grew its own announce lifecycle, but 0 hits for 'below.threshold' in up/agent/{conversation_compression,turn_context,context_engine}.py, so the cause-naming half has no counterpart. | none for the cause-naming; upstream's _announce_compression_start (no single sha cited) covers the generic 'announce before compaction' half | chat emit, not a log line: this row's 4 log literals = 0 hits; 'maintenance compaction' 4 + 'engine_preflight' 2 + 'below threshold' 2 lines in the window. Rare by design since #531/#508 gate the below-threshold arm. | loc 1148, conflict 5(3), deps #509 | KEEP | - |
| #168 | LCM ignored compression.target_ratio and kept a fixed 32-message fresh tail; map the knob to a token budget. — PR #168 body (2026-07-01; ~6.5K-token fixed tail on live traffic). | read-only: lcmup carries fresh_tail_max_tokens (command.py:804/851/893, 5 non-test hits) with tool-group integrity + orphan handling; `target_ratio` = 0 hits in lcmup. Ledger DD-1 ruled ADOPT-THEIRS. | lcmup fresh_tail.py (ledger DD-1, verified on 424a6b2 and still present at 8d1b1e6) | 0 log lines; the budget is applied silently every compaction (config.yaml compression.target_ratio: 0.2). Fork still carries _fresh_tail_start@engine.py:2874, _dynamic_fresh_tail_count@:1164, _refresh_fresh_tail_token_budget@:1136. | loc 927, conflict 1(1), deps - | SUPERSEDED-BY-UPSTREAM | - |
| #966 | FTS parity COUNT(*) over 2.5M rows ran under _LOAD_LOCK on every engine load — Apollo boot freeze #3 (card t_d3963974). — PR #966 body (measured on an APFS clone of the 10.9 GB db); card t_d3963974. | RED by read: lcmup db_bootstrap.py:2394 _fts_needs_rebuild_structural still runs `SELECT COUNT(*) FROM messages` (:2420) and `COUNT(*) FROM <docsize>` (:2429), and it is called from ensure_external_content_fts (:3092 via :2943/:3002), which store.py:391 calls in _init_db on every MessageStore construction. Upstream 7c5c960 (2026-09-03, 'serialize concurrent FTS bootstrap repair #570') adds a lock but keeps the COUNT(*). | none (7c5c960 is adjacent, not this) | 1,461 log lines / 20 files in the window (parity-marker + interval lines); 'parity check' 4 lines | loc 815, conflict 1(1), deps #1023, #903 | KEEP | - |
| #107 | Replayed rows all stamped time.time() at ingest (up to 1,049 rows on one instant) + dup-on-replay after compaction. — PR #107 body (2026-06-26). | read-only: timestamps — lcmup store.py carries observed_at / observed_at_source columns (store.py:445-452) and 'source time contract' (f68ceaa 2026-07-20) instead of our timestamp_original/_reconstructed/_verified (0 hits in lcmup); our upstream PR #444 was CLOSED unmerged 2026-08-08. Dedup — lcmup prevents the write via reconcile.py:421 _find_reconciled_cursor_for_store_tail (ledger DD-2: ADOPT-THEIRS; superseded_by = 0 hits upstream). | f68ceaa (source time contract) + reconcile.py (DD-2) | 0 log lines (silent path); 'reconcile' 20,123 lines in the window shows the replay path is hot | loc 758, conflict 7(3), deps #1053 | SUPERSEDED-BY-UPSTREAM | - |
| nopr:6c8202b164 | PRD-6 data-at-rest controls: per-row AESGCM encryption, 0700/0600 permission enforcement, synced-path detection, redaction corpus, retention. — Commit body (PRD-6 I-6/I-8). | read-only: RowCipher = 0 hits in lcmup; ledger wave-2: encryption/retention 'not sent (feature, not defect) — permanent fork divergence'; redaction regex PRs #457/#458 OPEN upstream unmerged. This row also introduced the fork-only `search_content` column (git log -S: first at 6c8202b164) that #887/#902/60ea/051c later had to defend. | none (#457/#458 open) | 2 log lines / 1 file; live lcm.db is mode 0600 (ls -la: -rw-------), so the permission half is in effect; sensitive_patterns_enabled not set in config.yaml (default False) so redaction is dormant; encryption key file not present under ~/.hermes (no lcm.key) — encryption not enabled. | loc 752, conflict 1(1), deps #887, nopr:60ea6b2b42, nopr:051c2076e1 | KEEP | - |
| #996 | Turn-start reconcile scan was O(n^2) per cursor (972,426 identity computations, 47 s dead air before the model was reached). — PR #996 body, card t_49c1f7d7 (Apollo dead air 2026-09). | RED by read: lcmup reconcile.py:449-470 rebuilds candidate_visible_messages / candidate_non_placeholder_messages list comprehensions inside `for cursor in range(len(messages), -1, -1)` — the same per-cursor rebuild; no memo (_replay_identity_memo_scope / _prefix_counts = 0 hits upstream). | none | 0 dedicated log lines (perf fix, silent); 'reconcile' 20,123 lines show the path runs every turn | loc 680, conflict 0(0), deps - | KEEP | - |
| #431 | Cherry-pick 9 upstream fixes (#263 #264 #265 #269 #278 #280 #282 #285 incl. the Anthropic HTTP 400 summary-role pin) into the vendored copy. — PR #431 body; /tmp/lcm-refresh-out/CHERRY-PICK-LIST.md (gone). | read-only: 29/30 added symbols exist in lcmup (ensure_candidate_loaded is the one miss, upstream renamed/relocated it); the content is upstream's own. | the 9 picked upstream PRs themselves | n/a (upstream content); 0 log literal hits | loc 634, conflict 0(0), deps - | KEEP | - |
| #1023 | Structural FTS repair ran DDL as separately autocommitted statements (index torn for 13-28 min) and ran on the engine-load thread; defer the rebuild off-thread and make DROP work on a fresh connection. — PR #1023 body, cards t_1e04c4b*/t_d3963974 follow-up to #966. | RED by read: lcmup db_bootstrap.py:2993 repair_external_content_fts has no BEGIN/transaction wrapping (grep BEGIN in that file only hits trigger bodies at :946/:960/:976/:1987/:2009) and no deferral (_defer_structural_rebuild / _run_background_fts_rebuild = 0 hits). Upstream 7c5c960 (#570) serializes concurrent repair but does not make it one transaction or move it off the load thread. | none (7c5c960 partial/adjacent) | 94 log lines / 5 files (gateway.error.log, tui_gateway_crash.log) in the window; 'structural rebuild' 2 lines | loc 563, conflict 0(0), deps - | KEEP | - |
| nopr:6622f41055 | LCM fail-open degraded fallback: observable degraded state, structured warning events, rate counters, over-context path that tries ContextCompressor before raising. — Commit body (2026-06-16); ledger DC-1. | read-only: fail_open = 0 hits in lcmup; their BypassMixin (bypass.py) is routing, not degradation; their compress re-raises. Ledger DC-1 ruled FORK-PERMANENT (partial adopt of their _compress_impl shape). | none — deliberate policy divergence (never-hard-fail-a-live-conversation) | 'fail-open' 9 lines + 'degraded' 606 lines in the window; this row's literals 0 hits | loc 505, conflict 0(0), deps - | KEEP | - |
| #531 | Maintenance compaction fired at 61% of threshold on a 100%-cached session, burning the warm prompt cache (cost to the next turn measured). — PR #531 body (session 20260806_084344, 8 consecutive 100%-cache calls; Ace: 'why did it compact again'). | RED by read: max_cache_hit_ratio / cache_hit = 0 hits in lcmup compaction.py/config.py. Our upstream PR #516 ('gate opportunistic maintenance compaction on token pressure') is OPEN unmerged. | none (PR #516 open) | 0 log lines (gate is silent when it holds); config.yaml compression.maintenance_max_cache_hit_ratio: 0.9 is set, so the gate is armed on the live gateway | loc 308, conflict 1(1), deps - | KEEP | - |
| #508 | compression.maintenance_min_pressure_ratio from config.yaml never reached LCMConfig via the production loader — #506's floor was inert (a 4th premature compaction at 29% of threshold landed after the deploy). — PR #508 body (measured 221,011 tokens, 29%). | read-only: maintenance_min_pressure_ratio = 0 hits in lcmup (upstream has critical_budget_pressure_ratio@config.py:484, a different knob); the bridge is fork-only config plumbing for a fork-only knob (config.yaml: maintenance_min_pressure_ratio: 1.0). | none (our PR #516 carries the pressure gate concept) | 0 log lines (config bridge); the knob is set to 1.0 live, i.e. the gate is fully armed | loc 122, conflict 1(1), deps #531 | KEEP | - |
| #465 | Active-context assembly dropped tool rows but never merged the adjacent bare assistant rows left behind — strict-alternation violation every downstream load had to repair. — PR #465 body. | RED by read: _merge_adjacent* = 0 hits in lcmup; engine.py:6010 _assemble_context has no adjacent-assistant merge. Our upstream PR #467 ('merge adjacent assistant rows in active-context assembly') is OPEN unmerged. | none (PR #467 open) | 0 log lines (silent normalization on every assembly) | loc 238, conflict 0(0), deps - | KEEP | - |
| nopr:051c2076e1 | msg_fts_* triggers existed by name but referenced the dropped `content` column after the content->search_content rename; name-only health check reported healthy while every INSERT aborted. — Commit body (2026-06-19). | GREEN by absence: search_content does not exist in lcmup (0 hits, never did per git log -S), so this drift class cannot occur upstream. lcmup db_bootstrap.py:2280 does compare normalized trigger SQL to expected (a generic form of the same check). | db_bootstrap.py:2280 normalized-trigger compare (generic; no sha isolated) | 0 log lines in the window (repair path; the drifted DBs were fixed in June) | loc 228, conflict 0(0), deps - | KEEP | - |
| #1053 | Per-session reads (get_session_tail, load_session_page, ...) returned soft-hidden superseded replay copies, so re-bind reconcile matched against hidden rows. — PR #1053 body, card t_9cdad21b (2026-09-25). | GREEN by absence: superseded_by = 0 hits in lcmup (upstream prevents the duplicate write, ledger DD-2), so there are no hidden rows to leak. | reconcile.py (DD-2 prevention design) | landed 2026-09-25, 0 log literal hits (too new; read path is silent) | loc 227, conflict 0(0), deps - | KEEP | - |
| #903 | Regression layer for the boot-scan class (a plan-level test that fails on any full-table scan during MessageStore init) + a loud signal when engine load stalls every turn. — PR #903 body (two Apollo freezes 2026-09-22, #887 + #902). | read-only: lcmup has no init-cost contract test; upstream's _init_db still contains an unconditional full-table UPDATE (store.py:454) so the class is live there. | none | 0 log lines (test layer + a stall signal that has not tripped in the window) | loc 224, conflict 0(0), deps - | KEEP | - |
| #509 | A '📦 lcm maintenance compaction' banner with no '🗜️ Context compacted' after it: 4 of 21 compressions announced then did nothing (214->214 messages). — PR #509 body (session 20260807_183558 timestamps). | read-only: preflight_is_user_visible = 0 hits in lcmup; NousResearch agent/turn_context.py has no equivalent gate (upstream's _announce_compression_start@conversation_compression.py:4006 announces before the feasibility probe by design, comment at :4078). | none | 0 log lines; the no-op class ('compression done' with equal counts) not observed in the window's 201 'compression done' lines was not re-measured — indirect | loc 211, conflict 1(3), deps - | KEEP | - |
| #412 | Plugin loaders set sys.modules[name]=mod BEFORE exec_module; concurrent child agents in a shared ThreadPoolExecutor could grab a half-initialized module (no register()/engine class yet). — PR #412 body (2026-07-21). | RED by read: NousResearch ../up/plugins/plugin_loader.py:75 `sys.modules[name] = mod` then :85 `exec_module(mod)` with no lock in that module (b54cc716c7 2026-09-02 'shared path-based plugin loader'); only plugins/memory/__init__.py:381 gained a `_discovery_lock` (684a2cfbd7) and it guards discovery, not the exec window for context_engine/cron_providers. | none (684a2cfbd7 partial, memory only) | 0 log lines (a race; the fork's fix is silent). Structural proof only: delegation.max_concurrent_children: 32 live. | loc 203, conflict 0(0), deps - | UPSTREAM | - |
| #887 | _backfill_search_content ran `SELECT ... WHERE search_content IS NULL` with no usable index on every boot (~21 min on 10.9 GB, under the engine-load lock) — Apollo freeze. — PR #887 body (measured 2026-09-22). | GREEN by absence: no search_content column or backfill in lcmup (fork-only schema from 6c8202b164). | n/a | 0 log lines for this row's literal; the fix is that the boot no longer scans — 'FTS' 815 lines in the window | loc 200, conflict 0(0), deps - | KEEP | - |
| nopr:60ea6b2b42 | After the 2026-07-25 parity merge live DBs had the old content-column FTS table; init ran _ensure_storage_columns() before the FTS self-heal so the repair could never run. — Commit body (2026-07-25 incident). | GREEN by absence (search_content is fork-only); lcmup store.py _init_db@357 calls ensure_external_content_fts@391 in its own order with no storage-column step. | n/a | 10 log lines / 3 files in the window | loc 118, conflict 0(0), deps - | KEEP | - |
| #825 | LCM's private Codex OAuth cap table substring-matched 'gpt-5.6' against 'gpt-5.6-sol-900k' and clamped the window to 372k for a week. — PR #825 body; ledger DC-3 (2026-09-21). | read-only, PARTIAL: lcmup codex_routing.py has _CODEX_OAUTH_EXACT_CONTEXT_CAPS (#557 a646bb0, 2026-09-03) covering sol/terra/luna-900k but not gpt-6-astra-900k and still 'gpt-5.6': 372_000; our upstream PR #617 (host deferral + 372->272) is OPEN. | a646bb0 (partial) | 0 log lines (silent cap); live cron models include gpt-6-astra-900k (moa config) which upstream's exact table does not cover | loc 113, conflict 0(0), deps - | KEEP | - |
| nopr:efe50db808 | Identifier-fidelity instruction was hardcoded into the summarizer prompt with no off-switch, so the baseline-repro arm could not reproduce the merge bug (infinite K=2 rerun loop, ~$40/3h). — Commit body (2026-06-19). | read-only: identifier_fidelity = 0 hits in lcmup; ledger wave-2 'identifier-fidelity prompt block: not sent (prompt taste), permanent fork divergence'. | none | 0 log lines; the toggle _identifier_fidelity_enabled@escalation.py:437 gates a prompt block on every L1 summary ('identifier' 2,111 log lines are mostly unrelated). The scripts/lcm_arm_b_node_recovery.py half is gone from the fork. | loc 108, conflict 0(0), deps - | KEEP | - |
| nopr:dc4245ab4c | _synthesize_expansion_answer did response.choices[0].message.content unguarded; under concurrency an error-shaped aux response crashed the whole lcm_expand_query. — Commit body (live Arm-B 0/N node-served recall). | RED by read: lcmup tools.py:1720 `content = response.choices[0].message.content` unguarded (fork guards it with _ExpansionSynthesisError@tools.py:1681/:1731). | none | 0 log lines for the degrade message in the window; 'lcm_expand_query' 1,929 lines show the tool is heavily used | loc 84, conflict 0(0), deps - | UPSTREAM | - |
| #902 | `UPDATE messages SET ingested_at = timestamp WHERE ingested_at IS NULL` scanned 10.9 GB on every boot (second freeze); gate one-time backfills behind a migration marker. — PR #902 body (2026-09-22 20:5x, one boot after #887). | RED by read: lcmup store.py:454 runs the identical unconditional UPDATE in _init_db on every construction; is_migration_step_complete = 0 hits upstream. | none | 0 log lines (the fix is the absence of a scan); the boot path runs on every gateway start | loc 82, conflict 0(0), deps - | UPSTREAM | - |

## Notes per row

- **#6** (KEEP): Plugin is fork-permanent (D4a bucket-3 re-audit 2026-07-25: zero outbound calls, so none of the ungated-telemetry predicates hold; upstream's June-2026 'no third-party product plugins' rule still makes it un-upstreamable as a plugin). Grade the seams separately: the tools/delegate_tool.py lineage seam is SUPERSEDED-BY-UPSTREAM (0d9b7132ff) — at next sync take upstream's delegate_tool.py and re-point the plugin at parent_turn_id. agent/conversation_loop.py hunks remain fork cost (conflict 4 files across 3 syncs). Action for lead: register blackbox in fork-features.json (lifecycle fork-permanent) — it is unregistered today.
- **#141** (KEEP): Measured in use. Cost is the highest of the blackbox rows: 24 files, gateway/slash_commands.py + locales/*.yaml conflict every sync (3/3). D4a/D7 already promoted this: move the card rendering INTO the plugin (plugin-registered slash command via the seam upstream now provides, 5943347a2a) so slash_commands.py carries zero blackbox lines. That is a fork_ext extraction card, not a drop.
- **#968** (KEEP): Fork-permanent plugin, 0 conflict files.
- **#432** (KEEP): Fires this week. 2 conflict files (2 syncs).
- **#154** (KEEP): Seam in agent/usage_pricing.py conflicts 2/2 syncs; the vendor-fallback half is generic and upstream's chain now covers it (models.dev) — at next sync converge the fallback on upstream's get_pricing_entry and keep only reprice in the plugin.
- **#1056** (KEEP): Plugin half (terminal_error column, orphans.py detector) KEEP. Seam half (emit_unfinalized_session_end in run_agent forwarder + conversation_loop) is a fork-shape fix; at next sync adopt upstream's finalize_turn chokepoint and drop the forwarder backstop — conflict 4 files (3 syncs) is all seam.
- **#799** (KEEP): 0 conflict files. Fleet-private (sub_key / lane_family).
- **#444** (KEEP): Half dead: cli_invocation_id has never been populated in the 30d window — the codex_runtime.py:764 writer is on a path the fleet does not exercise. Recommend trimming the cli_invocation seam (agent_init/turn_finalizer/codex_runtime hunks, 5 conflict files x3 syncs) and reading depth from upstream's _delegate_depth. Not a full DROP because depth is live.
- **#1014** (KEEP): Half duplicates upstream: the fork re-implemented _configured_home_channels at plugin_api.py:2290 where upstream has it at :1138 — converge that half at next sync (dashboard dist/*.js + plugin_api.py conflict 1 file x3 syncs). Facet half rides the fork's home-session concept. Unmeasured because it is <24h old; adversary should re-check fires.
- **#20** (KEEP): Plugin half KEEP (measured). gateway/run.py seam is SUPERSEDED-BY-UPSTREAM 5943347a2a — drop the fork's binding at next sync (conflict 3 files x3 syncs, gateway/run.py is the rank-1 god-file).
- **#202** (KEEP): Fold works for the common case; 2 residual leaks/30d from relay lane names — a small follow-up, not a drop. 1 conflict file (1).
- **#40** (KEEP): conversation_loop.py hunk conflicts 2/3 syncs; candidate for the same in-plugin extraction as #141.
- **#10** (KEEP): 1 conflict file (1) = plugins/blackbox/__init__.py itself.
- **#580** (UPSTREAM): CORRECTED mid-run: first draft said SUPERSEDED because a probe misattributed the fork's guard lines to the upstream file. Generic fix (no fleet-private concept; _is_past fails open on unparseable timestamps). Upstream branch audit/plugins/upstream-580 to be built in the branch pass; the fork commit is a clean 2-file diff (chronos/__init__.py + tests) with no fork-only symbols.
- **#9** (KEEP): Chat fields + cache% live; the alerts toggle half is dormant (see #44). 4 conflict files x3 syncs, mostly agent/conversation_loop.py + model_metadata.py seams.
- **#905** (KEEP): 0 conflict.
- **#915** (KEEP): 0 conflict.
- **#972** (KEEP): 0 conflict.
- **#660** (KEEP): 0 conflict.
- **#955** (KEEP): 1 conflict file (1).
- **#44** (DROP): Dead code path, but NOT a clean revert: `git revert 930d367238` off origin/main conflicts in plugins/blackbox/card.py (later fork commits built on _tokens_out_line: _tokens_in_label, zero-is-absence narrowing referenced from store.py:394). Reverting one 30-line cosmetic on a fork-only file buys zero parity (card.py conflicts by ledger listing only). Correct drop unit is the dormant ALERT feature as a whole (#9's alerts toggle + __init__.py:548-556 send_card path + render_card) via a plugin-cleanup card, not this commit alone. Revert branch deliberately not pushed; worktree aborted.
- **#912** (KEEP): 0 conflict.
- **#30** (KEEP): agent/model_metadata.py hunk conflicts 3/3 syncs for a display count — candidate for the in-plugin extraction with #141/#40.
- **#149** (SUPERSEDED-BY-UPSTREAM): 4-line patch to a built artifact; the review half is already upstream, the scheduled half only exists because the fork dashboard has a scheduled column upstream lacks. Taking upstream's dist wholesale is the sync-time resolution — no revert branch needed for a dist file.
- **#480** (KEEP): Vendored subtree = fork-permanent by construction (no shared history). The audit question for this row is the D1a refresh decision (bump to v1.0.0-rc.1 vs cherry-pick vs stay), which the ledger already frames (DD-1/DD-2 ADOPT-THEIRS, DC-1/DC-2/DD-4 FORK-PERMANENT, DC-3 partial). Not a drop. The 3 conflict files (3 syncs) are the NousResearch-side seams agent/conversation_compression.py + agent/turn_context.py, not the vendored tree — graded with #497.
- **nopr:8b869633a4** (KEEP): Fork-permanent vendored tree. 3 conflict files (3 syncs): agent/agent_init.py + agent/context_engine.py are ABC-wiring seams that upstream now owns — at next sync take upstream's agent/context_engine.py and keep only what the lcm plugin needs. Superseded by #480's re-vendor for content.
- **#497** (KEEP): Highest-cost LCM row: 5 conflict files x 3 syncs, all NousResearch seams (context_engine.py, conversation_compression.py, model_metadata.py, turn_context.py, hermes_cli/config.py). Measured only indirectly (the arm now rarely fires) — adversary may push this to UNRESOLVED. Recommendation: at next sync re-thread the announce onto upstream's _announce_compression_start lifecycle and keep only the LCM-side cause string; that converges 4 of the 5 seams.
- **#168** (SUPERSEDED-BY-UPSTREAM): Verdict = ledger DD-1 (ADOPT-THEIRS): delete our three tail functions, adopt their fresh_tail.py, re-express target_ratio as a ~5-line fresh_tail_max_tokens computation at config load. Landmine: same-named defs, delete explicitly. Executes at the next vendor refresh, not as a standalone revert (the two scripts/ paths in this row are already gone from the fork).
- **#966** (KEEP): Fires constantly; upstream RED. Generic (any large lcm.db pays it) — a strong contribute-back candidate on top of their #570, but framed against their db_bootstrap shape. 1 conflict file (1).
- **#107** (SUPERSEDED-BY-UPSTREAM): 7 conflict files x 3 syncs — the second-costliest LCM row, and 6 of the 7 are NousResearch seams (acp_adapter/session.py, agent/conversation_loop.py, cli.py, gateway/{platforms/api_server,session}.py, hermes_state.py) threading per-message timestamps. Ledger DD-2 says drop the superseded_by machinery and adopt their reconcile.py (+placeholder_ledger.py, priced). But note #1053 (2026-09-25) BUILDS on superseded_by — dropping #107 drops #1053 too; the pair goes together at the refresh. The timestamp half: upstream chose observed_at; converge on theirs.
- **nopr:6c8202b164** (KEEP): Partially dormant: permissions live, redaction+encryption off. Ledger flags the dormant openai_key regex defect (fix before enabling). Fork-permanent per ledger; the search_content schema it introduced is the root of the boot-scan class — a refresh that adopts upstream's schema retires #887/#902-half/60ea/051c together. 1 conflict file (1).
- **#996** (KEEP): 0 conflict files. Generic perf fix on upstream's own reconcile.py shape — UPSTREAM-worthy, but DD-2/DD-4 say we may adopt their reconcile.py wholesale at refresh; if so this becomes a contribute-back against their file rather than a fork carry. Keep until then.
- **#431** (KEEP): Superseded in content by the #480 re-vendor to v0.21.0-rc2 (which includes these picks). 0 conflict. Nothing to drop separately.
- **#1023** (KEEP): 0 conflict. Fires. Contribute-back candidate stacked on their #570.
- **nopr:6622f41055** (KEEP): Ledger DC-1: keep behaviour, adopt their _compress_impl split at refresh to shed conflict hunks in engine.py; fix our-side 0.20 vs target_ratio inconsistency. 0 conflict files.
- **#531** (KEEP): Measured-by-absence: the class of premature compaction it stops has not recurred in the log window ('maintenance compaction' 4 lines). Already pitched upstream (#516). 1 conflict file (1).
- **#508** (KEEP): 1 conflict file (1) = lcm/config.py. Rides with #531/#516.
- **#465** (KEEP): Already pitched upstream. 0 conflict. Absorb signal 18/48 is shared scaffolding, not absorption.
- **nopr:051c2076e1** (KEEP): Defends a fork-only schema (see 6c8202b164). Retires automatically if the refresh adopts upstream's schema; until then a required repair path for existing fleet DBs. 0 conflict. Not a DROP: reverting it would leave older DBs unrepairable.
- **#1053** (KEEP): Correct fix for the fork's tolerate-and-hide design; goes away with #107 when DD-2 executes. 0 conflict.
- **#903** (KEEP): Tests-only + docs + one signal in plugins/context_engine/__init__.py; 3 test-helper symbols no longer on origin/main by name (refactored since) — verify tests/context_engine/test_lcm_init_cost_regression.py still collects before any refresh. 0 conflict.
- **#509** (KEEP): 1 conflict file but 3 syncs (agent/turn_context.py). Converges with #497 onto upstream's announce lifecycle at next sync.
- **#412** (UPSTREAM): Generic. Re-port target is upstream's single plugin_loader.py (one lock), not the fork's three per-loader locks. Upstream branch to be built in the branch pass (audit/plugins/upstream-412) — needs re-authoring against plugin_loader.py, not a rebase. 0 conflict.
- **#887** (KEEP): Required as long as the fork carries search_content. 0 conflict. Retires with the schema.
- **nopr:60ea6b2b42** (KEEP): Fires. Fork-schema repair path. 0 conflict.
- **#825** (KEEP): Ledger DC-3 executes: at refresh adopt their exact table + keep our host deferral; canary tests/context_engine/test_lcm_codex_context_variant_cap.py. Already pitched (#617). 0 conflict.
- **nopr:efe50db808** (KEEP): Small, 0 conflict, fork-permanent per ledger. The toggle's off-arm only mattered for the June experiment; the on-arm is the live prompt.
- **nopr:dc4245ab4c** (UPSTREAM): Generic defensive fix on upstream's own tool. Upstream branch audit/plugins/upstream-dc4245ab4c in the branch pass (small: one guard + one test). 0 conflict.
- **#902** (UPSTREAM): Generic, small, upstream's own line. Upstream branch audit/plugins/upstream-902 in the branch pass. 0 conflict.
