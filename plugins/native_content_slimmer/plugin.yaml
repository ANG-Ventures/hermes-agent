name: native_content_slimmer
version: "0.1.0"
description: Native tool-result slimming with lossless offload plus optional eval-gated semantic-compression views. Disabled by default; shadow mode is the default when explicitly enabled.
author: Apollo
kind: standalone
provides_hooks:
  - transform_terminal_output
  - transform_tool_result
